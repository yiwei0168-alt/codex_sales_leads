# Stage 221: 11:50 UTC scheduled ECB FX recheck

The stored due time was 2026-09-14T11:50:06.101Z. One due credential-free official ECB GET returned a parsable but still dated 2026-09-11 reference. The refresh returned `status=unavailable`, `failureClass=staleOfficialReference`, `httpCalls=1`, `paidProviderCalls=0`, `jobsClaimed=0`, and stored the next attempt at **2026-09-14T12:50:36.977Z**. Do not retry before that time. The retained FX reference remains `expired-or-invalid`, as of Sep 11 with effective expiry Sep 14 00:00 UTC. Kimi k2.6/k3 and Beijing text-embedding-v4 tariff evidence itself remains within its verification window through Sep 20; their current `expired-tariff` admission result is caused by missing usable CNY FX, not by changing those model prices.

The following read-only budget check stayed at USD30 limit, USD12.324404 occupied and USD17.675596 remaining, with six unknown historical bills, zero unsettled calls and zero new paid provider calls. A separate read-only minimal-run preview continued to show `totalRunBoundUsd=null`: Extract, Terra and Sol judge single-call tariffs are present, while Kimi intent and embedding remain FX-held. No real A11 business run started.

Efficiency: one free official reference input produced zero valid fresh rates and zero downstream-usable FX outputs; one `staleOfficialReference` result was discarded, giving 0% useful refresh yield. Paid model tokens, API credits, cash, retries, mail and discarded business outputs were zero. Real user adoption and provider latency remain unknown. Opportunity: retain one shared scheduled retry and the conservative paid hold until the official reference date advances; a fresh FX rate alone would still not establish the missing whole-run A11 bound.
