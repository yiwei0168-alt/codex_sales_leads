# P06 最终评分来源引用有界压缩（阶段164）

分阶段最终评分原先在每条事实中重复写入原证据 ID 及阶段引用 ID；另有来源行保留同一 ID。即使事实阶段已完成，一个151来源/155事实的隔离合成公司仍可能在 OpenAI 兼容备用的最终完整 HTTP 请求中越过61,440字节上限，停为技术待恢复。本阶段在**原完整请求超限时**，把事实行里的两组重复证据 ID 表示为 `sourceRows` 的零基索引。`sourceRows`保留每个原 ID 和来源类型，模型指令明确要求先还原原 ID 再判断与引用，输出仍使用原 ID。输入缺失来源或引用不能无损还原则在付费前拒绝；原候选、证据、评分标准、批次5上限及路由均不变。

真实 `ResilientAiProvider` 合成路由、DeepSeek 与 OpenAI 兼容序列化字节合同的无外发单元测试证明，该151来源/155事实夹具的原最终请求超61,440字节，压缩后 OpenRouter 请求附加参数下的 `deepseek/deepseek-v4-flash`、`openai/gpt-4o-mini`、`openai/gpt-4o` 以及主路由均不超过上限；逐条反解引用与原 ID 一致，原请求对象未改。压缩版本进入请求内容、缓存和付费指纹；旧已完成响应的合同不能冒名复用，原有“其他已完成合同不得自动重付费”门禁继续适用。阶段/最终检查点的隔离 PostgreSQL 验证再次通过，跨进程加载、部分恢复、条件 Pro 及未知/错哈希拒绝均保留。

验证：1011项/207文件、TypeScript、Next.js生产构建、全库lint 0错误/11既有警告、生产依赖审计0漏洞；检查点真实SQL脚本通过并清理隔离夹具。真实供应商调用、token、搜索额度、现金、发信均0。本阶段是请求载荷与恢复身份验收，不证明模型能正确理解压缩引用或该夹具在完整备用图中产生合格公司；任意长单事实和151来源完整备用图、真实账单及 A11 仍需验收。冻结哥伦比亚测评未变。

效率口径：输入1个合成公司、155条校正事实、151个本轮来源；本地有效请求预检1、真实模型输出/下游合格/用户采用均未知，故不计算真实合格线索单位成本。原事实与原来源丢弃0，真实付费重试0。优化机会是对更多长输入测量可逆索引节省及模型语义稳定性，再核验备用严格费率与整次预算；不能用本地可容纳率推断市场填充率。
