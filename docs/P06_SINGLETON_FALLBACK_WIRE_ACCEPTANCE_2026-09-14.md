# Stage 219: plan oversized singleton chunks against every approved route's wire bytes

A no-paid fallback fixture exposed that a chunk fitting the DeepSeek primary request could exceed 57,344 bytes when serialized for an OpenAI-compatible fallback. The qualification Agent now asks the chunk planner for the maximum **actual provider request bytes across its approved execution routes**, then verifies each completed chunk request against every route again. It does not reuse the primary model's size estimate as the fallback contract.

The synthetic Agent test routes one chunk through a distinct OpenAI-compatible model/contract, completes the score, records fallback usage and replays with zero calls. The isolated product SQL verifier saves a fallback chunk under its own model/provider/paid fingerprint, loads it in a second process, rejects treating that completed fallback as a primary-route completion, and removes the fixture. The original unresized fallback fixture failed its wire-size assertion before any provider call; the route-aware plan passes. Full 1,043 tests in 208 files, TypeScript, focused ESLint and production build pass. No real model, search, paid credit, customer job or mail was called.

This proves request sizing and saved-route identity for the tested synthetic fallback. It does not activate a missing default fallback tariff, prove real provider semantics, or complete the 151-source graph or A11 whole-run gate.

Efficiency: one synthetic oversized-company input and one isolated fallback checkpoint input yielded valid bounded route plans and cross-process saved output, both used in local assertions. The unsafe initial fallback plan was discarded before transport; resumed work reused the completed chunk. Product downstream adoption and real latency remain unknown. Paid tokens, API credits, cash, external retries and discarded business outputs were zero. Full regression about 22 seconds, isolated SQL verifier about six seconds. Opportunity: use the tightest actual route serialization when dividing paid work so fallback cannot turn a purchased primary segment into a repeated or unbounded request.
