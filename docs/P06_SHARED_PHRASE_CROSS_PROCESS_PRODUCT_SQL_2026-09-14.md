# 阶段131：同一压缩评分任务跨进程进入产品 SQL

阶段129的跨进程评分与阶段130的产品 SQL 此前分别验收。本阶段把 `scripts/verify-shared-phrase-score-checkpoint.ts sql` 改为同一随机隔离任务的两进程路径：第一进程用 `updateState` 预置已校正候选并在真实图的评分边界暂停，PostgreSQL 保存101条当前证据、105条事实及运行身份；第二进程读取同一检查点，由当前 `LeadQualificationAgent` 构造完整 `exact-shared-phrase-v1` 请求，经 DeepSeek 实际序列化的61,440字节预检后调用内存模拟模型1次，再由产品 `persistLeadWorkflowResult` 写入实际业务表。发现、补证、角色校正和路由在本夹具中均未执行；第一进程模型调用0，旧的独立两进程模式继续通过。

第二进程核对用户、动作和原计划，其他用户/动作不能恢复；完成评分的缓存契约等于从恢复候选重新计算的合同。数据库读回的候选评估1条、证据和事实逐项等于原输入，证据快照101条，评分阶段指标及模型用量各1条；指标保留请求准备编码，现金费用字段仍未知。夹具缺少资格证据，因此确定性合格和交付均为0，未制造合格公司。第二账号不可读该评估，当前动作无付费预留。随机夹具的账号、工作区、业务行及检查点在断言后清理。

该验收从预置的已校正合成候选开始，使用模拟模型和隔离SQL。它没有执行自然语言输入、真实搜索/补证、真实模型对压缩字典的理解、可信供应商账单或用户采用；也未解决仍不可压缩的任意大单公司分阶段处理。P06的部分接线边界更完整，但A11真实业务闭环及整体验收仍未通过。

验证：`sql`两进程模式及原独立两进程模式通过，970项/203文件测试、类型检查、生产构建、全库lint 0错误/11项既有警告和生产依赖审计0漏洞通过。真实模型/搜索/SMTP调用、token/API credits、现金和付费重试均为0。合成评分有效1、产品SQL评估保存1、最终合格0、真实用户采用未知；本地两进程SQL脚本约7秒，真实供应商延迟未知。
