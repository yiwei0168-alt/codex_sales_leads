# Stage 204: A11 requested discovery result slots

The read-only minimal-run preview now distinguishes request capacity from provider output. For the synthetic Colombia / Distributor / target-one plan, the current policy selects one `distribution/strategic` track with four route steps. `plannedCandidatePool` yields 2 in round 0 and at most 6 thereafter while the accepted count remains zero; `discoveryResultsPerRoute` applies the policy default of 12 and maximum of 20. Thus every scheduled route step requests at most 12 results. Five permitted rounds times four steps times 12 requested results equals **240 requested result slots**. Conditional steps can be skipped, and duplicate results can appear across calls. This is neither 240 unique companies nor 240 qualified leads.

The actual returned-item ceiling remains **unknown** (`actualReturnedItemsAtMost: null`). The current Brave and Exa adapters pass their complete `web.results` and `results` response arrays into local candidate processing without slicing them to the requested count. SearchAPI and Gemini do slice their locally parsed result arrays. Provider HTTP-attempt bounds and requested-result slots also do not limit Gemini's server-side Google searches. This stage does not change candidate admission, the route, result truncation, tariff policy or historical Colombia results.

Validation: the read-only preview returned `requestedResultsPerRouteAtMost=12`, `requestedResultSlotsAtMost=240`, `actualReturnedItemsAtMost=null`, `totalRunBoundUsd=null`, `providerCalls=0`, `accountsModified=0` and `jobsClaimed=0`; TypeScript and script ESLint passed. The preview read the existing budget occupancy of USD12.324404/30 and remaining USD17.675596. No paid transport, model call, job claim or email was made. The next cost-admission work must bound *actual* downstream item volume and the remaining strict provider contracts before calculating a whole-run reservation. A requested count must not be substituted for that bound.
