# 处理恢复任务共享预算（阶段81）

实施 D13/O01–O02 已授权恢复边界的预算基础，不新增路由、模型或预算额度。历史部分结果恢复的创建、确认、执行与结果关联尚未接通。

迁移055增加同用户、同会话、同国家及原角色范围的追加式恢复关联，父任务必须已完成、子任务必须是未付费的新提案。关联和每次费用预留、预算修改均持有用户预算行锁。恢复任务继承所有祖先上限，每个上限按其整条恢复后续链累计占用；未知或未结束的祖先请求阻止新的预留。普通独立续搜保持原有规则。

预算API兼容增加 inheritedTaskLimits。自身及继承上限使用共享占用，阶段、公司、模型明细仍只展示当前任务，页面明确两种范围。此处不把服务商报告、估算和预留重复相加。

验证：全量860测试/189文件、生产构建与局部lint通过。真实PostgreSQL脚本 `scripts/verify-processing-recovery-budget.ts` 应用055并验证并发两个15微美元请求只有一个成功、三级上限累计28微美元、下调不得低于子任务占用、祖先未知费用阻止、RLS用户隔离、国家范围不匹配拒绝及关联不可修改。最终29微美元均为合成记录，fixture清理，真实供应商请求/费用/发信均为0。

本地3017更新至本阶段构建，桌面1366/移动390共54组Chrome回归通过，合成账号与数据已清理。继承上限的数值由真实SQL/API函数验证；此次通用Chrome回归没有预置恢复链，不能作为继承列表的专门浏览器验收。

兼容与回滚：迁移须先于新代码部署；既有任务无恢复关联时仍独立计算。迁移不改旧账单、评分或结果。若需回滚，先停用恢复入口并核对没有有效关联与在途请求，回退应用后才考虑移除新表/函数；存在恢复关联时不能回退到仅按单任务ID计费的旧代码，否则会失去共享上限。未执行破坏性回滚。

效率记录：运行时沿用每个预留的聚合输入量、未知输出/下游采用、token/API额度、延迟和重试观测，预算修改仍追加原有审计。此次无新增模型输入、生成或采用，不能声称采用率提高；可确定收益是阻止重复恢复绕开预算。后续优化机会是原子创建关联提案并复用有效检查点，仍需证明证据有效期和依赖一致。
