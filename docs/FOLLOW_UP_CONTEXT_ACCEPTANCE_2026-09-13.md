# 跟进上下文验收（阶段67）

已修复祖先邮件以对象字符串比较地址的问题：不同地址对象都可变成 `[object Object]`，造成错误匹配；改用规范化地址集合比较，保留原始邮件正文。同时递归祖先查询限定原工作区，沿用用户、公司、国家、已发送状态和深度8限制。

`scripts/verify-follow-up-context.ts` 使用隔离禁用账号、禁用邮箱连接、合成加密正文，调用实际 `followUpContext` 和应用数据库角色；不调用模型、邮箱服务或发送函数。真实SQL验证大小写地址匹配、错误收件人排除、原国家/工作区祖先边界、其他用户拒绝、同国家角色active风格记忆、同联系人导入来信及重复读取不变。来信仍仅称为往来记录，不假定回复或承诺。

最终835测试/183文件、生产构建含类型检查及改动文件lint通过。真实API/token/费用/发送均0，测试未创建费用预留。初次fixture参数uuid/text冲突已修正；下一次业务检查通过后清理遇到mailbox_message_company外键顺序，事务回滚。对该次两名合成用户及唯一合成公司完成身份核对后清理；随后完整复跑通过且全部fixture已清除。

观测范围：7条合成已发送记录、2条导入记录、4条风格记忆；首个上下文选择1条祖先/1条来信/1条风格记忆，其他请求测试隔离与重复，不算新的生成或用户采用。真实生成延迟、模型成本、用户采用率未知。改动降低无关上下文进入请求的风险，不宣称实测模型质量或节费比例。

仍缺策略邮件GET/UI复用、实际生成跟进请求及模型响应、页面审核与RAG完整链路；本报告不代表A13整项或整体验收通过。无数据库迁移；回滚代码会恢复旧地址比较行为，历史数据无需改写。
