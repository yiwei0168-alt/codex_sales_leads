# Stage 224 — P06 large-source fallback graph across product SQL processes

The current qualification Agent and product PostgreSQL checkpoints now run one synthetic company with 151 current sources and 151 corrected findings through a combined fallback and interruption graph. The first process completes phase one on an OpenAI-compatible fallback route and then hits a controlled budget denial before phase two. Its fallback provider/model, cache identity and paid fingerprint are saved in the append-only phase checkpoint. A second process loads that completed phase, runs only missing primary phases and a synthetic fallback final score, then saves the final response. A third process returns the completed assessment from checkpoints with zero model calls and zero new usage. Each resumed primary and fallback request is measured against the corresponding actual DeepSeek/OpenAI-compatible serializer and byte ceiling.

The independent in-memory Agent fixture from stage 223 covers 151 sources and 155 findings under actual fallback wire; this SQL fixture has 151 findings. The separate long-critical chunk SQL fixture remains intact. The new combined verifier initially failed only because its count assertion incorrectly expected the other fixture's 155 findings; correcting it to this fixture's actual 151 made the full verifier pass. No product implementation, score, route, tariff or confirmed rule changed.

Verification: the full `verify-qualification-phase-checkpoint-sql.ts` verifier passed, including prior tenant/country isolation, unknown-fee replay denial, chunk interruption, conditional Pro and fixture cleanup; TypeScript and changed-script ESLint passed. All requests and responses were synthetic; real provider calls, paid tokens, credits, cash, customer tasks and mail were zero. This closes the combined large-source **synthetic cross-process fallback recovery** check, but real model semantics, the default fallback strict fee contract and A11's paid natural-language closed loop remain unverified.

Efficiency: one 151-source/151-finding synthetic candidate yielded one saved fallback phase, one resumed completed score and one zero-call replay, all used in local SQL assertions. The attempted second phase was deliberately denied before a model response and was the only discarded test attempt. No customer-qualified output or downstream adoption was generated; actual adoption and provider latency remain unknown. External retries and cash were zero; the full SQL verifier took about twelve seconds. Opportunity: preserve exact alternate-route work across process restarts and avoid rebuying a completed phase when a later stage pauses.
