# 已保存结果的恢复执行接线（阶段86）

2026-09-14：阶段86生产build已通过，3017最新构建（session67951）桌面/移动端54组回归通过，fixture清理，无付费/发信；新恢复页面入口仍未开放。

生产执行器现已消费阶段85初始化结果，建立子运行检查点；不再统一拒绝所有恢复任务。恢复标识随检查点保存，构建计划后进入专用证据节点，发现节点另有禁止恢复任务进入的检查。恢复仅包含原来源范围中的待处理公司，原已选结果与费用留在父运行。

证据播种保留ID、采集时间与priorRunId，仅将已核验可用证据绑定到新运行；待补证来源标记stale，不把复制时间当采集时间。角色校正与评分进入当前代理的实际缓存契约检查，不直接复制旧模型结果绕过依赖核验。知识检索和计划仍使用现有流程及其缓存，未擅自降低费率门禁或改模型路由。

专用补证按候选逐项检查点保存。明确需要重验的公司调用原限定域名补证入口，并关闭其普通过期公共库快捷返回。无有效输出时，先保存本次费用计数与暂停状态，再在独立节点抛出未完成错误；不会自动循环重试。用户再次明确恢复后，仅执行剩余公司；未知费用仍阻止重放。有效公司进入校正、路由、评分、复核和新运行保存；最终目标不满足时保持shortfall，禁止补池发现。

验证：875测试/192文件通过；新增图测试覆盖有效证据跳过补采、跳过发现、第二家公司无效结果暂停、首项结果和4个合成API额度保留、重复invoke不重试、明确恢复只再处理第二家。seed测试验证原日期/来源/输入不变。真实SQL脚本经过实际确认、领取和生产执行器，在零任务预算/缺少知识的隔离账号中验证子运行检查点已播种，子任务付费预留为0；原运行与检查点保留，fixture清理。此实际测试没有生成模型业务结果；成功图的模型与保存仍使用合成依赖。

首次图测试的校正桩漏返第二家公司，修正fixture后通过；首次build发现三个测试参数缺类型，修正后重建。无正式实验、真实补证、模型盲审或SMTP重发。

效率：新节点记录单项输入、返回证据量、有效/下游项、API额度、延迟与供应商尝试；失败证据不算有效，费用保留。原运行费用不并入子运行额度，预算祖先约束独立核验。恢复可复用证据不新增付费采集；部分有效但整体需刷新时暂按该公司重验，后续可进一步复用逐条有效证据。

剩余：页面创建/确认入口、新旧结果关联展示、冲突角色完整业务恢复、长时间暂停后的全部依赖失效验证，以及真实模型闭环仍未完成。公共新版本证据复用和部分有效证据的精细复用仍有优化空间。不能把本阶段合成成功图视为真实业务验收或最终数量保证。
