# MA15 前端精简与半透明紫色视觉改造

确认范围与精确值见 [MA15-01 至 MA15-07](CONFIRMED_PRODUCT_RULES.md)。本文件替代此前未确认的颜色和导航候选；历史方案保留在 Git。

## 视觉与布局

淡紫半透明侧栏和页签用于定位，白色内容用于阅读。Segoe UI / Microsoft YaHei / PingFang SC 承载正文，内容左对齐。避免把任务、工具和知识证据全部拆成同等卡片；只有待批准动作突出边界。颜色、字级、控件统一从 globals.css 的变量读取，conversation-theme.css 管理应用布局。

```text
248px 侧栏        功能页：返回对话              刷新当前页面
新对话           页面名称
市场与线索       业务页签                  国家
客户开发         宽内容（最大 1440px）
知识库           正文阅读（800–880px）
对话历史
帮助 / 账户
```

## 交付阶段

1. 基础视觉与导航：已实现三大入口、帮助/账户菜单、功能 URL、返回对话、内存输入和基础样式。类型检查通过；视觉及端到端验收仍待阶段四。
2. 市场模式退场：待完成数据库兼容、DTO/API、旧工具调用及隔离回归。
3. 页面重排：待完成资料默认页、宽阅读、问答/审核、批准内容和详细页布局。
4. 完整回归：隔离本地数据、三种尺寸与两种缩放、路由/权限/恢复、构建/单实例/资源检查。

不得将方案确认或代码编写记为浏览器验收通过。相关 PRD 及主工作流共同引用本记录。
