# MA15 前端精简与半透明紫色视觉改造

确认范围与精确值见 [MA15-01 至 MA15-07](CONFIRMED_PRODUCT_RULES.md)。本文件替代此前未确认的颜色和导航候选；历史方案保留在 Git。

MA20 后续对话交互规则：消息流是主线。每个当前任务、待确认卡和必要异常按保存的 `runId` 跟在其用户消息后面；完成任务不常驻技术卡，执行事件只在展开详情时查看。确认减法的具体后端范围见 [主 Agent 工作流](MAIN_AGENT_WORKFLOW.md)，实现与验收分别见 [MA20 记录](MA20_CONVERSATION_APPROVAL_ACCEPTANCE_2026-09-22.md)。

MA21 对话历史补充：每条历史记录右侧只有一个更多按钮，展开重命名、查看技术流水、删除对话；当前对话用透明浅紫色选中态。技术流水在独立只读弹窗按任务加载，不占主消息流。删除二次确认后从历史隐藏，运行中任务不可删；详见 [MA21 验收记录](MA21_CONVERSATION_HISTORY_ACCEPTANCE_2026-09-22.md)。

## 视觉与布局

淡紫半透明侧栏和页签用于定位，白色内容用于阅读。Segoe UI / Microsoft YaHei / PingFang SC 承载正文，内容左对齐。避免把任务、工具和知识证据全部拆成同等卡片；只有待批准动作突出边界。颜色、字级、控件统一从 globals.css 的变量读取，conversation-theme.css 管理应用布局。

```text
248px 侧栏        功能页：返回对话              刷新当前页面
新对话           页面名称
市场与线索       业务页签                  国家
客户开发         宽内容（最大 1440px）
知识库           正文阅读（800–880px）
对话历史
帮助 / 账户
```

## 交付阶段

1. 基础视觉与导航：已实现三大入口、帮助/账户菜单、功能 URL、返回对话、内存输入和基础样式。类型检查通过；视觉及端到端验收见阶段四。
2. 市场模式退场：已完成迁移 103（允许空/去默认/保留历史）、DTO 与写服务删除、PATCH 410、工具目录退场及旧调用 unavailable/capability_removed。21 项单测、类型检查和定向 ESLint 通过。verify-market-workspace-tenant-rls 与 verify-workspace-audit-tenant-rls 在 127.0.0.1 隔离账户执行通过并清理，验证新记录为空、历史模式与审计不变、跨账户读写被拒绝。未修改远端数据库。
3. 页面重排：已实现资料默认页与宽阅读、按需上传/独立进度、单列问答/引用折叠、管理员审核页签、开发公司选择及批准业务字段。完整保存文本由受账户与 scope 限制的 GET /api/knowledge/library/[id] 读取，并注明检索片段重建边界。类型与定向 ESLint 通过；verify-private-knowledge-isolation --library-only 验证本人、外部账户、伪造 scope、共享正文四项读取边界通过。原脚本完整运行在既有 hybridSearch 的旧检索 fixture 断言失败（当前库使用 v3 release）；不是资料接口失败，未声称该完整脚本通过。视觉/端到端见阶段四。
4. 完整回归：六组尺寸/等效缩放、URL/权限/草稿/批准/503 恢复/无溢出通过，截图修复手机页头换行和提问按钮挤压；全量 1310 单测及 22 浏览器测试通过，收尾相关 117 单测通过，Lint 零错误，最终构建与 9 项静态资源检查通过。重建前停止旧实例，只保留一个本地服务。详见 [验收记录与边界](MA15_UI_ACCEPTANCE_2026-09-21.md)。

不得将方案确认或代码编写记为浏览器验收通过。相关 PRD 及主工作流共同引用本记录。
