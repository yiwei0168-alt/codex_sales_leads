# Stage 217: checkpointed oversized singleton execution in the qualification Agent

The qualification Agent now connects the P06 oversized-unit identity, Unicode-safe chunk planner, append-only chunk checkpoint, complete ordered assembly, bounded fact phases and final score. Before any chunk request, it plans every oversized original unit and preflights every primary/fallback chunk and phase route plus routine and possible Pro final requests. The dry-run uses maximally escaped 240-character summaries, so an unbounded final request leaves the company technical-pending without buying chunks. Original corrected facts and current-run evidence remain immutable; phase/final prompts label complete chunk summaries as derived interpretations with original IDs and SHA-256 hashes. An uncertain complete screening tied to identity, country or role stays pending instead of becoming a negative or supported eligibility gate.

The synthetic Agent run interrupted before its second chunk, retained the first, then resumed with only missing chunk calls and completed bounded phase/final scoring. A subsequent invocation reused all completed responses without a model call; incomplete chunks produced no completed cache contract. Separate synthetic cases proved oversized final preflight sends zero chunks and uncertain critical chunks do not enter phase/final scoring. The real PostgreSQL chunk-checkpoint verifier independently passed two saved chunks, cross-process loading, duplicate suppression, user/country isolation and mutation denial, then removed its fixture. Full 1,043 tests in 208 files, TypeScript, focused ESLint and production build passed. Real provider calls, paid tokens/credits/USD, customer jobs and mail sends were zero.

This proves the local integration and persistence contract, not real model semantic quality or full P06/A11 acceptance. A real large-company score, actual fallback-model chunk behavior, the 151-source end-to-end case, trusted provider bills and the natural-language paid closed loop remain unverified. The whole-run A11 quote is still unavailable and the cumulative USD30 gate remains in force.

Efficiency: one synthetic long critical candidate produced complete reusable chunk, phase and final outputs, all used in local assertions. A second synthetic budget interruption kept one completed chunk; a third preflight rejected one infeasible final request before any transport; an uncertain critical result retained its chunks without purchasing phase/final work. Mock calls only, zero real model tokens, paid API credits, cash or external retries. Full regression about 21 seconds and isolated SQL verifier about five seconds; real provider latency and user adoption unknown. Opportunity: reuse exact completed chunks across process recovery and avoid phase/final spend when critical evidence remains unresolved.
