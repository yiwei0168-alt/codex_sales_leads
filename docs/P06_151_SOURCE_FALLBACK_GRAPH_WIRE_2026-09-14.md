# Stage 223 — P06 151-source complete synthetic graph under the actual fallback wire

The current qualification Agent's existing phased-score fixture now checks the full 151-source/155-finding company with a synthetic primary and fallback completion path. The fallback path uses a real `OpenAiCompatibleProvider` serializer, model versions `openai/gpt-4o-mini` and `openai/gpt-4o`, and its own cache and paid fingerprints. Every phase and final request is checked against the applicable actual fallback and DeepSeek wire bytes. One phase and the final score use the configured synthetic fallback route; the company reaches `completed`, its final contract is derived from the actual route, and replay sends zero additional calls. This broadens the prior primary-only 151-source and smaller fallback graph fixtures.

The model responses and checkpoints in this particular 151-source graph test are in memory. The separate product PostgreSQL verifiers continue to cover primary 151-source interruption/replay and smaller fallback-route cross-process identity. This test does **not** prove one combined 151-source fallback graph across process boundaries or real provider semantics; those remain A07/A11 gaps. It does not activate a default fallback tariff or change a model route, score or business rule.

The focused graph test passed; full regression passed 1,044/1,044 tests in 208 files, TypeScript and changed-file ESLint passed. No real model, search, paid token, credit, cash, mail, customer job or budget record was used.

Efficiency: one synthetic 151-source candidate produced one valid completed score and one zero-call replay, both used for local route and cache assertions. This is zero real qualified/customer-adopted leads; actual model quality and adoption are unknown. Mocked fallback calls reused cached outputs without paid retry. Full regression took about eighteen seconds; real provider latency remains unknown. Opportunity: retain the exact fallback wire and identity checks in future large-company recovery work, then prove the combined cross-process graph without duplicating purchased phases.
