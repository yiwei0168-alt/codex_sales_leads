# A26：固定验收汇率、产品每周刷新与 USD50 本地验收上限

2026-09-14 用户确认：“汇率在验证阶段只取一次数值，不过期。真实产品运行时，每周取一次汇率。放宽本次验证上限至50美元或者以默认验证阶段汇率等价的人民币金额。加快验证完成。”本阶段选择明确的 **USD50** 上限，只用于隔离的本地验收账号 `cbee9803-3c43-4609-9228-66086b207012`；不设新的人民币上限，不改变其他用户产品预算。A25 的逐请求保守预留继续生效；单次费用未知、请求合同不符、预计累计超过 USD50 或未知付费结果，仍在该步暂停。

验收固定已入库的 ECB `ecb-cny-usd-2026-09-11` 快照：`USD/EUR=1.1592`、`CNY/EUR=7.7762`，即美元换算有理数 `1159200/7776200`；参考日 `2026-09-11T00:00:00Z`，取回时间 `2026-09-13T05:47:16.090Z`。验收进程只按此版本读取，不再按日历年龄失效或刷新，仍校验来源、日期顺序和原有 5% 预留缓冲。固定版本只经服务端费用上下文传递，HTTP 请求不能设置。普通产品继续共享 ECB 官方免费源，一次成功刷新后按周调度，失败按原有小时退避；普通运行的参考最长有效七天，过期后暂停人民币计价调用。该政策没有改变模型/搜索费率或供应商路由。

隔离账号额度已从 USD30 改为 USD50，原有 USD12.324404 占用完整保留，剩余 USD37.675596。一次合成预检误将 SDK 请求捕获置于费用上下文，造成 USD10.622880 的未发出请求预留；合成 `fetch` 在网络发送前抛出，唯一操作 `local-a26-read-only-preview` 和唯一记录经只读核对后，追加 `verified-unbilled` 零费用观测并核销这笔误预留。预检已改为只在纯报价段使用固定汇率上下文，合成传输不再触碰账本。重复预检后占用仍为 USD12.324404，原有六笔历史未知费用未释放、未改写。

本地生产无付费验收：1,051 项/208 文件测试、TypeScript、变更文件 lint、Next.js 生产构建、生产依赖安全审计 0 漏洞通过。真实数据库只读预检证实 Kimi K2.6/K3、北京 Embedding、S01 市场计划、DeepSeek 角色与评分、Terra/Sol 条件复核的当前单次上界均可报价且各自能放入当前剩余额度；Brave 核心搜索和 Exa/Tavily 的已审合同可用。`totalRunBoundUsd` 仍为未知，但依 A25 不再是启动硬门槛。SearchAPI 当前账号套餐合同缺失且配额为零、Gemini 内部搜索用量缺可强制上界、默认备用模型合同缺口及 S01 真实端点参数兼容性仍可能使相应步骤暂停。**尚未运行真实自然语言至合格公司入库的完整闭环；整体验收未完成。**

效率口径：本阶段固定参考读取和两次预检仅产生本地报价/状态输出，真实候选输入、有效候选、下游线索使用及用户实际采用均为零或未知；付费模型 token、搜索额度、供应商 USD、邮件、重试均为零。合成误预留及零费用核销分别留痕，不计为外部费用。预检每次约八秒，真实供应商延迟未知。下一步仅运行隔离账号的最小真实任务，并在每步重核保守单次上界与剩余额度；未核验的条件路由到达时暂停并保留检查点，不把工具缺口认定为市场耗尽。
