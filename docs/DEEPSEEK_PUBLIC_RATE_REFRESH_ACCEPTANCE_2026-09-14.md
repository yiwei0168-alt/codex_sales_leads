# 阶段107：DeepSeek Flash/Pro 官方费率只读复核

现行 `deepseek-flash-v41-text-json` 与 `deepseek-pro-0813-nonthinking-text` 共用 [DeepSeek 官方模型与价格页](https://api-docs.deepseek.com/quick_start/pricing/)。本阶段以 2026-09-14 实际 GET 的双模型表冻结[字段基线](DEEPSEEK_PUBLIC_RATE_BASELINE_2026-09-14.json)：Flash 为 DeepSeek-V4.1-Flash，Pro 为 DeepSeek-V4-Pro-0813，均为1M上下文；峰值 cache-miss 输入/输出分别为 USD0.30/1.20 和 USD1.32/3.96 每百万 token。按现行 1,048,576 输入加 8,192 输出的保守计算，分别仍为 USD0.324404 与 USD1.416561。非峰值及 cache-hit 字段也纳入逐项比较；只取便宜价格不能证明上界。

迁移 `057_deepseek_public_tariff_reference.sql` 将已有追加式公共费率快照、刷新状态与观测扩展到这两条现行静态规则。一个官方 HTTPS GET 同时复核两条，跨进程事务锁合并并发；验证成功后24小时再查，网络失败1小时后重试。身份、版本、上下文、输出能力、任一费率字段变动或页面无法明确解析时，受影响规则 `review-required`，新预留由既有 `reservePaidCall` 暂停且 hold 粘性保留；网络不可用保留旧快照与 hold，并设置独立的不可用状态和静态到期门禁。公开页面原文仅用于当次解析，账本只追加哈希、规范化字段和聚合量；没有账号密钥、客户内容或原邮件。刷新和预算页面读取均**不会**改静态合同金额、模型路由、`verifiedAt`、`expiresAt` 或历史费用，也不会自动确认新价格。

真实 PostgreSQL 验证 `scripts/verify-deepseek-rate-refresh.ts` 对迁移057重复应用通过。初次并发两次刷新合计最多1个公共 GET，当前 Flash/Pro 状态均 `validated`、hold=false；紧接着重复检查0 GET。迁移后的下一次尝试为初次检查后24小时。应用角色可读状态而不能改写快照；预算占用、付费预留数与费用观测数在验证前后一致。两个模型观测把一次公共 HTTP 记为1+0，不重复累计字节或延迟。解析单元测试覆盖单价/版本漂移、缺价格、错误HTML与非HTML源；仓储测试覆盖并发去重、粘性暂停、故障退避和无付费写入。

阶段107后续边界修正：成功响应却不是HTML、206部分页、页面转移、404或超大页面均作为可观察的合同无法核验，两个规则进入 `review-required`；429/5xx或传输故障作为暂时 `unavailable`，保留旧快照及既有hold、1小时后重试。合成响应测试覆盖这五类不能解析页面和两类暂时故障；未为测试发起新公共抓取或付费调用。此修正只改公开响应分类与测试，预算API和页面实现未变，沿用本阶段已通过的62组生产页面回归。

验证：全量935项/200文件、类型检查和 Next.js 生产构建通过；全库 lint 0错误/11既有警告，生产依赖审计0高危。本地独立生产服务使用隔离账号完成桌面1366px与手机390px共62组真实HTTP/Chrome回归，含预算API到DeepSeek状态页面、预算刷新只读、国家/用户隔离、任务与恢复、公司成本及原有UI；测试 fixture 已清理，服务关闭。页面探针仅产生并清理1笔合成预留，真实模型/搜索付费调用0、真实邮件0。

效率口径：公共输入1页，解析有效模型2个，预算状态下游只读使用2条；本次真实业务候选输入/输出及用户采用0。初次官方GET1、紧随重复GET0，模型 token/API credits/真实现金0，付费重试0，公开抓取延迟由聚合观测记录，不冒充模型业务延迟。机会是两个模型共享一次每日抓取并在漂移时先停新预留。剩余：其他静态模型/搜索来源及动态人民币费率刷新、价格变更确认流程、到期时生产 worker 长期触发和 A11 最小真实业务闭环未验收；静态截止仍为 2026-09-20T00:00Z，不能将当前 `validated` 误认为届时可调用。
