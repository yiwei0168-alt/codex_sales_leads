# Stage 211: cap provider over-delivery before candidate processing

The existing `maxResults` request parameter now also limits accepted `items` in the Google Places, Exa and Brave adapters. Gemini and SearchAPI already capped their candidate items. The original provider `rawResponse` remains available for audit; this change only limits the candidates and `sourceUrls` passed to downstream registry processing. It does not infer a unique-company count, increase the pool target, alter search routes or change provider billing.

Three simulated over-delivery responses each returned five rows for a request of three. The adapters accepted nine rows total, rejected six excess rows from candidate processing, preserved ranks 1–3, and issued only three mocked HTTP requests. Focused tests, **1,034 tests in 208 files**, TypeScript, focused ESLint and production build passed. The read-only minimum-run preview now reports at most **240 downstream accepted result slots** (four scheduled steps × five rounds × at most 12 requested items); actual provider response size, unique companies, Gemini server-side Google searches and total-run USD bound remain unknown. This is not a finding that 240 companies exist or will qualify.

The preview confirmed USD12.324404 occupied of the cumulative USD30 acceptance cap, USD17.675596 remaining, expired/missing tariffs on other routes and `totalRunBoundUsd: null`. It started no customer task and made no paid provider, search, model or mail call. A11 remains held; P06 runtime chunk integration is also unfinished.

Efficiency: 15 synthetic input rows, nine valid admitted item rows, six discarded with reason `provider-overdelivery`, nine used by local adapter assertions, zero product downstream use and unknown actual user adoption. Mock HTTP requests three; paid API credits, model tokens, cash, retries and external provider latency zero or not applicable; full regression about 20 seconds. Opportunity: keep per-step candidate work bounded by requested count while retaining separately measured provider response volume and final unique/qualified counts.
