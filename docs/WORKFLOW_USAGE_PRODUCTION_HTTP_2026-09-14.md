# 工作流用量本地生产 HTTP 回归（阶段98）

对 `df46797` 构建的独立本地生产服务 `127.0.0.1:3018`，运行隔离账号的真实 Chrome 1366×900 与 390×844 两视口回归，合计 **58 组检查通过**。脚本使用真实表单登录、同源 cookie、生产 API 与应用数据库角色；浏览器阻止外连以及不在验收白名单的写请求。只创建临时用户、工作区、任务、公司、阶段量和模型用量，结束时清理；未认领用户已有任务。

阶段97新增的 `/api/tasks/usage` 在每视口都读取同一合成阶段记录：输入2、生成2、有效1、下游使用1、利用率数值0.5；模型用量记录15个**合成** token。第二次 HTTP 读取结果完全一致，数据库各只有一条，不因刷新新增记录；`userAdoptionCoverage=unknown`、`totalCostComplete=false` 保持。原操作、HTTP及预算接口仍返回200。其余原有检查继续覆盖任务数量与九种停止原因、费用五口径、国家隔离、手工角色/等级/合作路径/记忆、渠道图手工添加、草稿批准幂等、暂停恢复、两年旧证据提醒及禁用账号鉴权。

第一次新增断言仅因 PostgreSQL 将 `1/2` 表示为 `0.50000000000000000000`，与脚本字符串 `0.5` 不同而失败；数值正确，fixture 已清理。断言改用数值比较后完整回归通过。没有改变 API 或费用计算。生产服务在验收后停止，未触碰原有 3017 端口。

本阶段真实模型/搜索调用0、真实邮件0，只有一条可清理的合成费用预留；用户真实采用、供应商账单及自然语言到最终公司入库的最小业务闭环仍未验收。付费前只读预算核查仍为上限 USD30、占用 USD12.324404、余 USD17.675596、历史未知账单6笔；此报告不授权或执行付费调用。
