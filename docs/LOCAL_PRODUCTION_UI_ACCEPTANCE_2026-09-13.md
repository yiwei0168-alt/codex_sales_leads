# 本地生产页面验收（阶段45）

阶段78最新：任务完成/失败/暂停事务调整后，生产Chrome两视口54组全部回归通过，包含实际暂停执行器、原检查点/额度保留、费用API/页面及人工操作。849测试/build/真实SQL完成事务回放通过；fixture清理，0付费/发信。新增事务证据见[报告](WORKFLOW_COMPLETION_ATOMICITY_2026-09-13.md)，真实业务闭环未执行。

阶段77最新：Chrome两视口54组通过，新增3个人民币模型费率期限与ECB参考日期/取得时间/72h截止的真实API→页面对账及移动端宽度检查。预算GET只读公共快照，读取失败/缺失仍unknown，不触发刷新、延长期限或增加采用。849测试/187files、生产build和局部lint通过；fixture清理，0付费/发信。生产实际有效参考展示已验证；过期、无效来源和数据库失败由确定性测试验证，未修改共享生产快照制造故障。原有国家/人工修改/草稿批准/暂停及成本UI回归均通过，真实业务闭环仍待。

阶段65最新结果：在阶段64的42组基础上，两视口各增加真实PostgreSQL检查点→进度API→页面的待处理数量测试，共44组通过。复用既有progress接口，不新增读写端点；页面公开待校正/待评分候选项，说明按阶段计数不可相加为公司总数。任务页刷新通过组件key重新读取进度；初始/读取失败显示尚无记录，已知0保留0，成功后撤销旧读取错误。

验证脚本只用updateState种入独立`ui-progress`检查点及一个cancelled job，不执行节点。页面先显示1/1，更新保存状态后点刷新变0/0，合成累计额度13保留；临时改为不匹配owner使实际API拒绝，页面清除旧数并显示失败；恢复owner后刷新0/0且错误消失。历史无job任务显示未知。最终仅预建cancelled job、无artifact采用事件，原11微美元费用fixture不变；父脚本删除独立checkpoint和SQL fixture，0真实调用/邮件。832测试/build/生成check、局部lint通过；首次lint指出effect同步setState，改用key重建后通过。阶段64“pending尚未投影”的限制已解除，实际付费业务生成到页面仍待A11。

阶段64当前页面回归：核验3017监听进程确为本仓库Next start后重启到最新生产build，运行`UI_VERIFY_BASE_URL=http://127.0.0.1:3017 node scripts/run-tsx.cjs scripts/verify-authenticated-ui.ts`。Chrome 1366×900和390×844共42组实际页面/API/SQL检查通过，无响应模拟、无浏览器外网或业务写入放行（保留原手工公司/个人修改测试白名单）。初次普通权限端口查询未能观测，启动返回EADDRINUSE，随后提权只读核实已有本任务进程再重启，未触碰其他端口服务。

新增九类任务结果：达标、供应商不可用、安全轮次上限、已有证据的搜索耗尽、处理未完成、角色待判、最终合格缺口、预算阻止、历史数量未知。分别核对目标/发现/评估/合格/保存、缺口、具体停止文案、角色待判和禁止续搜按钮；每种刷新后真实API结果不变，两个视口无横向溢出。预算阻止fixture使用failed状态+真实错误字段，不伪造成功完成。发现缺口原本仅completed显示，现任何有已记录保存数的状态都显示实际缺口；unknown仍不倒推0，不因此开放失败任务续搜。

原有登录与撤销会话、国家切换、角色/账户等级/合作路径及私人记忆、手工渠道图重复、两年以上证据提醒、费用五口径/未知及刷新均继续通过。整轮没有创建workflow job或artifact采用事件，只有原11微美元合成费用fixture，0真实模型/搜索/邮件；fixture清理成功。832 tests/182 files、最终生产build含类型检查、生成文档check通过。页面内容来自合成持久数据，真实业务生成结果到UI闭环仍待；待校正/待评分检查点数量还未投影到此页面，不能将A09整体标完成。

阶段51增量（产品14dfd6f未改）：扩展verify-private-knowledge-isolation.ts，真实SQL设置1个禁用邮箱连接、1封加密合成邮件、2个候选及预存知识。实际reviewMailboxCandidate验证另一用户missing、部分批准不能拒绝、另一事务持锁时busy、再次批准复用预存知识、重复批准时间戳不变、相反决定冲突，以及两次并发拒绝仅1次新决定。本人7条审核遥测合计2有效新输出，下游使用null，无合成正文写入遥测；恢复知识继续可检索。首次fixture缺少content_ciphertext非空字段导致setup整体回滚，补齐实际加密后通过。0嵌入/付费，全部fixture清理，typecheck通过。此为已有知识的恢复链路，尚未验证新邮件模型提取/嵌入、HTTP页面审核及答案生成。

阶段49增量（产品fd9682f未改）：扩展同一私有知识验证脚本，新增5文档/5切片，调用实际hybridSearch和应用角色SQL。两用户GB私有、MX私有、另一用户GB私有、共享GB、归档GB使用同一固定向量；通过四组精确ID集合检查及切片RLS检查。传GB仅返回本人GB和共享GB，传MX仅本人MX，另一用户仅本人GB和共享GB；不传market则本人跨国家知识可见，这是接口可选过滤契约，不代表自动国家隔离。独立读取knowledge_chunk实际向量维度，无模型或嵌入调用。首次fixture UUID/text参数推断冲突，setup事务回滚；显式转换后全部通过、fixture清理。typecheck通过。未调用answerWithRag生成答案，也未验证邮件导入/审核路径，不能将本结果扩展为全链路通过。

阶段48增量（产品fd9682f，脚本扩展）：真实Chrome桌面/手机24组检查通过，新增两年前合成已核实证据的列表提醒、证据弹窗与“超过一年仅提醒，不自动判无效”说明；证据仍保留，未重新搜索/评分。原22组继续通过，临时数据清理，无发信/付费。

另运行`node scripts/run-tsx.cjs scripts/verify-private-knowledge-isolation.ts`，真实应用数据库角色及searchOutreachKnowledge查询，两个隔离用户/工作区、六条合成记忆，使用从实际字段契约读取维度的固定非零向量，无嵌入API。验证用户/国家/角色隔离、archived排除、内部用途营销主张排除、company-classification不进入开发文本检索；应用角色直接查询也看不到另一用户记录。仅输出计数和通过状态，所有fixture清理。首次构造使用disabled记忆状态违反表约束，整个setup事务回滚；按实际archived状态修正后通过。typecheck通过，产品代码本阶段未改；812测试/build沿用阶段47，不冒充本阶段重跑。此处证明私有开发知识的SQL过滤，不证明通用RAG/邮件知识全链路或模型语义质量。

阶段47增量（基线cf2dcaf加本阶段修复）：真实渠道图表单新增公司、实际API/SQL保存、图节点更新及重复提交已在桌面/手机通过，总计22组检查。首次角色选择器严格名称定位失败，修正测试；随后发现实际产品新增公司入库成功但图不显示，原因是新增响应使用本地化国家名，英国映射被UK别名覆盖。修复为手工响应使用规范国家代码、marketCode将UK映射结果统一GB；不修改历史评分或搜索范围。测试断言同域仅1条当前国家记录、待核实/人工指定状态、重复返回原公司并打开详情。已查看手机截图，地图容器按现有方式滚动，页面无横向溢出。

阶段47生产build（含类型检查）、812测试/178文件及生成check通过；3017验收服务按核对后的进程身份重启，新exec会话1855。无模型/搜索/发送，1条成本合成预留不是真实费用；新增两公司及临时账号、记忆、成本数据全部清理。脚本只额外放行当前视口精确合成域名且GB国家的新增POST，其他写入仍禁止。手工添加已验收；手工关系编辑与策略/跟进/知识检索/提醒及真实业务闭环尚未覆盖。

阶段46增量（代码基线8030970加本阶段修改）：账户等级编辑入口补齐，沿用现有后端layer兼容规则。新构建在127.0.0.1:3017，真实Chrome桌面/手机共20组检查通过，新增两组角色/等级/路径编辑及个人记忆验证。桌面将GB公司设为Distributor/Priority Distributor/OEM/ODM，手机设为MSP/KA/Other；真实PATCH、随后GET与SQL记忆内容一致，market_codes=[GB]且绑定该公司；MX同域公司保留Retailer/Standard。每次主角色修改显示评分待更新，不调用评分。已查看手机截图，布局正常。

脚本仅额外放行隔离GB公司精确路径的PATCH，其他业务写入和外连仍阻止。新增模型/搜索/发信0，单条合成成本fixture及记忆均清理。先前3000重启因旧监听冲突失败，随后该端口登录超时；未将其记作通过，也未终止身份不确定的服务。改用独立3017端口加载新构建后通过。阶段46生产build（含类型检查）、812单元/集成测试、生成文档check通过；下面阶段45保留原始范围。本阶段仍未验收渠道图手工新增表单、策略邮件复用/跟进、私有知识检索及一年以上提醒、真实业务闭环。

2026-09-13，生产代码 `be7c9a3`，Next.js 16.3.4生产构建，服务仅监听127.0.0.1:3000。未启动工作流worker。使用已有verify-authenticated-ui.ts扩展脚本、真实Chrome、真实页面/API/应用数据库角色，临时独立账号及合成业务记录。**本报告只覆盖下表页面验收，整体验收未完成。**

命令：`UI_VERIFY_BASE_URL=http://127.0.0.1:3000 node scripts/run-tsx.cjs scripts/verify-authenticated-ui.ts`（PowerShell实际通过环境变量设置）。视口1366×900和390×844各完成9组检查，共18组。浏览器禁止外连，非GET/HEAD仅放行登录/退出；未启动搜索、模型、发信或用户原排队任务。fixture写入与清理由脚本执行，凭据仅在内存。

| 范围 | 实际证据 | 结果及限制 |
|---|---|---|
| 鉴权 | 真实表单登录、浏览器cookie、导航；持有cookie时禁用账号，下一预算请求401 | 两尺寸通过；测试后恢复临时账号以完成清理 |
| 国家隔离 | 同一合成域名分别建立GB/MX国家记录，页面只显示所选国家公司详情；渠道图/国家选择一致 | 两尺寸通过；手动新增动作来自应用服务函数，未验证新增表单操作 |
| 任务与知识导航 | 任务进程、知识库/RAG、邮箱学习导航；任务、市场、用量、预算API真实200 | 两尺寸无页面运行异常；不代表私有知识检索全链路验收 |
| 预算 | 已保存0美元提案、选择任务、取消确认；真实预算API返回任务上限0；用量缺失显示未知 | 两尺寸通过，未修改预算或启动任务 |
| 公司成本 | 第二独立合成任务，11微美元共享预留，2个实际处理公司键；真实结果域名/GB关联 | 两尺寸显示预留/占用6与5微美元，报告0、估算/账单未知；覆盖率1/1或0/1，五口径不相加 |
| 成本刷新 | 点击刷新任务预算，仍2个公司标题；重复API读取companyCosts完全一致 | 真实浏览器+API通过；仅1条合成预留，无真实供应商费用 |
| 布局 | 两尺寸无文档横向溢出；公司抽屉可打开/Escape关闭，国家渠道图可见 | 合成内容通过；已查看手机公司费用截图，长域名正常换行 |
| 清理 | 精确临时账号/邮箱校验；只允许synthetic-ui-cost费用记录清理，其他费用出现则保留并报错 | 最终运行所有合成账号/工作区/公司/任务/预留/观测清理成功，无客户记录变更 |

首次扩展fixture运行失败：lead_search_result缺少必填query_id；已补合成查询关联后重跑通过。该失败是验收脚本数据构造错误，未改产品规则。原失败运行的临时数据也已清理。

本阶段真实API/token/费用0、发送邮件0；1条合成费用预留不是真实支出。只读核对真实累计预算：USD30上限、占用USD12.324404、余USD17.675596、6笔未知、0在途，无重置。付费前仍须重新核验。脚本typecheck通过；产品全量回归沿用阶段44的812测试/178文件及生产build，未把本阶段18组浏览器检查混入该计数。

截图仅保存在本地tmp/auth-ui-*，不提交GitHub。最终报告只保存合成场景及聚合结果，不存账号密码、客户内容或原始邮件。

仍需：真实自然语言到计划/搜索/补证/角色评分/国家入库闭环；产品可控故障/零结果实际结果保存；用户修改角色/等级/路径及个人记忆、渠道图手工表单、策略邮件复用/跟进上下文、知识检索隔离及一年以上提醒；完整费用、恢复和遥测剩余项见[当前矩阵](CURRENT_ACCEPTANCE_MATRIX_2026-09-13.md)。SMTP沿用已通过证据，不重复发送。
2026-09-13阶段68：生产Chrome两视口累计46组通过，新增实际草稿保存/批准→API→页面复用检查。人工正文、approved、revision2保留，多次重开未生成新版本；当前上下文为current，新增本人风格记忆后changed且页面提示，清除旧版本记录后legacy-unknown且页面提示；MX接口不返回GB草稿。仅合成数据、浏览器拦截生成/发送写请求，0付费/发信。首次脚本未打开开发标签而等待按钮超时，改用现有页脚入口后全量通过；两次fixture均已清理。typecheck/局部lint通过，产品代码未变，沿用阶段67生产build/835测试。本项不覆盖真实跟进生成、UI批准采用链及新模型业务。
阶段70最新：生产Chrome两视口48组通过，新增草稿页面批准到SQL审计、并发重复不增版本/事件、编辑再批准、仅保存撤回批准。详见[DRAFT_APPROVAL_OBSERVATION](DRAFT_APPROVAL_OBSERVATION_2026-09-13.md)。836测试/build/局部lint通过，fixture清理，0真实费用/发信。
阶段71最新：Chrome两视口累计50组通过，新增预算页静态上界有效期限与真实API逐条一致、刷新及移动端无横向溢出。期限内仍需核对动态费率/FX/契约/预算/暂停；8条配置不等于全部入口覆盖。过期状态边界由共用门禁函数确定性测试证明，本轮未修改生产时钟或费率制造过期。838测试/build/typecheck/局部lint/生成check通过，fixture清理，0真实付费/发信；自动刷新与变更确认仍缺。
阶段73最新：生产Chrome两视口52组通过，新增实际暂停按钮→POST→执行器阶段边界→数据库cancelled/paused_at→恢复按钮。13合成credits和1/1待处理保留；下一校正Agent未执行，最终无新费用预留，原UI合成预留仍1笔。预建任务为本验收独有inline运行态并持有未来租约，不认领原队列、不启动worker；额外零任务预算。fixture与checkpoint清理，typecheck/局部lint/生成check通过。产品代码未变，沿用阶段71 build/838测试；不覆盖付费在途调用或恢复后真实模型链路。
