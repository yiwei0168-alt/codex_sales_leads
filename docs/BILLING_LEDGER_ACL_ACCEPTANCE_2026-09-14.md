# 费用台账与完成检查点的应用权限验收（阶段174）

阶段173发现汇率快照旧授权残留后，对15张费用、公开费率和完成响应表用应用账号执行只读有效权限普查。发现9张有多余的 `UPDATE` 或 `DELETE`：`billing_tariff_evidence_snapshot`、`billing_tariff_refresh_observation`、`billing_tariff_refresh_state`、`paid_call_reservation`、`paid_cost_observation`、`paid_rule_hold`、`spend_budget_change`、`task_spend_limit`、`user_spend_budget`。原因是历史迁移014在全量重放时对当时所有 `public` 表授予增删改查，后续狭窄的 `GRANT` 不会撤销旧权限。此发现来自真实应用角色的 `has_table_privilege`，不是从 RLS 策略名称推测。15张是本轮明确的费用与检查点清单，不代表全库权限审计。

[迁移071](../db/migrations/071_billing_ledger_acl.sql) 对上述9张表撤销 `PUBLIC` 和应用角色旧权限，再给公开价证、费用观测、费率暂停及预算变更记录仅 `SELECT, INSERT`，给费率刷新状态、付费预留、任务和用户预算状态 `SELECT, INSERT, UPDATE`。阶段173的汇率ACL与3张完成检查点保持原权限；迁移不改行、金额、费率或预算。回滚需按部署前 ACL 备份恢复授权，无数据回写。

真实本地数据库应用连接在迁移前发现9/15异常，迁移071后15/15满足预期：追加式表不可修改/删除，状态表可更新但不可删除，全部无 `TRUNCATE`、`REFERENCES` 或 `TRIGGER` 权限。重复执行071、再次只读审计及迁移056的官方价证校验后仍为15/15。费用核销 SQL 合成夹具通过：唯一完整报告幂等、未知费用保留、发票优先、跨用户隔离和公司分摊守恒；任务限额/RLS事务夹具通过且回滚。全量1015项/207文件、生产构建含类型检查、脚本lint及生产依赖审计0漏洞通过。只读预算仍是上限 USD30、占用 USD12.324404、剩余 USD17.675596，未知账单6笔。权限修复不证明历史费用记录从未被修改；真实服务商账单未取得，A11整次费用上界仍未知，整体验收未通过。

本阶段权限检查输入15表，初检有效6、拒绝9，修复后有效15、用于技术验收判定15；真实业务下游使用0，用户实际采用未知。迁移/SQL夹具的真实模型、付费搜索、SMTP、token、付费 API credits、现金和付费重试均为0；公开价证复验走缓存，新增官方HTTP为0。只读权限审计约1–2秒，付费供应商延迟未知。被拒的9项原因均为旧宽授权残留；优化机会是让全量迁移在业务流量隔离期运行，并在每次重放末尾执行ACL检查，防止历史 `GRANT` 再次扩大权限。
