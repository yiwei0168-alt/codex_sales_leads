# P06 分阶段评分图与产品 SQL 联通验收（2026-09-14）

阶段156在既有 `verify-shared-phrase-score-checkpoint.ts` 增加 `P06_SYNTHETIC_PHASED_VARIANT=1`。隔离公司预置 151 条校正事实及 151 个本轮证据源，完整单公司请求超出现行 61,440 字节限制。第一进程将图停在评分前 PostgreSQL 检查点；第二进程从该检查点调用**现行评分 Agent**，以本地假模型完成 3 个有界事实阶段和 1 个最终评分请求，随后使用现行 `persistLeadWorkflowResult` 入库。没有真实供应商传输。

结果：评分 1 家、结构有效 1、最终合格 0、国家交付公司 0。产品评估行保留全部 151 条原事实及 151 个原证据，证据快照 151 行；评分阶段指标 1 行、模型用量 4 行，阶段完成检查点 3 行。最终评分的精确合同写入评估缓存，并用已完成阶段派生的合同重新读到；另一用户读不到评估或缓存。图检查点的用户/动作不匹配被拒绝。任务和公司夹具清理后不留产品付费预留；真实模型、搜索、SMTP、token、API credits 和现金消耗均为 0。合成阶段输出的摘要为短固定文本，只验证接线、结构覆盖和字节门禁，不证明摘要语义、模型评分质量或真实合格率。

执行命令：`$env:P06_SYNTHETIC_PHASED_VARIANT='1'; node scripts/run-tsx.cjs scripts/verify-shared-phrase-score-checkpoint.ts sql`。该命令输出 `fakeModelCalls:4`、`modelUsage:4`、`paidProviderCalls:0` 并清理夹具。原无损共享短语模式再次执行，仍为 101 证据/105 事实、一次假评分和一条用量；`npm run typecheck` 通过。两模式均不改冻结测评数据。

范围边界：真实提供方响应的事实摘要语义、Flash→Pro 升级、备用提供方路由恢复、任意长度单一事实/摘要、最终付费响应遗失后的安全续跑及整个自然语言到合格线索闭环仍待验收。最终请求超界时维持技术未完成，不能给公司判低分。整次付费上界仍未知；隔离验收账号累计占用 USD12.324404/30，真实任务不得因此项合成验收启动。
