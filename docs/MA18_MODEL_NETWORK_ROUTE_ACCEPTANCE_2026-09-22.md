# MA18 model network routes — 2026-09-22

The user confirmed that GLM model calls remain direct, while Gemini, Claude and OpenAI model calls use a proxy. The local proxy is the running TrojanX HTTP listener at `127.0.0.1:7892`; its address is held only in ignored `.env.local`. The OpenRouter gateway URL stays `https://openrouter.ai/api/v1` for all OpenRouter models, with transport selected from the requested model rather than changing that URL.

Implementation: product paid requests to `openrouter.ai` with `openai/*` or `anthropic/*` model IDs use `MODEL_PROXY_URL`; `z-ai/glm-*` remains on default direct fetch. Direct OpenAI/Anthropic API hosts and Gemini's Google API host use the corresponding local model/Gemini proxy. Gemini search retains its dedicated proxy transport, while Gemini discovery uses the shared paid-request transport. Proxy-required routes fail before sending when the proxy is missing or invalid; injected test transports remain authoritative. Saved OpenRouter Batch submissions and receipt reads use the saved model identity to select the same route. Other providers and read-only metadata requests are unchanged.

Verification: 1,324 tests passed with one existing skip before the final Gemini fail-closed check; nine focused route/search tests passed after that small change. TypeScript and targeted lint passed after the final change. The authenticated OpenRouter `GET /api/v1/key` returned HTTP 200 through the proxy. The authenticated Sol endpoint read `GET /api/v1/models/openai/gpt-5.6-sol/endpoints` returned HTTP 200, the expected model ID and seven listed endpoints through the proxy in about 1.1 seconds; the product's actual model-transport selector repeated the read through its selected proxy and returned HTTP 200 in about 0.9 seconds. A direct authenticated GLM 5.3 endpoint read returned HTTP 200 and the expected model ID in about 0.9 seconds. These are read-only metadata checks, not paid inference or proof of Sol answer quality. No earlier outcome-unknown request was replayed.

The database had no running or queued Agent tasks before the local restart. After restarting the single local Next, LangGraph and worker processes, Next returned HTTP 200 and the LangGraph health/schema probe passed without external provider calls. Product model-routing integration is verified by mocked transport and live metadata connectivity; a paid Sol generation remains untested.
