# Stage 222 — P06 wide critical fact preflight boundary

The current qualification Agent was given one synthetic company with 151 distinct current sources, 155 corrected findings and an additional approximately 128 KB incompressible primary-role statement. Its complete chunk/phase/final route preflight could not prove that all subsequent requests would fit the approved byte contracts. The Agent returned `retry-required` with an explicit no-chunk-sent warning. The provider transport count was zero and the original company/evidence object remained byte-for-byte unchanged. This is a technical pending state, not a negative role or eligibility judgment.

The new test is isolated from the shared baseline fixture. Its initial shallow-copy mutation contaminated other tests; cloning the one large input removed that test-only contamination. The complete qualification-Agent file then passed 41/41 tests. Full regression passed 1,044/1,044 tests in 208 files; TypeScript and changed-file ESLint passed. No product route, tariff, score, threshold, database record or confirmed rule changed.

This validates a safe upper boundary for the tested synthetic case. It does not prove the full 151-source graph can reach a score, real model summary quality, provider fallback semantics or A11's paid natural-language-to-result loop. The no-paid full-graph recovery gap remains open. The live USD30 ledger was not touched; no model/search transport, paid token, credit, mail or customer task was sent.

Efficiency: one synthetic large-company input yielded one valid pending diagnostic used in the P06 acceptance check; valid scored output and downstream customer use were zero, actual adoption unknown. One technically infeasible completion route was rejected before any paid request, with zero retries and zero discarded business output. Focused Agent regression took about eight seconds and the full regression about nineteen seconds; real provider latency remains unknown. Opportunity: keep the all-route completion preflight before buying a segment, and investigate a bounded final synthesis for large source counts without omitting critical facts or citations.
