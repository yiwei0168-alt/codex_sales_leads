# 阶段140：正结果验收夹具的全局公司清理

阶段138–139的正结果脚本清理了工作区、用户、任务、预算和检查点，但原实现遗漏全局 `sales_company`。只读审计按本脚本独有的 `positive-graph-<UUID>.fixture.invalid` 域名核得6条合成公司身份；工作区引用、评估引用及所有指向 `sales_company` 的外键引用计数均为0。经上述身份与引用核对后，仅删除这6条本轮合成残留，再查为0。没有清理其他市场或用户数据。

验收脚本现随同隔离夹具事务删除相应 `sales_company` 行，并在提交后断言该域名不再存在。更新后的评分暂停恢复模式再次通过实际图→SQL，清理后全局合成残留仍为0。原验收账号的USD30上限、USD12.324404占用和6笔历史未知账单未改变；真实模型/搜索/SMTP外发、token/API credits、现金与付费重试均0。

这修正的是验收卫生与数据隔离证据，不改变候选筛选、评分、产品费用规则或阶段138–139的正结果与暂停恢复结论。合成输入1家公司、有效评分/保存各1、完成后夹具残留0，用户实际采用未知；本地运行约4秒，供应商延迟及金额节省未知。优化机会是任何未来正结果SQL夹具都显式核对全局公司身份及外键引用，避免把工作区级清理误认为完整清理。
