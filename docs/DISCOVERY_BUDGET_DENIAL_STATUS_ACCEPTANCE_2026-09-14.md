# 阶段115：发现阶段费用拒绝状态回归

完整回归：964项测试/202文件、typecheck、全库lint 0错误/11既有警告通过。最近一次生产构建为阶段111；本阶段只改测试与文档。

对当前 `executeHybridDiscovery` 做无付费定向回归，分别让合成 Brave 核心提供方在外发前抛出 `missing-tariff` 和 `budget-exhausted`。两种情况均向任务上层保留原始 `BudgetDeniedError.code`；没有写入完成调用、失败调用、提供方故障计数、无新增结果计数或调用遥测，也没有执行候选门禁。因此费用阻止不被解释为供应商不可用、搜索无价值或已确认市场耗尽。

这只覆盖发现执行器的合成费用拒绝边界，不证明真实任务页面状态、在途请求核销或自然语言到结果入库的完整闭环。真实供应商调用、搜索额度、模型 token、邮件及新增现金费用均为零；原 USD30 验收上限和历史未知账单保持不变。

验证：`hybrid-discovery-executor.test.ts` 19 项通过（含新增两种拒绝状态）；typecheck 通过。完整回归与静态检查结果见当前验收矩阵。优化机会是沿用这个显式费用错误到任务状态的传播路径，恢复时复用已有完成调用；后续用隔离本地生产任务核对 UI 展示与实际账本。
