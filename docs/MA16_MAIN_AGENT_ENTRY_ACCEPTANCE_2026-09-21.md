# MA16 主 Agent 统一入口验收（2026-09-21）

用户确认所有产品对话入口使用主 Agent，并明确知识库“知识问答”也如此。此前本地 `MAIN_AGENT_ROLLOUT` 未配置，消息 API 落入旧同步 `assistant_workflow`，因此调用了 Kimi 意图识别。该次 `PaidCallOutcomeUnknownError` 的本地 LangGraph 日志定位在 `invokeKimiIntent` 的 HTTP 传输阶段、收到响应之前；同环境的无凭据 Kimi GET 因 `connect EACCES` 失败，正常网络权限下返回预期的 401。旧未知费用记录保持原状，未自动重试、核销或清零。

现行消息 API 不再读取 rollout 开关或调用旧意图服务；新对话、历史对话续接和知识页提问均创建持久化主 Agent 任务。知识页选中的行业／公司／产品范围写入任务输入，主 Agent 仅发现知识搜索与状态工具，搜索工具强制使用保存的范围。旧 `/api/rag/query` 作为兼容入口也返回 202 任务回执，不再运行专用知识图。知识页提交后打开有任务进度、答案与来源的账户对话。

验证：定向路由与范围测试 20 项通过；全量 265 个测试文件、1318 项通过、1 项既有跳过；TypeScript、全库 Lint（0 错误、11 条既有警告）、生产构建、9 个静态资源检查通过。MA15 隔离浏览器回归在 1366×768、1440×900、390×844 的 100%／125% 六组布局验证知识问答交接与导航、草稿、批准、溢出。MA16 隔离本地 API／数据库检查创建 3 个合成入队任务，确认会员账户、历史对话续接、知识范围和兼容入口均进入 `main-agent`，付费记录 0；夹具已清理。产品 `127.0.0.1:3000`、LangGraph `127.0.0.1:2024` 均返回 200；主 Agent worker 单实例运行，零付费健康图探针通过。

本次没有发起真实 GLM Batch 回答、搜索或外发，因此不把入队和 worker 启动视为最终答案质量验收。此前未知费用须按原始供应商时间窗口另行核对；仅凭本地 `EACCES` 复现不能改写原账本。知识页目前在对应对话展示答案与来源，不在知识页重复渲染一份即时结果。
