# Stage 213: confirmed Terra secondary and Sol judge contracts

The user approved both [Terra secondary](OPENROUTER_TERRA_REVIEW_PROPOSAL_2026-09-14.md) and [Sol disagreement judge](OPENROUTER_SOL_JUDGE_PROPOSAL_2026-09-14.md) for strict-contract implementation and no-paid acceptance only. `request-bounds-v1.9.0` activates the two narrow OpenRouter credits rules: Terra, medium reasoning, strict JSON, at most 8,192 total output tokens and USD11.019202 per-call conservative reserve; Sol judge, high reasoning, strict JSON, at most 12,000 total output tokens and USD27.736500. Both use the existing standard-provider routing parameters, at most 61,440 body bytes and expiry `2026-09-20T00:00:00Z`. No BYOK, tools, special service tier or explicit cache feature is part of either contract. The older v1.8.0 policy remains versioned history.

The exact task-attributed paid transport selects Terra only for `lead-review-secondary` and the larger Sol rule only for `lead-review-judge`. Unattributed or unrelated Terra requests still have no default tariff; ordinary Sol keeps its USD27.345252/4,096 standard contract, and S01 market playbook retains its separate OpenAI-only USD10.622880 contract. The validator checks model, task-specific strict JSON schema name, reasoning effort, output and byte limits, text message roles, provider parameters, method/path, and absence of tools, plugins, streaming and extra paid features. An omitted `stream` flag is accepted as the current nonstreaming compatible-client wire; `stream:true` is rejected.

At 10:29 UTC the two no-key OpenRouter official endpoint audits returned unchanged relevant price/capability projections and the approved conservative maxima. The official OpenAI [Terra](https://developers.openai.com/api/docs/models/gpt-5.6-terra) and [Sol](https://developers.openai.com/api/docs/models/gpt-5.6-sol) model pages confirm model output/reasoning support, but their direct prices are **not** used as OpenRouter gateway prices. Actual standard-endpoint completion compatibility and a uniquely matched provider bill remain unverified.

An isolated PostgreSQL check used separate synthetic USD30 fixture budgets, one per rule. Each exact task saved one reservation at the approved maximum and retained unknown reported cost; unrelated tasks, wrong reasoning and duplicate requests were blocked before transport. Two in-memory mock responses were returned, no real provider call, and fixture records were removed. Strict contract tests, full **1,038 tests in 208 files**, TypeScript, focused ESLint and production build passed. The read-only A11 preview reports Terra USD11.019202 as individually within the last USD17.675596 balance, and Sol judge USD27.736500 as individually over it. The live account stayed USD12.324404/30 occupied; six historical bills and `totalRunBoundUsd:null` remain. No customer task, model token, API credit, actual USD cost or mail was used.

Efficiency: two synthetic review request inputs, two valid mock response/reservation observations, both used for isolated SQL contract verification; wrong-task, wrong-effort and duplicate attempts created zero further provider work. Product downstream use, real semantic quality and user adoption remain unknown. Mock transports two, paid tokens/API credits/cash/external retries zero; isolated SQL check about two seconds, real endpoint latency unknown. Opportunity: keep review subcalls and their exact fees recoverable without widening a lower-cost route or replaying unknown paid work.
