# Stage 121: zero-result workflow and search-artifact isolation

The local SQL check found that a second application user could read a `lead_search_run` row belonging to an isolated fixture user. The run, query, result, provider-call and occurrence tables had no row-level policies. Migration 060 now restricts those five tables to the owning workspace through the current application user. Search discovery and the legacy global-search writer now set that user in their database transactions. Discovery refuses to write without an owned workflow context. No historical search results or scores were rewritten.

`npm.cmd run db:migrate` initially failed while replaying migration 057: its old tariff constraint excluded the Exa/Places evidence rows admitted in 059. Migrations 057 and 058 now preserve later source pairs during replay. The same full migration command subsequently applied 001–060 and the LangGraph checkpoint setup successfully on the local acceptance database. On rollback, remove only the five policies in 060 and disable their RLS within a controlled transaction if this deployment must be reverted; the original unscoped callers must also be restored together. Historical data need no reverse migration. A rollback would reopen the verified cross-user read, so do not treat it as an isolation-safe steady state.

The isolated verifier `node scripts/run-tsx.cjs scripts/verify-zero-result-graph-sql.ts` used two disabled fixture accounts. Real queue/claim, graph persistence, task completion and one receipt produced requested 1, accepted 0, reason `provider-unavailable`. A repeated completion did not create another receipt. The synthetic 5 micro-USD task reservation remained occupied and unallocated with no settled charge; company count remained zero. One failed-call telemetry record was persisted through the actual discovery writer under an owned context. The owner could insert and read search evidence; the second user saw zero rows across all five tables, updated zero rows in four dependent tables, and was refused a foreign query insert. Fixtures and checkpoints were removed. The verification output reported 546 ms to its final assertions; this is not provider latency.

`npm.cmd test -- --run` passed 969 tests in 203 files; typecheck, targeted ESLint and production build passed. Production dependency audit reported zero vulnerabilities. This is a local, synthetic SQL acceptance of A10/A12/A13, not a real natural-language-to-qualified-company A11 run. It made zero provider calls, zero model calls, zero search credits and zero SMTP sends. No production server or historical Colombia result was changed. The most recent separate browser evidence remains stage 120's 66 desktop/mobile checks.

Input volume: one synthetic target, one synthetic failed discovery observation and seven search-artifact rows across five tables (one run, two queries, one result, two provider calls and one occurrence). Valid qualified output and downstream-used companies: zero; one valid completion receipt was saved and read. User adoption is unknown. Tokens, API credits, actual cash cost and retries: zero. Five micro-USD is a test reservation, not an actual expense. One isolated run took 546 ms; per-stage latency was not measured. The discarded-output reason is provider-unavailable, with no market-exhaustion inference. The efficiency opportunity is to retain tenant-scoped telemetry and the once-only receipt while avoiding replay of a failed or unknown paid request.
