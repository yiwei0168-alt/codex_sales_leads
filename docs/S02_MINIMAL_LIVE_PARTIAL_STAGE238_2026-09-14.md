# 阶段 238：S02 最小真实业务链的部分通过与复核暂停

本阶段使用隔离、停用且无密码的本地验收账号，固定库内 `ecb-cny-usd-2026-09-11` 汇率，按 A25/A26 的逐次原子预留及 USD50 累计上限执行。任务是哥伦比亚 Distributor、目标一家、西班牙语；没有重跑冻结哥伦比亚测评，也没有处理用户已有待执行任务。新会话先生成英文查询计划，经第二轮明确纠正为 `queryLanguage=es`，取消前一提案后仅确认新动作 `b3d7fa08-3b37-40b1-a505-158da92a89a0`。旧 S01 HTTP404 动作及占用未重放或释放。两个新意图调用各预留 USD0.283612，费用报告不完整；真实执行前占用 USD24.088144/50。

真实图任务于 2026-09-14 14:42:00–14:44:29 UTC 执行。S02 OpenRouter credits/OpenAI-only Sol 市场计划通过实际端点，返回有效输出；请求预留 USD10.622880，响应报告 USD0.017461、输入 4,221 / 输出 691 token、约 20.5 秒。本笔账本目前占用报告金额，但供应商账单的完整性与唯一对应仍未核验。随后实际发生知识检索、Brave 搜索、DeepSeek 发现门禁、Tavily 补证/角色校正、两次 DeepSeek 校正及一次 DeepSeek 主评分；账本分别记录有效 HTTP/模型输出。首次评分后进入 Terra 二次复核，单次预留 USD11.019202，约 1.6 秒后记为费用未知且有效输出 0；同一请求指纹再次尝试时，防重放门禁抛出 `paid-request-already-recorded`，任务以失败状态安全停止。现有数据不足以判定 Terra 首次失败的具体 HTTP 原因或实际现金费用，不能称为供应商端点兼容性已通过。

只读 SQL 对账：本动作共有 18 笔新预留，其中 S02 一笔已有不完整但唯一关联的报告，其余 17 笔账单未知；动作期间保守占用净增 USD15.782162，账户目前 USD39.870306/50、剩余 USD10.129694，共 29 笔账单未知、未结请求 0。DeepSeek、Brave、Tavily、Embedding 的成功响应不代表零费用；其原上界继续占用。图中有 9 个持久检查点，复核前工作没有因失败被删；`lead_search_run`、`lead_candidate_assessment`、工作流最终阶段遥测和国家结果均尚未写入，最终合格数量、缺口和真实用户采用仍未知，不能从一次评分推断合格公司。当前剩余额度小于 Terra 单次 USD11.019202 上界，且该复核请求费用未知，因此不在同一动作上重试，也不以跳过复核充作合格结果。

本次验收证明 S02 修订后的真实市场计划、自然语言确认至搜索/补证/校正/评分的主要接线；**未证明**二次复核、结果入库和真实页面闭环。A11 和整体验收保持未通过。下一步优先核查 Terra 首次响应/服务商账单与可复用图检查点；若需要新的请求合同或路由，按 A11 另列可审边界。对 A27 的妥协仅为将本次结果记录为部分通过，并继续用已有无付费 SQL/生产 UI 证据验证后半段接线；不降低角色、评分或复核标准，不把技术失败记作搜索耗尽，也不释放未知费用。剩余真实资金或单步上界不够时保留停止原因。

效率记录：两次意图输入产生一项被确认的西班牙语计划，另一项因语言不符取消；实际图的 18 笔付费尝试中 17 笔账本记录有效输出，Terra 复核 1 笔无有效输出，最终国家线索输出为 0、用户实际采用未知。图内阶段的准确候选输入/有效/下游使用数量尚未最终持久化，不能用调用数代替公司数，也不能逐笔断言下游采用。已知 S02 token 为 4,221/691，其他模型及各搜索工具的 token/API credits 仅按各请求记录的已知字段展示，缺失项保持未知；业务级重试在首个 Terra 失败后被门禁阻止，实际请求内部重试数未被独立证实。任务约 148 秒，S02 约 20.5 秒、Terra 约 1.6 秒；优化机会是持久化失败节点前的阶段汇总，并在未知费用的复核失败时明确保留待核实状态，避免读者误认为已合格或可免费重试。

核验命令：`node scripts/run-tsx.cjs scripts/run-s02-minimal-live-workflow.ts`、`node scripts/run-tsx.cjs scripts/audit-s02-minimal-live-workflow.ts`、`node scripts/run-tsx.cjs scripts/verify-acceptance-budget-status.ts`，均只读；本报告不包含公司、邮件内容、密钥或原始请求载荷。阶段 237 的 1,052 项回归和生产构建未因本阶段只读脚本与文档而改变。
