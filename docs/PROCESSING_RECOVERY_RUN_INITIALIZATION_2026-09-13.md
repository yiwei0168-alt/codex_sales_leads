# 恢复运行初始化（阶段85）

`prepareProcessingRecoveryExecution` 已实现恢复执行的事务初始化：读取不可变恢复关联，重新验证来源证明、原范围与缺项计划；要求当前用户已确认子任务及对应线程已领取的运行中作业。原/祖先及子任务未知或在途费用阻止初始化。仅准备新运行，不调用发现、补证或模型。

在用户预算和动作锁下，新运行按子任务/线程关联幂等创建；重试验证工作区、国家和来源证明后复用同一运行。原运行、评分和账单保持原样。新运行记录原任务来源和禁止发现标识，采用现行评分策略快照；不沿用原运行ID覆盖历史。没有新增迁移。

实际PostgreSQL验收沿用合成已保存来源，经过真实 `confirmAndQueueLeadWorkflow`、仅领取该合成子任务，验证未确认拒绝、错线程/计划拒绝、两次并发只产生一个新运行、原运行仍完成、来源变化拒绝。合成fixture/检查点清理，0供应商/真实费用/发信；1微美元为既有合成门禁预留。

效率：新增/复用初始化审计分别记录输入1、生成/有效/保存1或0、下游使用0、用户采用null、token/API/费用0、实际延迟、重试0及重复运行避免原因。不把初始化当业务结果或用户采用。

边界：尚未让生产执行器消费初始化结果，普通搜索保护仍在；新运行检查点播种、限定公司补证、有效缓存复用、角色冲突、评分及最终关联展示仍待接。此处真实确认仅发生在隔离合成账号，不涉及用户任务或实际业务。
