# 汇率过期与追加式权限验收（阶段173）

2026-09-14 05:47 UTC，计划中的无凭据 ECB 刷新运行一次，服务返回的仍是 2026-09-11 参考日。按已确认 A10 的最长 72 小时规则，快照在 2026-09-14 00:00 UTC 失效；刷新记录为 `unavailable`，下次尝试时间为 06:47 UTC。旧快照保留，但付费预留不得使用。官方来源：[ECB 汇率公布页](https://www.ecb.europa.eu/stats/policy_and_exchange_rates/euro_reference_exchange_rates/html/index.en.html)。本次不更改 72 小时规则、历史参考值或任何账单。

检查迁移 053 时发现应用角色在旧表授权残留之下，对本应追加保存的公开汇率快照仍有 `UPDATE`/`DELETE` 权限；刷新观测也缺少显式旧权限撤销。迁移 [070](../db/migrations/070_billing_fx_append_only_acl.sql) 先撤销这三张表对 `PUBLIC` 和应用角色的旧权限，再明确授予快照/观测 `SELECT, INSERT` 及刷新状态 `SELECT, INSERT, UPDATE`。不变更数据或费用合同；必要回滚须从部署前 ACL 备份恢复原授权。

本地同库管理员事务重放 053、070 后，应用连接实测三表有效权限：快照和观测可读可追加、不可更新/删除/截断；刷新状态可更新、不可删除/截断。快照无内容变化的 `UPDATE` 实际返回权限拒绝。两个并发刷新至多一次 HTTP 请求，后续重复刷新使用缓存；到期分支通过官方响应复核，返回 `rateUsable=false` 与 `official-reference-expired`。这验证的是权限和门禁，不是新汇率可用或真实付费可运行。

本阶段正式验收重复运行 2 轮；每轮输入 3 张公共参考表、2 次并发刷新尝试和 1 次缓存重复，有效权限检查 3/3、下游用于继续阻止过期汇率预留 1，用户真实采用未知。两轮模型/付费搜索/SMTP 调用、token、付费 API credits、现金、付费重试均为 0；每轮官方无凭据 HTTP 复核 1 次，已有计划刷新此前 1 次。供应商付费延迟未知；每轮本地校验约 2 秒。丢弃原因为官方参考日超期，优化机会是复用每日快照及失败一小时退避，避免按用户重复请求；未核实的新参考日不得推断或人工补写。A11 整次上界、真实账单及真实业务闭环仍缺，整体验收未通过。
