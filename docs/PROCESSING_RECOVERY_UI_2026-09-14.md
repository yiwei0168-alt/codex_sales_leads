# 恢复提案页面入口（阶段87）

已保存的部分结果若停止于 processing-incomplete 或 role-unresolved，任务详情提供“准备缺项恢复计划（不执行）”。新增鉴权POST接口只接受严格的 propose:true，请求仅保存提案。费用/来源冲突返回409，不进入执行。已有关联从数据库读取，刷新后仍指向同一子任务。

子任务页面展示原范围待处理公司数量、原任务已保存数量、原任务/结果/费用链接，以及继承预算。当前运行数量独立展示，不把父子计数直接相加冒充唯一公司数。复用原确认入口，首次执行确认框说明必要补证/模型可能产生费用及原任务与各级恢复预算同时生效。

验证：新增3项API测试覆盖未认证、无效ID/参数、越权参数注入形状、会话用户归属和费用冲突拒绝。全量878测试/193文件、生产build及局部lint通过。真实Chrome测试使用隔离账号、零原任务上限和合成保存结果，验证页面准备、刷新复用、原任务链接、继承0美元上限及取消确认不创建作业。测试不接受执行确认，不进行付费业务。

最新3017生产构建（session6583）桌面1366/移动390共56组Chrome回归通过，含两个新增恢复页面组。首次浏览器测试在异步任务详情渲染前count按钮，误跳过点击而失败；改为等待恢复区可见再检查。第二次通过操作后，旧收尾断言漏计新增的预置已完成来源作业；改为精确核对两项预置作业及该来源的7项保存事件，最终通过。失败及成功fixture均已清理，无产品规则放松。

效率：POST复用阶段83聚合保存/复用审计；GET及刷新不写采用事件，准备和取消确认不算用户采用/执行。没有客户隐私、邮件内容或密钥进入文档。后续需验证实际点击接受确认后的完整业务、跨恢复链结果唯一性、长期暂停依赖失效；本阶段不证明最终数量补满或冻结测评改善。
