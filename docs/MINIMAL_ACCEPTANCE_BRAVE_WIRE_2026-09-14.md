# 阶段113：最小路径核心 Brave 请求合同预检

在阶段112只读路径清单上，增加生产 `createDiscoveryProvider("brave")` 的合成请求捕获。使用当前 Colombia/Distributor/目标1家计划、现行核心路由及确定性市场计划查询，注入临时合成密钥和会立即抛出预算拒绝的进程内传输。真实网络调用为0；请求只在内存通过当前静态费用合同及 `assertRequestContract`，不记录查询内容或凭据。原环境密钥在结束时恢复，预算占用前后只读核对。

捕获结果：`GET /res/v1/web/search`、查询参数127字节、`count=2`、哥伦比亚在 Brave 不支持的国家参数上按现有产品逻辑为 `country=ALL`、严格静态预留 USD0.005/次，合同检查通过。这说明生产提供方对这条**合成**查询的传输形态与当前费用合同一致。`ALL` 是上游地域过滤退化，实际用户请求范围仍需由查询及候选国家门禁约束；不能据此认定产品已找到哥伦比亚公司、地域覆盖充分或真实市场查询已验收。[现有提供方回归](../src/providers/discovery.test.ts)也断言 CO 使用 `ALL`，本阶段不改路由或搜索范围。

阶段112的其余缺口保持：现行 Sol 市场计划 USD27.345252 单次预留超 USD17.675596 余额；SearchAPI/Gemini/Terra 缺完整可执行合同，Sol 裁决12,000输出超现行合同；实际市场计划知识上下文、其他实际请求、条件触发次数和整次上界未知。`actualRequestContractsChecked=false`，不得启动 A11 付费闭环。

验证：只读预检、typecheck、脚本 lint、Brave提供方与费用请求合同定向测试通过；预算占用仍为 USD12.324404/30。此阶段合成提供方查询输入1、捕获请求输出1、合同有效1、用于内部预检1，真实业务候选/下游线索/用户采用0；HTTP请求、模型token、搜索API credits、现金、付费重试均0，供应商延迟未知。优化机会是按实际执行器生成的查询捕获请求并核实国家过滤与真实返回，随后逐项补齐其他可达路由，避免为技术门禁失败付费。
