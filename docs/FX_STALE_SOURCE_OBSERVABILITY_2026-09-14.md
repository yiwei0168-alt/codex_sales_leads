# Stage 195 — official FX stale-source observability

The ECB refresh previously mapped every unsuccessful fetch to `unavailableOrInvalidReference`. A valid official XML response dated 2026-09-11 is unusable from 2026-09-14 00:00 UTC under confirmed A10's conservative 72-hour rule, but the old observation could not distinguish that case from a transport or malformed-response failure. A synthetic current-date response reproduced the ambiguity. The earlier real 07:49 UTC `unavailable` result cannot be retroactively classified.

The parser now raises a typed stale-reference error only after date and rates parse successfully and the 72-hour window has elapsed. The refresh preserves the existing `unavailable` database state, one-hour recovery, prior snapshot, and paid hold; it writes `staleOfficialReference:1` to the append-only discarded-reason metrics for that known case. Other exceptions stay `unavailableOrInvalidReference:1`. The single-purpose refresh return also carries the safe `failureClass`; no raw XML, exception, credentials or personal data are logged. The one-second-before-expiry boundary still accepts the reference. No migration or business tariff changes.

Verification: 10 focused FX tests and a full 1,022 tests across 207 files, typecheck, changed-file ESLint and production build passed. The old standalone FX verifier was updated to assert the typed stale error; it was not run because its migration replay and second official fetch are unnecessary for this narrowly scoped refresh. Synthetic refresh inputs: one official-date response generated one discard and zero valid/downstream-used outputs; transport failure generated one distinct discard; each used one free HTTP mock, zero token/API credits and USD0, zero retries, at most one-hour next retry. Actual network latency, live customer adoption and paid billing remain unknown. The concrete efficiency opportunity is to separate stale-publication follow-up from network diagnostics while sharing one daily source snapshot.

Scheduled official-source result: at 08:49:42 UTC, one due credential-free ECB HTTP attempt returned a well-formed but expired official reference. The refresh returned `status=unavailable`, `failureClass=staleOfficialReference`, `httpCalls=1`, and stored next attempt 2026-09-14 09:49:42.422 UTC. The read-only status still showed snapshot `asOf=2026-09-11T00:00:00Z`, expired at 2026-09-14 00:00 UTC; it did not store a new rate. The command reported zero paid provider calls and zero jobs claimed. This classifies **this** retry only, not the earlier 07:49 result. Do not retry before the stored next-attempt time.

The original USD30 acceptance ceiling and USD12.324404 occupancy are unchanged. This is A05/A10/A08 observability and no-paid regression evidence, not a completed A11 live chain or a new confirmed rate.
