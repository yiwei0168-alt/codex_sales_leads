# Stage 216: identify an oversized qualification unit for P06 recovery

The fact-phase planner now raises `OversizedQualificationFactUnitError` when one bounded phase still exceeds the exact provider byte limit. It retains the original source fingerprint and the selected original finding statement or evidence excerpt, with its stable ID and citation closure. Selection uses the excerpt actually visible after approved supported-source folding, so a folded long source does not hide a still-oversized critical statement. The error remains a `LeadRequestTooLargeError`, preserving the existing technical-pending and no-paid-call behavior.

Synthetic tests cover a long current source, a long corrected statement, and a longer source safely folded beside a long critical statement. Full 1,040 tests in 208 files, TypeScript, focused ESLint and production build pass. No provider transport, paid reservation, job, or database mutation was made. The separate chunk planner, SQL checkpoint and complete ordered assembly are **still not wired into the qualification Agent**. This stage identifies the exact unit for that next step; it does not score a large company or complete P06/A11.

Efficiency: three new synthetic oversized candidate variants produced three valid recoverable unit identities, all used by local assertions. One previously covered ordinary phase path remained unchanged. Product downstream output and real user adoption are unknown; model tokens, paid API credits, cash, calls, retries and business-output discards were zero. Local full regression took about 20 seconds, actual model latency unknown. Opportunity: use the typed unit to plan and checkpoint only the necessary segments, while preflighting a bounded final score before buying any chunk.
