# Current-build local production UI regression — stage 190 (2026-09-14)

At commit `9219fa7`, after complete database migrations 001–075, `npm.cmd run build` produced the Next.js 16.3.4 production build. A local `next start` service ran only on `127.0.0.1:3029`. The existing `verify-authenticated-ui.ts` then used real Chrome against that production HTTP service at desktop 1366×900 and mobile 390×844. All **66 checks passed** (33 per viewport). The service was stopped after verification.

The checks covered login and disabled-session denial, nine task outcome/shortfall states and refresh, country switching and private state, role/tier/path and personal-memory edits, manual channel-map persistence without model calls, strategy draft manual body/approval/reuse, usage and tariff/FX APIs, task and company cost views, pause/recovery checkpoint presentation, stale-evidence reminder and navigation runtime errors. The verifier seeded an isolated synthetic user/workspace/company set, one synthetic cost reservation and local checkpoints; it removed the fixture at exit. A synthetic claimed-job pause boundary was exercised, but no existing user job was claimed and no natural-language business generation, real search/model transport or SMTP occurred. The post-run read-only A11 preview showed USD12.324404 occupied of USD30, USD17.675596 remaining, zero paid provider calls, zero claimed jobs, missing checked tariffs and no whole-run bound.

This establishes that migrations 073–075 and current code have not broken the tested production UI/API paths. It does not prove real user adoption, RAG answer quality, real provider invoice reconciliation, arbitrary P06 input semantics or the A11 natural-language-to-qualified-company loop. A11 and overall acceptance remain open.

Efficiency record: input was one current production build, two browser viewports and 66 isolated checks; valid outputs 66, all used for A10/A13 acceptance. Customer lead output and actual adoption remain unknown. The synthetic cost reservation was removed, with no live budget delta. Model tokens, paid API credits, cash, external retries and mail sends were zero. The browser run took roughly half a minute; paid provider latency is unknown. Continue to reuse the isolated full-UI verifier after changes to database privileges or user-facing data paths, without using customer tasks for regression.
