# 阶段112：最小业务闭环的当前付费路径覆盖预检

只读扩展 `scripts/preview-minimal-production-acceptance.ts`：用生产的市场计划构造器、当前混合搜索策略、现行静态费用合同及数据库公开费率状态，列出一条合成的 Colombia / Distributor / 目标1家路径。脚本不创建任务、不认领既有队列、不调用模型或搜索、不改预算。它仍只捕获一条合成 LangChain 市场计划真实序列化请求；其余模型参数和搜索路线仅作静态可达性检查，不能据此声称实际请求已验收。

2026-09-14 当前配置及只读账本：累计上限 USD30，占用 USD12.324404，余额 USD17.675596；历史未知账单6笔仍保留。首轮目标候选池2。市场计划 `openai/gpt-5.6-sol` 的现行单次上界 USD27.345252 超余额，合成传输4,171字节/4,096输出符合现行合同。S01 仍待用户确认，新路由和 USD10.622880 候选上界未准入。

| 实际策略可达路径 | 本次静态证据 | 剩余验收 |
|---|---|---|
| Kimi轻量意图、条件复杂意图；北京Embedding | 单次上界依次 USD0.283612、USD3.345151、USD0.006412 | 真实请求字节、输入及多轮调用次数未验证 |
| 市场计划 Sol | 合成 SDK 请求合同有效，当前单次预留不可容纳 | S01决定、实际RAG上下文与完整费用预检 |
| Distributor 发现 | 核心 Brave USD0.005；缺口触发 Exa USD0.027；SearchAPI与Gemini无完整严格合同 | 实际查询/返回、触发、两缺口入口上界；不能推断核心入口会找到合格公司 |
| Tavily补证 | 静态 USD0.016/次 | 实际补证请求及次数 |
| DeepSeek校正及主评分 | 当前配置均为 `deepseek-v4-pro`，各自静态上界 USD1.416561/次 | 实际批次、输出及账单；不能将两项或多家公司费用当一次 |
| 条件二次复核及分歧裁决 | Terra 为 `missing-tariff`；Sol裁决12,000输出为 `request-out-of-bounds` | 合同变更确认、完整请求核验、条件发生时的逐次保守预留 |

脚本输出 `checkedTariffsAvailable=false`、`allSearchRouteBoundsPresent=false`、`actualRequestContractsChecked=false`、`totalRunBoundUsd=null`，不是付费准入结果。首轮候选池2不等于两家公司会被找到或合格；条件路径的费用不能无条件相加，也不能假设不会触发。真实运行应逐次核销并核验剩余预留；预算或供应商约束导致部分完成时，页面必须保留缺口及停止原因。

验证：只读预检再次运行及预算前后占用一致，`typecheck`、脚本 lint 通过；供应商调用/模型token/API credits/现金/重试均0，业务候选生成与用户采用均0。此次脚本输入1个合成计划，输出8项模型阶段、4条发现路由、1条补证路由及1条合成请求合同结果；这些只供预检决策使用，不算真实业务采用。脚本本地运行约6秒，非供应商延迟。优化机会是逐入口捕获真实请求契约，完成缺失费率及 S01 路由决策后再做整次保守预算预检；不能以单次报价直接启动A11。整体验收仍未完成。
