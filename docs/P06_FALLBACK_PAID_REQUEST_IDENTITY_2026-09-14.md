# P06 备用路由付费请求身份前置核对（2026-09-14）

本阶段最终回归：全量1006项/206文件、生产构建、类型检查、改动文件lint及生产依赖审计0漏洞通过。

阶段160为现有 OpenAI 兼容提供方补充只读的 `paidRequestFingerprint`。它使用与费用层相同的 `paid-request-replay-v1` 算法，绑定实际 POST 端点、模型、完整请求体及输出限制，不含密钥。现行 Resilient Provider 仍只对外公布主路由缓存和付费身份；本阶段没有改变路由选择、批准新的供应商或允许备用响应以主路由身份入库。

无付费测试以本地拦截的 `/v1/chat/completions` 请求体、OpenAI 模型映射和供应商偏好字段核对费用哈希完全相等；模型变化使哈希变化，密钥不出现在身份中。提供方身份测试6项、相关提供方测试共17项及类型检查、改动文件 lint 通过。合成输入1、结构响应有效1、下游仅用于身份核对1；真实用户采用未知。真实 token/API credits/现金、外部重试均0，本地测试约2秒，真实延迟和单位线索成本未知。

这只是备用恢复的必要条件。评分阶段及最终响应检查点当前仍只接受主提供方；备用路由的实际模型、请求合同、完成响应和跨进程读取尚未绑定，故返回备用响应时继续安全暂停，不能再付费重放。默认 OpenRouter 备用路由的严格费率合同仍有缺口，A11 整次上界仍为未知。优化机会是将已批准备用路由的**实际**请求身份与结果分别保存和读取，并证明在配置变化、费用未知或响应截断时不重放；真实语义与账单随后核对。
