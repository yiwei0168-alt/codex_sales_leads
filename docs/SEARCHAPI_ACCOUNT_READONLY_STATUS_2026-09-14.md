# SearchAPI 当前账号只读额度核验（阶段169）

2026-09-14 05:24 UTC，按 [SearchAPI 官方 Account API](https://www.searchapi.io/docs/account-api) 的 `GET /api/v1/me` 只读查询当前产品配置的 SearchAPI 账号。凭据仅从本地环境注入请求头，不写入命令输出、日志或 Git。成功响应为 HTTP 200；只投影非身份字段：`account.monthly_allowance=0`、`account.remaining_credits=0`、`api_usage.hourly_rate_limit=200000`，响应没有可核验的订阅价格/速度等级字段。本次不是 Google/Bing 搜索请求，未认领产品任务、未修改账号或预算；产品账本新增预留与核销均为 0。

这证明**查询时**账号报告的月度配额和剩余额度均为 0，不证明此后一直为 0，也不能由公共套餐表推定账号的实际单次价格或增强速度费率。当前 `searchapi-google-bing-v1` 因缺账号级严格合同仍在 `missing-tariff` 门禁中；本证据不放行该入口、不将工具不可用解释为哥伦比亚市场耗尽。若账号后续获得额度，仍需另行核对收费计划、请求合同、有效期及预算，才可参与真实业务。

查询后复核隔离验收账号的产品账本：累计上限 USD30、占用 USD12.324404、剩余 USD17.675596、历史未知账单 6 笔，均与查询前相同；新供应商付费调用 0。这个账号状态仍不能推算整个任务的费用上界。

效率口径：发起只读账户状态查询，有效响应 1、用于费用准入判断 1、真实搜索线索 0、用户采用未知；模型 token、搜索 API credits、产品现金预留、付费重试均 0，外部实际计费未知，成功命令约 1 秒。首次受限沙箱请求失败且未取得响应，获准的只读请求成功。优化机会是利用账号状态与严格合同一并判定路由可用性，避免对无额度入口的无效搜索计划；不据此关闭其他市场或搜索工具。
