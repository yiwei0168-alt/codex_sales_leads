# Scheduled ECB FX retry — stage 188 (2026-09-14)

At 07:49 UTC, after the stored one-hour backoff expired, the existing credential-free `refreshBillingFxReference` attempted one official HTTP fetch. It returned `status=unavailable`, so no validated new FX snapshot was inserted. The stored 2026-09-11 reference still reports `expired-or-invalid` after its 2026-09-14 00:00 UTC expiry. The refresh state set its next attempt to 2026-09-14 08:49:24 UTC. A second invocation within the backoff returned `cached-refresh-state` with zero HTTP calls. The unavailable result does not establish whether the official source had a newer rate; it preserves the conservative paid CNY-request hold.

The new [single-purpose command](../scripts/refresh-billing-fx-reference-once.ts) invokes only the existing public FX refresh and read-only status functions, then closes its DB pool. It does not start the lead worker or claim a user job. The current no-paid A11 preview still reports USD12.324404 occupied of USD30, USD17.675596 remaining, `checkedTariffsAvailable=false`, total-run bound null, zero paid provider calls and zero claimed jobs. The approved S01 route and other rate contracts did not change.

Verification: the live scheduled invocation and cached follow-up passed; nine focused FX/ECB unit tests, typecheck and script lint passed. The workflow input was one due public reference fetch; valid new-reference output and downstream-usable FX output were zero. One unavailable/invalid source result was discarded, while the existing historical snapshot was retained. Free HTTP calls one, repeated HTTP calls zero; model tokens, paid API credits, cash, paid retries and SMTP zero. Local refresh/status took about two seconds; actual downstream customer use and provider billing latency remain unknown. Continue shared one-hour backoff and reject CNY-bound paid requests until a fresh validated reference is stored.
