# 阶段 237：S02 市场计划参数修订与无付费验收

用户明确回复“批准 S02 并继续最小真实验收（推荐）”，并补充：“遇到类似阻塞点时，降低要求，以跑通产品主要端对端业务验收为准。类似问题记录、妥协或者绕开。”本阶段只实现已具体批准的 S02：现有 OpenRouter credits `openai/gpt-5.6-sol` 市场计划继续固定 `provider.only=["openai"]`、`allow_fallbacks=false`、严格 JSON Schema、最多 61,440 请求字节与 4,096 总输出 token，单次保守上界 USD10.622880；SDK 请求从 `max_completion_tokens=4096` 改为 `max_tokens=4096`，省略 `temperature`。未改变模型、价格、供应商路由、合格标准或哥伦比亚冻结测评。A27 的“类似问题”原则用于后续隔离产品验收的技术阻塞处理；本阶段没有泛化关闭费用、身份或角色门禁。

`request-bounds-v1.10.0` 仅将市场计划专用请求合同升为 `openrouter-sol-openai-playbook-v2`，保留 v1.9.0 与更早规则文件；旧请求、费用和公共费率暂停关联不迁移。市场计划缓存身份显式纳入 v2 请求合同，使旧缓存不能伪装为新合同结果；付费请求指纹仍由完整实际请求生成。非市场计划 Sol、Terra 复核与 Sol 分歧裁决沿用各自原合同。旧失败动作 `537155d0-8c04-4ca8-9754-54d658b12027` 和其费用未知预留不重放、不释放。

无付费证据：实际市场计划 LangChain 合成传输 4,186 字节，参数仅 `max_tokens/messages/model/provider/response_format/stream`；输出上限 4,096，严格 JSON 与 OpenAI-only 路由通过 v2 合同。额外 `max_completion_tokens`、`temperature`、工具、服务层、非 OpenAI 供应商及超限请求仍在外发前拒绝。当前全量 1,052 项/208 文件测试、类型检查、变更文件 lint、Next.js 生产构建通过；生成工作流文档已按源码更新。只读最小路径预检显示本次 Sol 单次 USD10.622880 可放入 USD26.479080 余额、核心 Brave 搜索有合同；SearchAPI/Gemini 条件路径和 Sol 高价裁决仍可能在到达时按 A25 暂停。合成请求与预检均未发出供应商调用或新增预算占用，也不能证明 OpenRouter 实际端点必然接受。

费用与效率口径：本阶段一项已批准请求修订、一次实际上下文合成线形及全量回归，线形输出有效并用于下一步验收；真实候选、合格线索和用户采用未增加。外部模型 token、API credits、USD、重试及邮件均为零，测试约19秒、构建约19秒，真实供应商延迟未知。下一步先只读重核 USD50 账本和隔离身份，然后创建**新的**最小自然语言任务；若供应商再次拒绝，记录错误和妥协范围，不将故障算作搜索耗尽或合格公司不足。
