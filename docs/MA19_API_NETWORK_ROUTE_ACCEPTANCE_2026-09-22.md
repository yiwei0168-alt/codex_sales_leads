# MA19 external HTTP API routes — 2026-09-22

The user confirmed that fixed product HTTP API calls other than GLM, Kimi, DeepSeek and the Alibaba Cloud compatible embedding API use the local proxy. Non-HTTP connections and on-demand conditional destinations are outside this route change. This expands MA18 without rewriting its historical decision.

Implementation: the shared paid-request transport routes Google Places, Exa, Brave, SearchAPI and Tavily to the existing local `MODEL_PROXY_URL`. Gemini continues through `GEMINI_PROXY_URL`. OpenRouter requests route by model: `z-ai/glm-*`, `deepseek/*` and `moonshotai/*` remain direct; other model IDs and model-unspecified gateway calls use `MODEL_PROXY_URL`. Historical Batch receipt reads now apply the same rule even if their saved model identity is absent. Both proxy settings accept only credential-free loopback HTTP proxy URLs and fail before a direct request if the required proxy is missing or invalid. Explicitly injected transports in tests remain authoritative. Direct Kimi and DeepSeek API hosts, Alibaba Cloud embeddings, local LangGraph/BGE, mailbox protocols, official-company websites, Skill imports and optional Snov/LangSmith connections were not changed.

Verification: 39 transport/discovery/Tavily/Batch tests and 38 paid-fetch/SDK/OpenRouter tests passed. TypeScript, targeted ESLint and `git diff --check` passed. Tests assert proxy selection for each fixed provider, the direct exceptions, injected transports, and failure on an invalid local proxy. A read-only database check found no running or queued Agent/lead jobs before restarting the local LangGraph and main Agent worker processes. The restarted LangGraph `/ok` endpoint and existing Next.js home route both returned HTTP 200, with no startup errors in the new process logs. This verifies routing behavior and local service health; it is not an authenticated provider-connectivity check or proof that a paid request succeeded. No paid call, SMTP operation or historical outcome-unknown replay was made.
