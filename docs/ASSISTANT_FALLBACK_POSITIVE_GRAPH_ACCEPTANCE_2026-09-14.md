# 阶段141：两轮自然语言降级计划至正结果入库

隔离脚本在进程内暂时移除 Kimi API key，使实际 `processAssistantMessage` 走明示的 `deterministic-fallback`，不发起意图模型请求。第一轮输入“搜索哥伦比亚2家分销商”，生成目标2的待确认计划；第二轮“请改为搜索哥伦比亚1家分销商”，旧提案状态为`cancelled`，新提案目标1、国家CO、角色仅Distributor，两条助手消息均记录确定性降级来源。随后调用现有确认入队与任务认领，产品图执行合成知识、计划、搜索、补证、校正、评分及复核；评分前受控暂停并从PostgreSQL检查点恢复，前置阶段各只运行一次。真实产品SQL最终合格并保存1家、交接1、回执1、`target-met`。

本地生产服务的实际任务API及Chrome 1366×900、390×844两视口显示目标达成，刷新稳定；跨用户公司读取拒绝。7 micro-USD夹具共享预留完整归属，0未分配；完整清理测试账号、工作区、任务、全局公司身份与检查点。原验收账号预算和6笔未知历史费用未改变。

此验收的自然语言**仅由确定性降级解析**，确认通过产品服务函数而非浏览器按钮，图内各业务阶段输出仍由夹具提供。它补足无付费的对话服务→修改提案→确认→图→SQL→页面接线，不证明真实Kimi意图、多轮语义理解、真实搜索/补证/角色/评分模型、可信账单、市场合格率或A11完整付费闭环。

遥测口径：输入自然语言2轮、生成有效待确认计划2、下游实际使用新计划1、旧计划因用户修改取消1；合成候选1、有效评分1、保存1，用户实际采用未知（确认夹具不代表真实用户采用）。意图模型/搜索/SMTP真实外发0、真实token/API credits/现金/付费重试0，夹具预留7 micro-USD不是真实费用。内部计划利用率1/2、有效评分到保存1/1，不推断商业效率；本地SQL模式约5秒、生产双视口模式约8秒，供应商延迟和实际节省未知。优化机会是保留同一输入/输出及费用边界，在预算与合同齐备后用真实供应商链路逐项替换夹具而不重复已完成工作。

验证：`verify-positive-result-graph-sql.ts --assistant-fallback --pause-resume` 的SQL及本地生产双视口模式通过，临时清空密钥只作用于隔离脚本进程且完成后恢复；全量984项/203文件、typecheck、生产构建、全库lint 0错误/11既有警告及生产依赖审计0漏洞。只读重核原验收账号USD30上限、占用USD12.324404、余USD17.675596及6笔历史未知账单未变；无正式测评或外部付费调用。
