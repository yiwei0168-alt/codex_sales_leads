# Stage 202 — current-build local production UI regression

After the stage 200 SearchAPI effective-account recovery change and stage 201 evidence reconciliation, the current Next.js 16.3.4 production build was served only at `127.0.0.1:3029`. The existing `scripts/verify-authenticated-ui.ts` used real headless Chrome against local production HTTP, API and database at desktop 1366×900 and mobile 390×844. All **68 checks passed, 34 per viewport**. They covered authenticated login, measured discovery targets/gaps/stop labels including old and new stagnation reasons, country detail and isolation, user role/tier/path edits and memory, manual channel-map persistence, strategy draft reuse, task/recovery status, cost and FX/rate surfaces, usage refresh idempotency, and disabled-user denial. The script used one isolated synthetic user/workspace/company with one synthetic reservation and reported its cleanup; local port 3029 was no longer listening afterward.

The follow-up read-only budget check returned USD12.324404 occupied out of USD30, USD17.675596 remaining, six historical unknown bills, zero new provider calls and zero unsettled calls. The browser verifier reported `paidCalls=0`, `realMailSent=0`, and did not claim a user job. This check tests UI/API/database behavior under synthetic inputs. It does not exercise an actual model/search request, prove true user adoption, settle historical unknown bills, validate arbitrary private routes, or complete the A11 natural-language-to-qualified-country-result and trustworthy-bill chain.

Validation scope: stage 200 full 1,026 tests across 207 files, typecheck, production build and focused lint passed before this current-build browser run. The service was stopped after the run. No source, tariff, route, model or migration changed in this stage.

Efficiency record: one current production build, two browser viewports and 68 isolated check inputs yielded 68 valid results, all used for A09/A10/A13 local acceptance. No customer lead or downstream customer adoption was produced; those volumes remain unknown. Model tokens, paid API credits, USD spend, real provider calls, external retries, mail sends and discarded business outputs were zero. Browser execution took roughly 30 seconds; real provider latency is unknown. Opportunity: reuse this isolated verifier after material recovery or user-data-path changes and keep its result separate from the paid A11 closure.
