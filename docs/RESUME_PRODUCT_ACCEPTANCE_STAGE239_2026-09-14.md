# 产品端到端与质量闭环恢复节点（阶段 239，计划已确认、尚未实施）

## 下次从这里开始

基线提交为 `34acffc`（已推送 `origin/main`）；先核对 `git status`、[当前唯一验收矩阵](CURRENT_ACCEPTANCE_MATRIX_2026-09-13.md)、[阶段 238 真实部分验收](S02_MINIMAL_LIVE_PARTIAL_STAGE238_2026-09-14.md)和隔离账号只读账本。不要把本文件的计划视为已实现或验收证据。德国实验未跟踪产物与本任务无关，不纳入提交；不处理用户已有待执行任务。

阶段 1–3 的哥伦比亚候选流失调查及 O01–O05 已完成产品实现与确定性回归，冻结 39 个输出槽位／38 家唯一公司和盲审结论不变。阶段 4–7 的主要代码及无付费 SQL、跨进程、桌面／移动回归已有证据，但真实费用核销、真实任务聚合与用户采用仍未验收。阶段 8 的新隔离任务 `b3d7fa08-3b37-40b1-a505-158da92a89a0` 已真实走过自然语言、西班牙语计划确认、S02 市场计划、Brave 搜索、Tavily 补证、DeepSeek 角色校正与首次评分；Terra 必要复核无有效输出且费用未知，同指纹请求被防重放阻止。该任务有 9 个图检查点，尚无 `lead_search_run`、最终评估、国家公司或最终阶段遥测；不能把首次评分计作最终合格。

上次只读账本：隔离 USD50 上限，保守占用 USD39.870306，剩余 USD10.129694，29 笔账单不完整；旧 S01 HTTP404 动作 `537155d0-8c04-4ca8-9754-54d658b12027` 及新 Terra 请求的未知费用继续保留。下次实施前重读实际账本，不把这些快照当实时余额。当前余额低于 Terra 单次 USD11.019202 上界；不得重放旧动作、同指纹复核，或人工释放未知费用。固定验收汇率为库内 `ecb-cny-usd-2026-09-11`，不因验证日历过期或再次抓取；普通产品每周刷新规则不变。

## 本轮已确认的验收口径

用户要求“暂时不验收真实供应商费用，以尽可能完成端到端工作流完整和质量闭环、验收为主”，并在规划澄清中选择“额度内允许调用”和“分层验收（推荐）”。真实新调用仅限现有已批准请求合同、隔离 USD50 累计额度、已知单步上界和原子预留，未知结果不自动重放。完整、唯一匹配的供应商账单和实际现金线索成本本轮暂缓验收；费用未知仍显示未知且占用原预留。

分层出口：① 受控产品与质量闭环可在真实产品入口、LangGraph、PostgreSQL、HTTP 与桌面／移动页面上验收，外部模型和搜索用确定性传输响应；② 真实供应商链目前只证明到首次评分，Terra 复核、真实入库、实际质量和供应商账单单列未通过。不得把两个不同任务的证据拼成同一条真实闭环，也不得以受控模型结果宣称填满 50 家、恢复冻结盲审或完成原计划整体验收。角色、证据、评分、复核、国家／用户隔离标准不降低。

## 下次实施顺序和完成条件

1. **基线与停点。** 只读核对隔离账号、原动作、图检查点、Terra 首次请求状态及余额；用现有 `scripts/audit-s02-minimal-live-workflow.ts`、`scripts/verify-acceptance-budget-status.ts`。区分候选公司数、请求数与阶段输出数，不披露公司、邮件或密钥。任何未证明未付费的原请求保持不可重放。
2. **同一条受控正结果图。** 扩展现有 `scripts/verify-positive-result-graph-sql.ts`：走产品 `processAssistantMessage` 的两轮计划修改和确认，现行搜索／补证／校正／评分 Agent 的真实序列化与校验逻辑，外部传输使用固定响应。以 `LeadAssessmentReviewAgent` 和确定性 `LeadReviewInvoker` 执行必要盲审，断言盲审输入不含主评分；同意才确认，重大分歧经现行裁决，目标一家最终只保存一家。通过真实 SQL、任务 API、生产 Chrome 桌面／移动页面和刷新核对同一个合成动作的国家结果、数量、停止原因、交接与成本分摊。夹具与端口在结束后清理。
3. **质量反例与中断恢复。** 同一验收套件覆盖错类转移、重复、证据不足／角色未定、复核无有效输出、裁决要求补证、预算阻止及未知费用。技术失败不得写合格或宣称搜索耗尽；完成检查点只复用，不重付费。审查 `review-failed` 与最终保存筛选是否一致，如实际规则存在可输出未完成必要复核的漏洞，则在既有合格标准下修复并补回归，不修改冻结历史评分。
4. **真实失败进度和遥测。** 对照阶段 238 的图检查点检查任务详情能否显示最后成功环节、已发现／待处理／最终合格／缺口及“复核未完成、费用未知”。目前完整 `workflow_stage_metric` 仅在 `persistLeadWorkflowResult` 写入；若失败前记录缺失，使用与检查点绑定的幂等内部进度记录或安全读取，使刷新／恢复不重复计数。不得把有效 HTTP 响应数当公司数，缺失 token、credits、成本及用户采用维持未知。真实旧动作仅只读核查，不用新夹具污染其账本。
5. **可选真实尝试。** 仅当新请求有已批准的实际合同、不会触碰未知原请求、按执行时余额能完整预留时，才考虑下一次最小真实调用；否则记录阻止原因，继续受控产品验收。新模型路由、参数或费率若需变更，先形成具体边界并按 A11 单独确认；不得用 A27 概括授权。SMTP 已有通过证据，发送实现未变则不重发。
6. **回归、报告和提交。** 运行相关单元／集成、真实 SQL、类型检查、lint、Next.js 生产构建、依赖安全检查及本地生产双视口；Next.js 应用代码改动前读 `node_modules/next/dist/docs/` 对应指南。逐阶段同步确认规则、PRD、工作流文档、搜索文档、效率台账和当前验收矩阵，区分预留／估算／报告／核验账单；检查差异后提交并推送 `origin/main`。最终仅在所有受控链断言通过时写“本轮产品层验收完成”，真实供应商语义／费用及原计划整体验收继续标未通过。

验收断言至少包括：一家公司凭当前引用和指定 Distributor 主角色经必要复核后只保存一次；错类、重复和证据不足不填数；盲审分歧进入裁决且未完成复核不伪装为确认；同一任务 SQL／API／双视口数量、状态和用户归属一致；暂停与跨进程恢复无重复费用或采用量；共享费用分摊守恒、未知金额不写零。合成数据的真实供应商 token、API credits、现金和用户采用为零或未知，不能借合成覆盖真实值。
