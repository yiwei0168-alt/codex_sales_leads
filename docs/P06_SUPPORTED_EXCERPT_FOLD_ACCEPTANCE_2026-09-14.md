# 阶段146：P06 单公司证据原文的受限结构化压缩

现行评分请求先沿用无损的重复文本、字段表和共享短语压缩。仅当单家公司仍超出完整请求字节上限时，新增 `supported-excerpt-fold-v1`：保留已进入该请求的所有校正事实完整对象、状态、顺序和证据引用，保留请求内全部证据 ID、URL、标题及原始公司状态；只对可由已支持事实追溯且非每类首条关键来源的冗长证据原文，按长度确定性折叠。任何被未支持、冲突或未知事实引用的原文均受保护。请求明确说明折叠文本不是独立佐证，不得推断被省略的内容，若原文缺失会影响门禁或分数应标记未知并要求复核。原始证据和校正事实仍留在任务状态及持久化快照中，`requestPreparation` 记录折叠条数、完整与准备后字节数。最终请求继续按实际提供方序列化字节和原61,440字节上限检查；超界仍是 `retry-required`，不会算低分或业务拒绝。

定向合成 Agent 验证：55条高度独有的补充事实/证据，含1条冲突事实，现行 DeepSeek 请求序列化及缓存身份下可完成1次模拟评分；发出请求保留全部事实与证据 ID/URL，冲突事实原文保持不变，原公司对象未修改。把全部事实改为冲突时，0模型调用、技术待恢复1。150条独有事实的旧边界仍会技术暂停，说明此方案扩大可处理范围，但尚未完成任意大单项的分阶段处理。模拟模型输出不证明真实模型对折叠标记的遵从或评分质量，不能据此修正冻结盲审或推断填充率。

验证：定向40项/2文件；全量986项/203文件、类型检查、生产构建及改动文件 lint 通过。原验收账号只读预算仍为 USD12.324404/30，6笔历史未知账单保持；真实模型/搜索/SMTP、token/API credits、现金及付费重试均为0。输入为2种55条事实场景和既有150条回归，有效模拟评分1、技术待恢复场景继续保留，用户实际采用未知；本地定向运行约数秒，真实供应商延迟与成本节省未知。下一步仍需任意大单项分阶段处理与真实模型结构遵从验收。
