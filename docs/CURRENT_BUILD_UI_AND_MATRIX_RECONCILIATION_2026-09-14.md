# Stage 220: current-build production UI and acceptance-matrix reconciliation

The production build at `97d4f10` was served only on `127.0.0.1:3030`. `scripts/verify-authenticated-ui.ts` exercised real local HTTP, application database paths and headless Chrome at 1366×900 and 390×844. All **68 checks passed, 34 per viewport**: login and disabled-user denial, country/role/tier/path edits and memory, manual channel-map persistence, draft approval/reuse, task counts and stop reasons, recovery, cost/FX/rate and usage refresh, and privacy-visible navigation. The isolated synthetic user/workspace/company was removed; the local server was stopped and no 3030 listener remained. The verifier reported zero paid calls and zero mail sends. A subsequent read-only ledger check remained USD12.324404 occupied of USD30, USD17.675596 available, six unknown historical bills, zero unsettled calls and zero new provider calls.

The current acceptance matrix now distinguishes current contract and UI status from its older stage narratives. A22 Tavily basic Extract and A23/A24 Terra/Sol judge are active under their approved single-call rules; the old proposal-only rows remain historical. The read-only minimal-run preview reports Extract `static-bound-present` at USD0.032000, Terra and Sol judge `available`, but `totalRunBoundUsd=null`, `checkedTariffsAvailable=false` and zero provider calls/jobs. Kimi intent and Beijing embedding appear as `expired-tariff` because the required CNY FX reference is stale; SearchAPI and Gemini discovery remain `missing-strict-contract`. The four-phase synthetic playbook-plus-Pro example is USD19.122246, above the current USD17.675596 balance. The preview wording now reflects the activated Extract contract while still marking its real response and the whole run unverified.

This is current-build UI and read-only prerequisite evidence, not a real natural-language search-to-qualified-result chain, trusted bill or user adoption. No frozen Colombia experiment or other market was run. The A11 paid gate stays closed.

Efficiency: one current build, two viewports and 68 synthetic checks yielded 68 valid local results, all used for A09/A10/A13 acceptance. Three read-only preview inputs yielded current contract/blocker observations, with zero business leads or downstream customer adoption. Paid model tokens, API credits, cash, external retries, mail and discarded customer outputs were zero. Browser verification took about 40 seconds; real provider latency remains unknown. Opportunity: keep the matrix's current rows tied to the latest verified build and preserve historical stage snapshots without treating synthetic UI output as lead yield.
