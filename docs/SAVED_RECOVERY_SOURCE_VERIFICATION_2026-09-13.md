# 已保存部分结果的恢复来源核验（阶段82）

本阶段实现 D13/O01–O02 恢复所需的实际读取层：先核对用户预算与已完成原任务，再对账唯一已完成运行、原国家、运行元数据、作业、终结检查点及原任务结果。结果/计划采用完整JSON规范化指纹比较，检查点必须有版本ID，实际已选公司从原运行评估表读取，不由排名推测。原任务或祖先有未知/在途费用时阻止准备。

通过后输出深拷贝的缺项范围、原状态和版本化来源证明（检查点ID、状态/结果/已选身份指纹）。读取不会刷新证据日期、创建任务、生成新公司或变更原结果。`readSavedProcessingRecovery` 已使用实际数据库及生产检查点读取；暂未暴露HTTP入口，避免在执行器未支持恢复子任务时进入普通发现流程。

验证：867测试/190文件、生产build、局部lint通过。`verify-saved-recovery-source.ts` 使用实际结果保存函数、PostgreSQL检查点及生产读取函数，验证重复读取稳定、原状态不变、用户隔离、结果/国家冲突拒绝、在途费用阻止；只有一个原合成任务，无新任务/供应商调用/发信。1微美元仅为合成预留，fixture及检查点已清理。首次验证将底层保存结果误当完整工作流结果，缺少工作流追加的停止原因而失败；修正fixture组合后通过，没有放松产品校验。

范围：此次通过证明来源核验，不证明证据仍有效或语义判断正确。恢复提案原子创建、用户确认、执行器限定缺项与禁止发现、证据依赖检查及新旧结果关联仍未完成。普通Chrome54组沿用阶段81；此阶段没有新页面。

效率：新增读取层未接入用户流程，没有实际生成/保存/下游采用事件；这些观测不填成采用率提升。验证供应商输入、token/API费用均为0；重复读取不产生动作与检查点写入。后续提案需追加一次保存观测、重复请求单独记录复用，执行时沿用预算和各阶段观测，避免重复付费。
