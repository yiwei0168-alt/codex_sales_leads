# 阶段136：超界评分在图检查点暂停与恢复

P06 已有 `LeadQualificationAgent` 完整请求预检：101 条证据/105 条事实的独有短语可无损编码，151 条证据/155 条事实的不可压缩输入抛出 `LeadRequestTooLargeError`，模型传输为 0。本阶段补图边界验证：在合成图运行到 `score_candidates` 时注入同类 70,000/61,440 字节超界，图拒绝继续，检查点停在评分节点，已校正候选及原证据、此前发现/补证/校正额度保留，评分与结果保存均未发生；错误不是业务拒绝或低分。

随后仅把模拟评分依赖换成有效响应，从同一检查点继续。发现、补证及主角色校正各仍只有一次调用，既有额度未清零，图得以完成。原 `LeadQualificationAgent` 的真实超界预检测试与此次图检查点测试是**两个合成夹具**，不能宣称任意大单公司已经能被分阶段评分，或真实模型理解无损编码。不可压缩公司仍需保留未完成状态，等待可验证的分阶段方案或人工处理；不能自动重购已发生费用。

本项输入一家公司、图内有效评分输出在暂停前 0/继续后模拟 1、下游图结果模拟 1，用户实际采用未知；付费模型/搜索/SMTP调用、token/API credits、现金与付费重试 0，业务事实丢弃 0。复用早期三个阶段各一次，相较完整重启避免三次重复工作；实际节省金额和供应商延迟未知。优化机会是以同一检查点扩展可恢复的独有事实分阶段评估，并先证明完整引用及评分契约，不以载荷大小修改业务资格。

验证：定向图测试 28 项通过；全量 **982 项/203 文件**、`typecheck`、Next.js 生产构建、全库 lint 0 错误/11 条既有警告及生产依赖审计 0 漏洞通过。未调用供应商或修改真实预算。
