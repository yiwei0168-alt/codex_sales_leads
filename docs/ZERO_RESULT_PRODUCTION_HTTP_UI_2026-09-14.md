# Stage 122: graph-produced partial result through production HTTP and UI

The stage 121 SQL verifier now accepts an optional `UI_VERIFY_BASE_URL` restricted to local HTTP. With a fresh active fixture account, it runs the same real queue, graph persistence and completion path using synthetic provider-unavailable output. It then logs in through the production auth endpoint, reads the production task API, and opens the actual task page in Chrome at 1366×900 and 390×844. The browser blocks nonlocal requests and page writes. No existing account or queued task is touched.

Both authenticated API reads returned `completed`, accepted 0 and `provider-unavailable` for target 1. Both page widths showed the underfilled status and “缺口 1 家；搜索服务不可用”; reload preserved that text, and document width stayed within the viewport. The SQL assertions from stage 121 also passed: one completion receipt after duplicate completion, no company saved, the 5 micro-USD synthetic reservation unspent/unallocated, and cross-user isolation on five search tables. The fixture users, session and graph checkpoint were removed. The local production server started for this check was stopped afterward.

The first browser launch used Playwright's absent bundled headless shell and stopped before any HTTP page assertion. Switching the verifier to the already installed Chrome channel made the full check pass. This was a verifier environment issue, not a product HTTP failure. The final passing run reported 3,481 ms to final assertions, including graph, SQL, login, API and two browser widths; separate stage latency is unknown.

Typecheck and targeted ESLint passed for the changed verifier. The production build, 969-test suite, full lint (0 errors, 11 pre-existing warnings), production dependency audit (0 vulnerabilities) and repeatable 001–060 migration passed in stage 121 without another application-code change. This is A09/A10/A12/A13 local synthetic production-HTTP evidence; A11 remains unverified. No model, search, SMTP or paid provider call was made, and the frozen Colombia evaluation was not changed.

Efficiency input was one synthetic target and one unavailable-provider observation; qualified output, saved companies and downstream-used companies were zero. One valid receipt was read through SQL, and the action result was used by two API reads and two browser viewports with one refresh each. User adoption is unknown. Workflow tokens, API credits, actual cash and workflow retries were zero; the failed browser launcher was one test-harness retry. The 5 micro-USD reservation is a synthetic occupied amount, not cash cost. The opportunity is to keep the partial-stop contract identical from graph to API to page without repeating unknown paid work.
