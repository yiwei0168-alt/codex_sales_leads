# Stage 229 — A25 progressive paid acceptance boundary

The user explicitly reduced the importance of exact cost and FX acceptance and selected a concrete replacement for the previous whole-run gate: retain the cumulative USD30 hard ceiling, allow progressive reservation against each known single-request upper bound, and pause at an unknown single-call cost, expired FX reference or projected limit breach. [A25](CONFIRMED_PRODUCT_RULES.md) records the exact confirmation and scope. Earlier whole-run language is preserved as historical approval context; no price, model route or provider was newly approved.

The existing product transport already quotes the **actual request** against a strict, current per-call contract before sending it. `reservePaidCall` then locks the user's budget row and atomically rejects a reservation above the remaining cumulative or task limit; a missing report retains its conservative occupation and blocks unsafe automatic replay. These are existing implementation properties, not new test evidence from a real run. The read-only minimum-acceptance preview now explicitly reports A25 progressive admission, keeps `totalRunBoundUsd=null` as a completion forecast, and lists required stages whose tariffs are currently held. It does not claim that the workflow can finish merely because its first call fits.

The current no-paid preview shows USD12.324404 occupied of USD30 and USD17.675596 remaining. Both required `intent-light` and `knowledge-embedding` are `expired-tariff` while the official FX reference is stale; no job was claimed or provider called. This first-step hold is consistent with the user's selected boundary. The full 1,048-test regression, TypeScript, script lint, production build and generated-workflow `--check` pass. The scheduled official reference retry is due no earlier than 12:50:36 UTC. A11 real natural-language, search, evidence, role, score, persistence and page loop is still unverified.

Efficiency: one user rule clarification and one read-only product preview yielded one valid progressive-admission status observation used for acceptance, zero customer candidates or qualified/downstream-used leads, and unknown actual adoption. Provider calls, paid tokens, API credits, USD, retries, mail and discarded business output zero. The local preview took about nine seconds, real provider latency unknown. Opportunity: use per-step quotes and durable checkpoints to avoid paying for work that cannot resume, while preserving a clear stop reason and remaining-company shortfall when a later step is unavailable.
