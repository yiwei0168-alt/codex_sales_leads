# 阶段151：P06 已完成事实阶段响应的追加式 SQL 检查点

迁移 `067_qualification_phase_checkpoint.sql` 新增独立完成响应表，以用户、工作区、任务、国家、公司、完整来源指纹、阶段序号及实际提供方请求契约为联合身份。应用角色只能读取和插入自己的记录，不能更新或删除；插入前及跨进程读取时均重新校验逐事实/来源完整覆盖、引用、模型版本和实际提供方身份。相同响应重复保存只留一行，同契约冲突响应被拒绝。备用提供方身份不同于主路由时不伪装为主路由可复用结果。

本地迁移001–067执行通过，067在 SQL 验收脚本中再次执行通过。两组隔离账号、工作区、会话与任务夹具验证：第一进程保存一条受控完整阶段响应，第二进程读取一致；重复写幂等、跨国家和跨用户读取为零、越权插入和修改被数据库拒绝，无效输出及错误路由在写前拒绝。夹具清理后该两账号的阶段记录为零，真实模型、搜索、SMTP、预算预留和现金支出均为零。全量1000项/205文件测试、类型检查、生产构建、改动文件ESLint通过。

此迁移只增加表，旧版本代码不依赖它。回滚前必须核对并保留已完成响应与对应付费请求；只在表为空或已有安全备份且确认不会要求从这些阶段恢复时，才可由迁移权限删除此表。当前未执行回滚。该检查点尚未接入评分 Agent；缓存缺项也**不**授权重放未知付费请求，运行时必须沿用现有 `paid_call_reservation` 请求指纹与评分恢复门禁。模型阶段输出、备用路由实际契约、逐阶段用量汇总及最终综合仍待端到端验收，P06与A11未完成。

效率口径：隔离阶段输入一份模拟完整响应、有效输出1、数据库完成行1、跨进程下游读取1，用户实际采用未知；重复保存无新增行，错误输出/错误路由被丢弃且不产生成本。真实token/API credits、现金、外部重试0，本地SQL脚本约3秒，供应商延迟与实际节省未知。优化机会是把此检查点接入现行Agent，在每次实际请求前读缓存、完成后立即保存，并对未核销费用保持暂停。
