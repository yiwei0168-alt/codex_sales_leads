# 阶段118：完整评分 Agent 的超大单公司边界

把 P06 的合成字段表验证推进到当前 `LeadQualificationAgent` 的实际请求组装路径：输入一家公司、61条当前证据及65条各自带证据引用的事实，其中60条证据/事实为独有内容。该 Agent 构造完整提示、评分规则、输出 Schema 和证据身份后，原请求超过61,440字节；`exact-field-table-v1` 使准备后的 DeepSeek 请求低于该上限。测试反解字段表，逐项比较证据和事实的完整值及原顺序，检查全部证据 ID、原候选不变，合成评分返回一条完成结果。

同一实际 Agent 路径对101条证据/105条事实的更大单家公司，在压缩后仍超出61,440字节，抛出 `LeadRequestTooLargeError` 且模型调用为零；候选原值保留，不产生低分、业务不合格或补量公司。这不是任意大小载荷的解决方案。后续若需要让此类公司继续进入评分，须在已确认 B07/B26 边界内设计可核对的分阶段处理，并单独验证其费用和恢复；不能删除独有事实或引用以求通过。

定向31项/1文件、全量967项/203文件、typecheck和全库lint 0错误/11既有警告通过；最近生产构建为阶段116。本阶段新增的是实际 Agent 合成回归与边界证据，未改产品评分实现、阈值、搜索路由或冻结实验。真实模型/搜索/邮件调用、token、API credits、现金与付费重试均0，真实业务有效输出及用户采用未知。
