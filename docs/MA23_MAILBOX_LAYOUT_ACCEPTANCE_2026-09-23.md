# MA23 邮箱工作台与固定页面布局验收（2026-09-23）

## 已验证

- `npm.cmd run db:migrate` 成功执行至 `105_multi_mailbox_access.sql`，历史邮箱未自动升级为“可发信”。
- `npm.cmd test`：1341 通过、1 跳过；`npm.cmd run build` 与全量 ESLint 通过（仅仓库既有警告）。
- Playwright 桌面 1366×900、手机 390×844 六项隔离浏览器用例通过：邮箱顶部管理与分页、固定页面高度、知识资料四分区切换、资料每页 12 条、侧栏三项图标/文字垂直中心误差不超过 2px。
- 服务端新连接先验证 IMAP；选择发信时验证 SMTP。发送服务、主 Agent 入队入口与开发信选择器均要求 `access_mode='send-enabled'` 且 SMTP 已验证。公网 DNS 校验拒绝任意私网解析结果；批量候选审核以逐项内容哈希和独立结果防止旧版本误批准。
- “全部资料”联合 SQL 在本地实际数据库执行成功；查询只读、未输出资料内容。范围包含当前用户私有知识、共享知识和公共证据库，不包含个人长期记忆。

## 尚未验收与边界

- 没有使用真实第三方邮箱、SMTP、Kimi 或上传真实资料做端到端验证；没有发送任何邮件。
- 市场概览的“全部国家”外层框已在代码中移除，尚未打开实际市场页面做视觉核对。
- “不允许无限向下滑动”已登记为全产品设计硬规则；本阶段实现并验证了邮箱与知识库资料页。其他既有页面尚未逐页审计，不应宣称全站符合。
- 旧邮箱迁移后默认只读，即使以前留有 SMTP 验证时间，也不能自动发信；用户需主动选择“可发信”并通过新的 SMTP 验证。没有 SMTP 配置的账号需重新连接。
