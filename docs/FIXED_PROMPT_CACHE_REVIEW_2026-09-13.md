# 固定提示、评分规则与缓存审核

最新实施索引（2026-09-13，阶段 2–6）：P02 已有逐尝试数值/路由归因和预算折叠汇总；P05 公共角色与用户/国家评分缓存已收紧完整请求依赖，评分逐批保存；P06 常规批次与最终发送均有完整 UTF-8 上限保护。不是全项完成：跨进程幂等、全部 provider/评分版本观测、批内部分结果与超大单家公司处理、账本和真实业务验收仍待推进。以最新验收报告及恢复记录为准，以下阶段 1 为历史状态。

状态：P01–P06 已于 2026-09-13 全部获用户确认，进入统一实施和验收；并非全部已实现。日期：2026-09-13。

实施阶段 1：P01 固定前缀已用于 DeepSeek 及兼容 fallback 的补证/评分请求；只改键顺序，不改值/数组/消息角色。P03 模型投影已由完整配置确定性生成，保留全部语义、全部角色/产品轨道，移除重复副本/纯管理项和确定性等级阈值；原配置与 checksum 审计保留。提示版本已更新。P04 完整输出 Schema 保持不变，不为优化强制重写。596 tests / 142 files 和类型检查通过；真实评分质量/缓存折价未验收。P02 逐尝试观测、P05 精确结果复用、P06 最终请求边界及账本验收仍待实施，不扩展新优化。

最新范围：用户要求继续剩余验收，同时完成 P03–P06，再以验收为主线。此安排取代临时暂停优化讨论的建议，但不等于批准具体方案。完成本组六项后不扩展新优化议题；验收缺口与通过依据见 `LOCAL_PRODUCTION_ACCEPTANCE_2026-09-12.md` 顶部表。

## 依据和限制

检查 `src/providers/deepseek.ts`、`src/lib/leads/workflow/qualification-agent.ts`、`evidence-correction-agent.ts`、`role-correction-cache.ts`、`persistence.ts`、`src/lib/leads/scoring-policy.ts` 及 `config/lead-scoring/policy-v2.0.0.json`。参照已完成的 207 家冻结请求离线计数，不新增模型或搜索评测；本轮网页访问只核查服务商文档，不增加候选证据。没有数据库读取/修改或产品逻辑变更。

已测单次固定片段：仅评分系统提示含 Schema 1,162 token、rubric 2,041、instructions 662；带路径分别 1,661、2,299、844。片段嵌套/分词边界不同，不能相加当完整输入量。未实测新方案，不能承诺降幅或无质量损失。

官方依据：[DeepSeek 上下文缓存](https://api-docs.deepseek.com/guides/kv_cache/)说明缓存基于已落盘公共前缀，可能需要多请求形成，命中是尽力而为；输出仍需推理，不保证缓存命中。页面直接打开超时，使用官方同页搜索索引核对；未以旧新闻的缓存块大小作为当前约束。[Anthropic 兼容说明](https://api-docs.deepseek.com/guides/anthropic_api/)将 `cache_control` 标为忽略，不应直接套用 Claude 手动缓存设置。端点实际返回的字段仍需独立验证，本轮未调用 API。

## 现有结构中的具体发现

1. Provider 发送稳定 system（含输出 Schema），user JSON 次序为 `task -> promptVersion -> evidenceIds -> input`。input 是 `instructions -> market -> cudyFitBrief -> candidates -> scoringRubric`。因此固定任务指令之前已遇动态证据 ID，完整固定 rubric 又位于动态候选之后；不是“完全没有缓存”，system 仍可构成固定前缀，但后面固定内容难以连续复用。
2. 明确重复字段：rubric 顶层 `policyKey`/`policyVersion` 与嵌套 policy 标识重复，`dimensions` 与 `policy.weights` 相同；instructions 又用自然语言重复主要权重。checksum 是不同的审计功能，不应与版本简单视为重复。是否删除某处表达须看它对模型的强调作用。
3. 完整 policy 内含模型不输出的账户等级枚举/80和75阈值、元数据及程序配置。模型同时被告知等级由程序计算且不能影响分数。这里有可投影空间，但“KA 不用于一级代理”“等级不影响评分”约束仍须保留。
4. 不是可任意删除的重复：instructions 中未知规模中性 8/15、采购影响封顶、按主角色比较、同主体目标国家门禁等关键说明不能仅靠 policy 的一般字段替代。policy 的角色 scorecards、产品轨道和 subweights 也不能只用总权重替代。
5. 潜在表述不一致需审阅：完整 policy 的 eligibility required 包含 viable-cooperation-path，但 score-only 指令不生成路径并改评采购影响。应明确“无需生成路径对象，但仍判断商业影响/采购适配”，不因精简把采购影响要求删掉。历史 freshnessDays 与用户一年提醒分属证据策略/界面提示，不能只因两个数不同就判重复或自动修改。
6. Provider 当前 usage 归一化只保留 input/output/reasoning/total，未保留 cache-hit/cache-miss；无法从现有离线报告判断实际命中或缓存节省。
7. 已有公共角色矫正结果缓存：公司域名＋国家＋公共证据指纹＋prompt版本＋角色体系版本，命中后重绑定当前证据 ID；不是没做缓存。当前依赖指纹未显式包含模型、queryRoles/missingEvidence 等请求输入，应先审阅这些输入如何影响结果与兼容性，再扩展复用，不直接降低失效条件。
8. 在本次检查的 qualification agent 中未看到相同依赖的跨任务评分结果命中短路；这不等于产品没有结果存储、任务检查点或其他缓存。保留历史评分展示/恢复与主动发起新评分是不同语义。

## 建议及实施顺序（P01–P05 已确认，P06 待确认）

| ID | 建议 | 预期收益与约束 | 进入实现前/后验证 |
|---|---|---|---|
| P01 | 固定前缀先行：在原有消息权限层级内将通用 instructions、完整 rubric 放在动态候选/证据 ID 前，固定序列化顺序。首轮不删语义、不压缩 Schema、不搬私有信息到共享区。 | 优先争取服务商缓存命中，通常不是减少传输 token；不能靠固定政策 ID 代替完整规则。不给缓存预热额外发付费调用，不降低已批准并发。 | 离线证明字段/值、证据顺序、消息角色和隐私域保留；随后另行授权的少量质量/usage 检查，排列变化仍可能影响模型输出。 |
| P02 | 缓存观测：逐尝试保留原协议缓存命中/未命中/创建量（若服务商提供）、token 总量、模型/端点和提示版本。 | 分开“本地结果复用免调用”与“服务商输入缓存折价”；未知字段仍未知，不补 0。冷启动/跨账号/网关切换不假定复用。 | 完整且同口径字段才能验证守恒；不把不同服务商 cache 创建/读取字段机械互换。预留仍按缓存未命中。 |
| P03 | 从版本化完整 policy 自动生成模型专用投影，去掉确无推理职责的元数据/确定性等级阈值，并单点表达精确重复字段。完整原规则仍入库审计。 | 有望降低输入 token，幅度待离线测量；不删除语义子分、未知/冲突规则、资格门禁、升级边界或关键强调。role scorecards 不能在 Hybrid/不确定主角色时裁成单一类别。 | 明确保留/删除映射，逐条人工确认，回归守恒；模型质量须另测，不能只做 JSON 深比较证明语义等价。 |
| P04 | 保留完整输出 Schema；先只审查重复说明、冗余示例与可复用定义。 | Schema 是成功解析/证据绑定保障；token 少但 invalid JSON/重试多可能更贵。当前 Schema 以 system 文本传入，不是服务商保证的原生结构输出。 | 必填字段、枚举、边界、证据引用与完整性测试不降级；不把定义缓存到本地后只给模型名称。 |
| P05 | 先完善角色矫正缓存的依赖审阅，再评估用户/工作区/国家隔离的精确评分结果复用。 | 命中可免整次模型调用，但评分受用户阶段、目标、产品轨道、记忆、证据、纠正角色、评分/提示/schema/实际模型版本影响，不能只按域名命中。 | 当前证据引用重绑定、国别/租户隔离、撤销/更新失效、强制重评绕过、并发去重与费用未知不重放。未批准模型兼容性时不得跨模型复用。 |
| P06 | 在上述收益实测后再讨论批次重组；继续只为需要路径的任务生成路径。 | 仅评分/带路径输入差 13.67% 是此前模式比较，不是可无条件削减的产品功能；不为缓存强制等批或减少目标数。 | 以完整请求字节/可靠 token 界限加输出完整性约束，单公司超限保护照常；观察弃用、重试、每条有效线索成本。 |

P01 已确认：保留原消息权限层级、全部规则内容、证据与数组顺序，仅调整字段排列。固定通用内容在前，用户/市场上下文与候选/证据在后；不把个人记忆移到共享区，不用规则 ID 代替规则，不付费预热、不降低并发。先离线验证内容保留，质量和实际缓存命中另行验证；不得预报节省比例。未修改模型、thinking、评分权重、证据、缓存策略、运行配置或预算门禁。

## P02 已确认细则（2026-09-13，待实施）

1. 以每次 provider HTTP 尝试为计费观察单位，关联任务、阶段、尝试/重试、实际服务商/网关/模型/端点和提示/评分规则版本；不只记录最后一次成功响应。
2. 保留服务商提供的输入/输出/推理、缓存读取/未命中/创建数及字段来源；不提供则 unknown，不自行补 0，不混同各协议创建/读取语义。提供商输入总数和缓存字段只有同口径且完整时才校验守恒。
3. 仅保存数值、状态、版本与必要的受控内部关联；不保存密钥、提示原文、邮件或证据正文。指标记录失败不重放成功业务调用。
4. 本地结果缓存命中记录免调用次数和下游采用；服务商前缀命中记录实际报告的缓存 token。前者不是服务商返回的 cache-hit，后者不意味着输入 token 减少。提供商费用和本地避免成本估算分开。
5. 输入缓存命中率 = 同口径缓存读取 token / 输入总 token，只在字段齐全且分母大于 0 时计算；同时展示数据覆盖率，避免只看有 usage 的成功调用。输出、失败/未知费用与重试纳入账本视图。
6. 根据该次 token 与已核验费率计算“缓存折价估算”；未经实际账单/完整费用核验不得称为已核销节省，更不能与本地避免调用估算相加冒充实付降低。预留仍按未命中，保留已有释放规则。
7. 界面复用任务中心/费用详情的可展开摘要；按任务/阶段/模型汇总命中、覆盖、延迟、重试、有效/下游采用，避免新增冗余页面。冷启动、模型/网关切换分别看，不跨隐私域承诺缓存复用。

后续顺序：讨论 P06，全部确认后统一修改产品代码；获准不等于已实现或已验证节省。

## P03 已确认细则（2026-09-13，待实施）

目标是从唯一完整评分配置生成“模型实际需要的版本”，不是另维护一套评分标准。原始配置、投影版本、内容哈希和每次评测采用版本继续保存在审计侧。P01 先按全字段保留验证重排；P03 若获准则单独作为显式投影变更验证，不混在 P01 中偷偷删内容。

| 内容 | 本轮建议 |
|---|---|
| 总权重、全部子权重、产品轨道及家族权重、各主角色评分标准 | 完整保留；首版不根据候选角色删掉其他角色标准，避免影响 Hybrid/角色纠正。 |
| 未知/冲突、证据引用、国家/实体门禁、规模中性 8/15、采购影响上限、最佳产品轨道、升级条件 | 完整保留，包括尚不能由 JSON 字段准确替代的自然语言说明。 |
| `dimensions` 与 `policy.weights`；内外重复的 policyKey/version | 数值/标识只保留一处权威表达；需引用时指向它，不重复整块。短自然语言权重强调首版保留，后续单独测其必要性。 |
| `accountTierPolicy` 等级完整枚举、80/75 阈值、可配置标志 | 不传模型，交由原确定性程序计算；保留“等级不影响评分”和“KA 不用于一级代理”语义约束，后续策略仍接收程序生成的实际等级。 |
| status、schemaVersion、配置 checksum 等只用于管理/审计的信息 | 留在应用审计/缓存依赖中；模型只携带一份可识别的策略 key/version。不得因此删除完整数据库快照或缓存失效依赖。 |
| 研究深度、证据新鲜度、资格、升级、私人记忆边界 | 首版不做激进裁剪；对职责归属/潜在冲突明确说明，缺证与过期不能自动判负。 |

执行约束：程序确定性生成，不多调用一个模型压缩，不按每家公司重新生成静态规则。保留/排除清单与内容测试可审阅；未知新增策略字段须提示未分类，不能自动丢弃。纯评分模式解释“不生成路径对象但保留采购影响评价”，带路径模式仍带五种路径、最多两条和相关规则。输出 Schema 暂不改，交 P04 单独讨论。

验证顺序：先静态映射/规则覆盖、完整证据绑定和离线 token 差异，再另行授权的小样本模型质量回归。不能用字节减少代替质量证明；不承诺节省比例、不改变模型/thinking/输出上限、不立即解锁付费。投影与提示版本变化须触发相关缓存重新评估，防止复用旧语义结果。

## P04 已确认细则（2026-09-13，待实施）

1. 本轮优先保留现有完整输出 Schema：字段、必填、类型、枚举、数值范围、数组限制与各模式差异不删减，不改字段名为难读缩写；路径仍最多两条，不重新添加已取消的路径 confidence。
2. 单一来源生成 Schema；模型提示与程序验证器不维护两份手写约束。按模式/版本本地复用序列化结果，但每次模型仍收到完整所需 Schema，不能只传 schema ID 或依赖模型记住上次请求。
3. 只审查 Schema 周边重复的格式说明/示例，不借格式精简删除未知/冲突语义、证据绑定、每公司一结果、量表与主角色差异。没有明确冗余时允许不改，以验收为先。
4. `$defs`/`$ref` 等结构去重不是本轮默认实施项；需要明确离线收益、引用闭合、程序兼容及模型理解验证后另行批准，不为节省少量输入增加返回失败风险。
5. 程序仍验证 JSON、Schema、候选 ID 完整性与唯一性、证据引用和模式边界；无效/截断不能当合格结果或自动给低分，不因 Schema 变化增加重试次数，沿用预算与未知费用处理。
6. 验收看成功结构化输出率、完整候选覆盖、无效/截断/重试及有效结果成本；只做本地校验不能证明线上模型遵循率，真实模型验证另受已确认费用上界与累计预算约束。

P04 确认意味着保留格式保障并完成必要的冗余审查，不强制为了优化而重写 Schema；P05/P06 仍须确认后统一实施。

## P05 已确认细则（2026-09-13，待实施）

1. 先复用现有公共角色矫正缓存，审计缺失依赖；公共公司身份/证据可以共享，用户相关评分/路径结果限定用户、工作区、国家，不能仅按公司域名跨用户复用。
2. 评分缓存只复用相同有效输入/语义版本下成功且通过完整校验的结果。依赖包含公司身份、目标国家、任务目标和用户阶段/产品轨道、纠正角色与发现、证据内容/来源/有效状态、实际使用的私有记忆、提示/评分/投影/Schema/模型/端点/生成参数版本及会影响结果的业务上下文。不直接把“没搜到新信息”当输入相同。
3. 先采取保守的完整请求依赖策略，包含候选批次成员/顺序；未经证明批次不影响语义前，不把批处理输出抽出当作任意新批次的可复用单公司评分。可后续另评估更细粒度复用，不为命中率牺牲正确性。
4. 原证据 ID 换成新 run ID 时，必须核验同内容、同来源并完整重绑定；任何被引用证据缺失、撤销、冲突、来源或有效状态改变，不能视为精确命中。历史依赖字段缺失不猜测兼容，保留旧结果供查阅并标记，不自动当新评分。
5. 用户修改主角色/路径/等级的覆盖状态优先展示，历史缓存不能覆盖；影响模型输入的用户阶段/记忆/路径变化使相应结果失效。等级若确为纯下游程序字段，不无故触发模型重新评分，按依赖范围处理。过一年仅提示不自动 invalid/付费刷新，仍遵循已确认新鲜度规则。
6. 用户可显式要求重新核实/重新评分，按请求范围绕过对应结果缓存，但不自动删除可复用证据或发起额外搜索；每次新付费调用仍须费率、预算与授权有效。普通页面打开/历史查看不触发模型。
7. 相同任务/请求并发通过 owner 检查和持久幂等/进行中记录避免双重调用；成功结果在重启后可复用。业务结果成功与账单是否已知分别记录：有已校验完整结果但费用未知，仍可复用结果并保留预留；超时/执行中且没有完整结果不能当缓存成功，不立即自动重试，保留占用和恢复/核对状态。
8. 记录缓存命中/未命中原因、复用来源版本、避免调用次数、耗时和下游采用，估算避免成本与真实账单分开。测试必须覆盖租户/国家隔离、输入/版本变化、ID 重绑定、批次变化、人工覆盖、并发和失败恢复。模型能力/路由变化默认不跨模型复用，除非另获兼容性批准。

P05 目的为减少重复工作；已有矫正缓存不重做新系统，新增评分缓存不得改变评分标准或用户操作含义。具体实现以安全依赖审阅与测试为门禁，P06 确认后再统一实施。

## P06 已确认细则（2026-09-13，进入实施）

1. 先修正确的批次安全边界，再评估扩大批次。初版保留现有每批最多 5 家限制；增加完整请求 UTF-8 字节检查：补证 36 KiB、仅评分 56 KiB、评分＋路径 60 KiB。按 P01/P03 处理后的最终请求测量，不只计算 input 或套用旧字节统计。现有更小的已配置批次数上限仍生效，不自动扩大。
2. 输入字节限制、可核验的 token/计费上界与输出完整性是不同约束，都须满足。默认输出上限仍 8,192，不自动升到 16,384；较复杂请求必要时减少同批公司数。输出预算风险估算不能冒充确定模型一定完整返回的保证。
3. 多公司超限拆批，单公司超限沿已确认保留关键信息的压缩/分阶段处理；仍无法满足时保存进度和明确原因，不静默删证据、漏公司、低分或当无效候选。额外付费处理仍受权限/预算约束；不自动多搜索补证。
4. 任务模式由任务要求决定：工具线索价值测评用 score-only；需要合作路径的产品任务继续生成最多两条路径。不为减少输入把用户需要的路径删掉，也不在纯评分任务附加策略/邮件。每种模式各自使用完整 Schema。
5. 按现有队列保留候选及证据顺序，不为凑满/缓存跨用户、工作区、国家混批，不额外等待固定满批、不降低已批准并发。批次组成变化按 P05 依赖策略处理，已有成功结果/检查点不重放。
6. 可明确识别的部分成功结果通过完整性检查后保存；仅缺失/失败部分按已有允许的恢复机制处理，完整批次缓存只收完整结果。未知费用继续占用，不增加自动重试次数，不为凑齐结果反复重跑成功公司。
7. 验收不仅看输入变短，还检查全部候选有明确结果/待处理状态、引用有效、无重复请求、无跨域混批、截断/失败/重试及每条有效结果成本。先离线/单元/集成检查，真实模型/业务验证须在可信费用上界和累计预算下进行。没有实测收益不扩大批次或宣称成本下降。

P06 确认后，P01–P06 统一进入实现并分阶段验证/提交，完成已确认预算账本与未通过验收门禁；不继续扩展优化议题。批准实施不等于豁免模型计费门禁或已有逐项外部动作授权。
