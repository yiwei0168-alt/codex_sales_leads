# Stage 225 — A11 Gemini 2.5 Interactions bound audit (read-only)

The current product's `gemini-full` route still sends `gemini-3.6-flash` to Google's Interactions API with `google_search`, `thinking_level: low` and `max_output_tokens: 12,000` (`src/providers/discovery.ts`). No route or tariff changed. The read-only minimal-run preview again returned `totalRunBoundUsd=null`, `checkedTariffsAvailable=false`, zero provider calls and zero claimed jobs; USD12.324404 of the cumulative USD30 remains occupied, with USD17.675596 available before any new conservative reservation.

The [official Interactions schema](https://ai.google.dev/api/interactions-api) lists `gemini-2.5-flash`, GoogleSearch and `max_output_tokens`, and the [official thinking guide](https://ai.google.dev/gemini-api/docs/thinking) lists `low` for that model. [Google Search grounding](https://ai.google.dev/gemini-api/docs/google-search) bills 2.5 and older models per grounded prompt rather than per model-generated query, and the [2.5 Flash standard price table](https://ai.google.dev/gemini-api/docs/pricing) lists USD0.30/M text input, USD2.50/M output including thinking and USD35/1,000 grounded prompts beyond the free allowance. The free allowance is not used in conservative estimates.

This does **not** establish a full per-request fee ceiling. The [Interactions Usage schema](https://ai.google.dev/api/interactions-api) separately reports `total_tool_use_tokens` as tokens in tool-use prompts and `total_tokens` as the interaction total. The currently published GoogleSearch fields list search types, but no maximum model-internal search/tool-step count. Absence from the public schema does not prove no private limit; it means this audit cannot prove a strict maximum of all billable tokens from the present request. A 1,048,576-token model input limit and the 12,000 output setting should not be multiplied by rates as though they bounded every repeated internal tool prompt without provider evidence. A Gemini 2.5 route also needs a separately confirmed A11 model/quality change and exact live contract review. Thus it remains research only, not an active billing contract or paid admission.

The other A11 blockers also remain: current-account SearchAPI pricing/credits, expired usable CNY FX for Kimi and Beijing embedding, default fallback and conditional phase-count ceilings, six unverified historical bills and a whole-run bound. The current synthetic four-phase score plus market-plan conservative example alone is USD17.705685 without Pro, USD0.030089 above the remaining USD17.675596 before search, evidence or other mandatory stages. This is an example, not a complete run quote. No paid lead run was started.

Efficiency: one read-only product preview and four official documentation topics yielded one valid blocker assessment used for A11 readiness; zero candidate companies, qualified outputs or downstream customer use, with actual adoption unknown. No model/search API credits, paid tokens, cash, external retries, mail or discarded business output. The local preview took about seven seconds; actual provider latency unknown. Opportunity: obtain a provider-enforced total tool-use/token cap or a bounded non-agentic search route before proposing a strict Gemini contract; keep the existing hold until the full route and budget quote are provable.
