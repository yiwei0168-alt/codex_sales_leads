# Stage 212: confirmed Tavily basic Extract contract

User confirmed the exact candidate in [the proposal](TAVILY_EXTRACT_TARIFF_PROPOSAL_2026-09-14.md): existing basic `POST /extract`, 1–20 HTTPS URLs, text, no images or query reranking, USD0.032000 maximum reservation per call, expiry `2026-09-21T00:00:00Z`. The active policy is now `request-bounds-v1.8.0`. The prior v1.7.0 file remains as history. No other route, price or model changed. The strict body/method/path/byte validator already existed and is now applied to the active rule. The same canonical paid-request fingerprint guard used for synchronous searches now covers Extract.

Current official [Tavily pricing](https://docs.tavily.com/documentation/api-credits) confirms USD0.008 per credit and basic Extract one credit per five successful URLs; [the endpoint reference](https://docs.tavily.com/documentation/api-reference/endpoint/extract) documents the 20-URL limit. All 20 successful URLs cost at most four credits, hence USD0.032000 at the published pay-as-you-go rate. Account-specific enterprise pricing and actual invoice remain unverified; this rule uses the confirmed published-price scope and expires on the approved date.

The isolated PostgreSQL fixture allowed one synthetic 20-URL request and reserved 32,000 micro-USD. With no uniquely matched trusted bill, the reservation stayed occupied and the bill stayed unknown. A duplicate same-operation request was blocked before transport, a new request beyond remaining fixture budget was blocked, and a 21-URL request failed its contract before reservation. Exactly one in-memory transport was called, no provider. Fixture rows were removed. Full **1,034 tests in 208 files**, TypeScript, focused ESLint and production build passed. The read-only A11 preview now shows the Extract tariff present; real occupied USD12.324404/30 and `totalRunBoundUsd:null` remain unchanged. No actual Tavily credit, model token, cash or customer task was used.

Efficiency: one synthetic valid 20-URL input, one complete mock HTTP response, one reserved/settled-unknown fixture row; three attempted repeats or invalid requests produced zero additional provider work, for duplicate/budget/URL-limit reasons. Product evidence output, downstream use and user adoption remain unknown. Paid API credits/cash/model tokens/external retries zero; isolated SQL verifier about two seconds, actual provider latency unknown. Opportunity: reuse exact Extract outcomes and retain unknown fees rather than replaying them.
