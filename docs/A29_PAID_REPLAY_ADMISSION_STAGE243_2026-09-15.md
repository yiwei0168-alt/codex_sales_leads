# Stage 243 — A29 paid replay admission and Terra v2 compatibility

The user explicitly allowed all paid calls for the current product stage and, after the duplicate-charge risk was explained, answered “接受”. A29 is implemented as a disabled-by-default process override requiring the exact rule ID and exact owner ID. For that owner it admits cumulative/task budget overage and replay of unknown-cost requests with no reusable output. It does not release prior occupancy, erase or rewrite bills, bypass a missing/held tariff or frozen account, or permit concurrent/bound-exceeded/successful duplicate work. Every admitted reservation records `{ruleId:"A29",allowBudgetOverage:true,allowUnknownReplay:true}`.

The first resumed original action reused its ten existing graph snapshots and reached secondary review. The provider adapter's existing two-attempt policy emitted two same-fingerprint Terra requests under `request-bounds-v1.10.0`; both returned no valid output, no token or provider-cost report, and remain fee-unknown. Their latencies were 1,271 ms and 375 ms, each conservatively occupying USD11.019202. Account occupancy therefore rose from USD39.870306 to USD61.908710 against the unchanged historical USD50 display limit; this overage is authorized admission, not known cash spending. No company, completed review, national result or customer adoption was produced.

Current authenticated OpenRouter endpoint metadata returned HTTP 200 and listed seven active Terra endpoints. None listed `temperature`; the OpenAI endpoints listed `max_tokens`, while Azure listed `max_completion_tokens`. With `require_parameters=true`, the old request's unconditional `temperature:0` excluded every endpoint. The compatibility repair creates `request-bounds-v1.11.0` as a delta over immutable v1.10.0 and changes only Terra secondary review to `openrouter-terra-review-json-v2`: omit `temperature` and use `max_tokens=8192`. Model, medium reasoning, strict JSON schema, data-collection denial, request-size/output limits and USD11.019202 conservative ceiling remain unchanged. Sol judge and all earlier contracts are unchanged.

Verification passed 69 focused assertions across the request contract, paid fetch, review Agent, owner override and repository; TypeScript and changed-file lint passed. A real PostgreSQL synthetic contract run verified v1.11.0 routing, exact reservations, invalid/unrelated rejection, default duplicate blocking and fixture cleanup with zero provider calls. The broader A29 implementation checkpoint also passed 1,062 tests in 210 files, production build and generated-document consistency before the compatibility delta; a final full regression remains due after the delta.

The v2 real retry was not sent: execution approval stopped before transport because the saved task contains workspace evidence that would be transmitted to OpenRouter, and the user has not yet explicitly authorized that specific data and destination. This is not a provider failure or budget denial. The next safe step is an explicit user decision permitting this task's workspace evidence to be sent to OpenRouter for the Terra review; only then may the same checkpoint resume. No further replay should occur before that decision.

Efficiency record: one resumed task consumed two provider attempts, both invalid/unknown and neither used downstream, so valid-output and utilization efficiency are 0/2. Reported tokens, API credits and cash are unknown rather than zero; conservative incremental occupancy is USD22.038404. Provider latency summed to 1,646 ms; one transport retry occurred. Ten graph snapshots, the prior candidate evidence, correction and primary score were reused, so those stages made zero new calls. Discard reason is endpoint-parameter incompatibility. The concrete optimization is the Terra-only v2 serializer plus exact checkpoint reuse; stage authorization should be removed after acceptance.
