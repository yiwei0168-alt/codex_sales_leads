# P06 分阶段评分 Agent 与 SQL 恢复接线（2026-09-14）

阶段154将超限且角色可评分的公司接入现有评分图。普通批量路径保持不变；超限公司在任务的用户、工作区、动作和国家范围内逐阶段读取已完成响应，缺失阶段才尝试同一请求合同的模型调用，并在下一次付费调用前验证并追加保存完整结果。复用响应不重复累计本次模型用量；费用层的持久预留和精确请求防重放门禁仍控制未知结果。备用提供方、缺项、检查点写入失败或费用阻止会暂停后续请求。

全部阶段完成后，Agent 运行阶段153的最终综合和实际请求字节预检，再发一次完整评分请求。输出仍走既有评分Schema、纠正证据核对与确定性计算；成功的主路由评分合同进入现有公司评估缓存。图在恢复门禁前读取分阶段合同对应的已完成评估；缺失最终评分结果仍受原付费响应门禁阻止。若阶段摘要过长、最终输出缺项、备用路由或需要尚未实现的 Pro 阶段升级，公司保持`retry-required`，不记作业务不合格。

无网络验收：本地 PostgreSQL模拟151条事实/来源拆为4个阶段，第一进程保存第1阶段后中断，第二进程仅完成其余3阶段和一次默认“评分及合作路径”最终请求，第三进程读取同任务合同且不调用模型；夹具清理完毕。单元夹具还验证了预算中断、备用路由、阶段缺项和检查点写失败后不发送下一阶段。评分图的缓存先于恢复阻止通过内存图测试；评分单独模式与合作路径模式的150事实最终请求均通过各自实际字节上限。SQL阶段夹具0真实提供方调用、0新增实际占用；这尚不证明真实模型摘要语义、真实阶段费用上界、备用路由恢复或完整生产图端到端。1005项/206文件全量回归、生产构建、类型检查和改动文件ESLint通过。
