# 阶段137：超界评分公司与同批可评分公司隔离

完整评分请求的批次预检此前只要遇到一家公司在无损准备后仍超61,440字节，就在批次调度前抛错；同批其他公司也无法评分。当前 Agent 先按实际主路由及获准备用路由的请求字节契约逐家公司预检，再对可请求公司按原最多5家的批次规则建批。超界公司保留原证据与校正事实，生成 `retry-required` / `research-required` 的技术未完成记录，不发送评分请求，也不计为业务不合格。缓存契约只为实际可请求批次生成；已完成公司的精确请求契约不受超界同伴的位置影响。

合成回归覆盖超界公司在有效公司前、后两种次序：每种输入2家，可评分1、模拟有效评分1、完成回调/缓存契约下游使用1、超界待恢复1、模拟提供方调用1；原候选序列化前后相同。单独超界场景输入1家、模型调用0、技术未完成1。该结果仍使图在处理未完成检查点暂停，不会把已评分同伴直接发布为最终合格结果；后续恢复由原检查点与既有费用防重放规则约束。更大的独有事实仍需分阶段方案，真实模型质量与自然语言业务闭环未验收。

本阶段真实模型/搜索/SMTP调用、token/API credits、现金、付费重试均0；实际用户采用、供应商延迟与节省金额未知。合成有效输出利用率为2/2个可请求实例，超界3个测试实例均未外发；不能据此推断市场合格率。优化机会是对未完成大公司建立保留全部事实与引用的分阶段评分，并维持现有缓存、预算和未知费用门禁。

验证：定向 Agent 与图测试62项；全量984项/203文件、`typecheck`、Next.js生产构建、全库lint 0错误/11条既有警告、生产依赖审计0漏洞通过。
