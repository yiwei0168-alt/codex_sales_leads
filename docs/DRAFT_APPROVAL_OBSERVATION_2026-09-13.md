# 草稿批准观测验收（阶段70）

`updateDevelopmentDraft` 在草稿原行锁内区分正文变化、新批准和重复请求。完全相同请求不写新版本、时间或事件；保存不同正文但不批准回到generated并清除approved_at；明确批准新版本追加 `outreach-draft.approved` 审计。页面编辑已批准正文后重新开放批准按钮。

事件边界为 `explicit-draft-approval-not-sending`，actor=user，包含国家、版本、正文SHA256及聚合输入/有效/下游使用量、零模型token/API费用、耗时、重试/丢弃及优化机会。事件不含正文。approvedDrafts按批准版本计数，不能将多版本求和当唯一公司数；mailSent=null，未推定实际发送或成交。旧批准缺事件保持未知，不回填历史。

生产Chrome两视口48组通过：各视口实际保存草稿→页面批准→API→SQL，版本2事件仅1条；两个并发相同PATCH返回200且版本不增；页面编辑再批准产生版本3事件，随后仅保存正文回到generated。共2合成草稿、4实际页面批准事件，刷新/重复请求不新增批准观测。原草稿复用、国家隔离、任务状态、成本等回归通过；836测试/183文件、最终build含类型检查、局部lint通过。

测试0真实模型/API/token/费用/发信，原UI合成预留1笔保持。首轮服务停止失败，旧进程仍占端口，页面未进入工作台；该次fixture已清理。核验确切PID后停止本任务旧服务，启动最新生产build，最终48组通过且fixture全部清理。当前3017服务session96038。

无schema迁移；使用现有租户审计表和原草稿事务。回退代码不删除新事件，但将失去重复批准保护。其他工作流的采用追踪、完整真实业务、旧终结缺项恢复仍未完成；不宣称A08整项或整体验收通过。
