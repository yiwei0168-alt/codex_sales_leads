# Workflow telemetry and workspace audit ACL — stage 186 (2026-09-14)

The application writes `workflow_stage_metric`, `workflow_model_usage`, `workflow_artifact_event` and `workspace_audit_event` by INSERT. Full migration replay reissued the broad CRUD grant in migration 014 and, for the first three, migration 031 also explicitly granted CRUD. A read-only effective-role audit found UPDATE and DELETE true on all four tables. A tenant could alter its own workflow counts, unknown-adoption observations or workspace action history after they had been recorded. Tenant RLS did not make history immutable.

[Migration 074](../db/migrations/074_workflow_telemetry_audit_append_only_acl.sql) follows the broad grants and narrows just these four application contracts to SELECT/INSERT. Existing rows, RLS policies, delivery idempotency and public API shapes remain unchanged. `workflow_optimization_opportunity` stays mutable because product persistence updates its status and observation on conflict. The migration is idempotent on complete replay; the documented compatibility rollback would reopen history mutation and does not rewrite rows.

Local verification: before the fix, the application-role audit rejected exactly four additional tables with UPDATE/DELETE true. The full 001–074 sequence ran twice. After each replay, the 22/22 effective-role ACL audit, workspace audit tenant verifier, artifact observation idempotency verifier and workflow usage summary SQL verifier passed. Workspace audit and artifact event owner UPDATE/DELETE attempts are now denied; normal insert, cross-user isolation, seven artifact-event idempotent writes and aggregate queries still pass. A separate real product SQL persistence verifier passed two-country result/telemetry writes, 14 artifact events, concurrent replay idempotency, user-edit preservation and a zero-qualified run using synthetic input. All synthetic fixtures were removed or rolled back. Full 1,021 tests / 207 files, typecheck and changed-script lint passed. No live model, search, paid API, email or existing user task was touched. This is local database integrity evidence, not real user adoption or A11 business completion.

Efficiency record: inputs were two full replay runs and four isolated fixture types. Valid outputs were two 22-table audits and seven business verifiers, all used for A08/A13 integrity acceptance; customer output and actual adoption remain unknown. Four old ACL rows were rejected for excess privileges; no business lead was discarded. Model tokens, paid API credits, cash and external retries were zero. The persistence fixture reserved 28 synthetic micro-USD and removed it on cleanup; the live USD30 occupancy did not change. Local SQL verifiers took about 0.5–2.2 seconds each; real provider latency is unknown. Repeat the effective-role audit after complete migrations to avoid silently changing past adoption and cost observations.
