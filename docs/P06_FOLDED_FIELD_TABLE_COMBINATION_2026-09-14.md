# 阶段148：P06 折叠原文与无损字段表组合

现行单公司评分请求在原有无损字典、字段表及共享短语尝试后，可能仍超过 61,440 字节。阶段146的受限原文折叠原先只与这些无损形式比较最短值。本阶段允许折叠后的请求继续用字段表压缩重复字段名，并在需要时尝试共享短语；仅实际序列化字节变小时采用。字段表中的每条证据都有明确 `excerptFolded` 布尔值。准备元数据同时记录折叠、字段表及可选短语编码、完整和最终字节数及折叠条数；原任务候选、全部校正事实、来源身份和引用不变。冲突、否定、未知来源原文仍受保护，最终完整请求继续按实际提供方字节上限检查。

定向评分夹具中的 110 条高熵补充证据/事实（含一条冲突）在现行 DeepSeek 序列化合同下完成一次模拟请求。发出请求的字段表可反解全部校正事实、证据 ID/URL，冲突来源原文完整，折叠项不引用冲突事实，字节数不超过 61,440。125 条的试探仍为技术待恢复，既有 150 条独有事实边界仍在外发前暂停；这证明范围扩大，不证明任意大单项已分阶段完成。

实际 PostgreSQL 图检查点的独立进程恢复和 `persistLeadWorkflowResult` 路径也使用 110 条补充事实夹具通过：评分前不重跑发现/补证/校正，第二进程一次模拟模型响应，SQL 产品评估保留 111 条原始证据和 111 条事实，证据快照 111 条，评分遥测与模型用量各一条；合格/交付均为零，第二账号不可读，夹具清理后无残留和真实付费预留。55 条折叠及 101证据/105事实共享短语旧路径的跨进程与 SQL 模式均复验通过。

验证：987 项/203 文件全量测试、TypeScript 类型检查、Next 生产构建、改动文件 ESLint、六种跨进程/SQL 模式及差异检查通过。没有真实模型、搜索、SMTP、token/API credits、现金支出或付费重试。模拟模型结构遵从不能证明真实质量；当前整次费用预检仍为 `totalRunBoundUsd=null`，A11真实业务闭环未启动。

效率口径：新增组合夹具输入1家公司、111条证据/111条事实；有效模拟评分1、下游产品评估保存1、最终合格和用户实际采用分别为0与未知。真实供应商延迟/节省未知，本地跨进程脚本约9–12秒。原始事实/引用丢弃0，按规定折叠的请求原文条数见准备遥测；125/150条继续技术待恢复，0外部重试。下一优化是可检查点化的分阶段事实处理与最终综合，不能用本夹具的容量增量推断市场填充率。
