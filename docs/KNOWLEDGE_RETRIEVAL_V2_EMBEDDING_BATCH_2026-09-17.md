# KQ02 v2 十项向量批次证据

日期：2026-09-17。

用户明确确认：“我明确允许将这10个知识库切片正文发送到阿里云百炼生成向量，并接受可能产生的费用”。确切范围仅为本次十个切片、阿里云百炼 `text-embedding-v4`、1536维向量及其可能费用；不自动扩展为剩余语料或其他供应商/模型。

沙箱外无凭据连通性探测先取得预期401，随后 job `2124afeb-c94d-442b-817a-823ebf5da346` 成功处理10个输入，返回并写入10个1536维向量。供应商报告1,567输入tokens和847ms调用延迟；embedding请求1次、重试0、现金费用未知。没有回答模型、搜索、SMTP、generation activation或弃用输出。

只读复核显示 generation `3917a242-724e-4e36-a1da-04c496a9df2d` 仍为validated/inactive。10,871个chunks中10个已有向量、10,861个仍缺失，估算还需1,087次同步请求。该授权不覆盖剩余语料，全量构建与激活仍未发生。
