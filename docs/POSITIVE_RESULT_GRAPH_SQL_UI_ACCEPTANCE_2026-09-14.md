# 阶段138：正结果图任务经产品 SQL 与本地生产页面

隔离合成账号使用现有 `confirmAndQueueLeadWorkflow`、任务认领和完整销售线索图，从预置的一家哥伦比亚 Distributor 候选依次走知识读取、市场计划、发现、补证、主角色校正、评分、复核、交接与实际 `persistLeadWorkflowResult`。各模型和搜索阶段由确定性夹具代替，没有联网推理或搜索。结果为目标1、最终合格并保存1、`target-met`；各前置阶段只执行1次，交接1、评估SQL 1、国家公司SQL 1、任务完成回执1。重复完成操作未增加回执。公司库使用规范化市场候选ID，按国家与域名核对，不能把原搜索候选ID当成同一个持久化字段。

本地生产服务上，隔离账号通过实际登录与任务HTTP读取上述结果；Chrome桌面1366×900和手机390×844均显示“目标已满足”，刷新后仍一致且无横向溢出。第二账号读不到该公司。夹具预留7 micro-USD按共享费用规则全部归入该公司，未分配0；它不是服务商报告或真实现金支出。测试后账号、任务、公司、预算和检查点清理。实际用户采用未知。

此验收从**预置计划和确定性阶段输出**开始，未运行自然语言意图模型、多轮确认、真实市场搜索、补证、校正、评分模型、真实账单或服务商回执。它证明正结果的图→SQL→HTTP/UI接线与费用分摊，不证明真实合格率、填满数量或A11完整付费闭环。当前整次预检和累计USD30门禁仍须独立通过。

遥测范围：合成输入候选1、评分有效输出1、下游交接/评估/国家公司各1，模拟阶段调用各1、模型/搜索/SMTP真实调用0、真实token/API credits及现金0、付费重试0、业务输出丢弃0。内部有效评分到保存利用率1/1，实际用户采用与供应商延迟未知；脚本单次本地执行约4–8秒。优化机会是继续用隔离夹具覆盖故障与恢复分支，真实供应商费用必须按请求和可信报告核销，不能用此7 micro-USD 推断真实线索成本。

验证：`node scripts/run-tsx.cjs scripts/verify-positive-result-graph-sql.ts` 的 SQL 模式和本地生产 Chrome 双视口模式均通过；全量984项/203文件、类型检查、生产构建、全库lint 0错误/11既有警告、生产依赖审计0漏洞。清理后只读重核原验收账号仍占用USD12.324404/30、余USD17.675596，6笔历史未知账单保留，新增真实供应商调用0。运行时无密钥、客户数据或外部调用写入GitHub。
