# Stage 218: oversized singleton Agent recovery across processes and product SQL

The current qualification Agent was exercised with the real append-only product phase/final checkpoint adapters and isolated PostgreSQL fixture rows. A synthetic long critical finding generated bounded chunks. The first process saved chunk 0, then a synthetic budget denial stopped before chunk 1. A second process resumed the same action, loaded chunk 0, executed only missing synthetic chunk and phase/final model responses, and completed scoring. A third invocation made zero model calls and zero new usage records. The verifier confirmed the exact completed cache contract, append-only rows, earlier phase/final replay checks and fixture cleanup. It replayed migrations 067–069 before the fixture. No external provider, paid credit, customer job or mail was touched.

Command: `node scripts/run-tsx.cjs scripts/verify-qualification-phase-checkpoint-sql.ts`. Result: `phaseCheckpointSql=passed`, `chunkAgentCrossProcessResume=1`, `chunkAgentFullReplayCalls=0`, `paidProviderCalls=0`, fixture removed. TypeScript and focused script ESLint passed; stage 217's unchanged product code had passed 1,043 tests and production build.

This closes the prior gap between separately tested Agent logic and product SQL recovery for one synthetic long critical finding. It does not prove actual model semantic output, provider bill reconciliation, all fallback routes, the 151-source graph, or the natural-language A11 closed loop. The cumulative USD30 and whole-run preflight gates remain unchanged.

Efficiency: one synthetic long-company input produced one retained completed chunk before interruption, then the remaining chunk, valid phase and final outputs in the next process; all were consumed by local checks. The third run reused all completed work. Real customer output/adoption and model latency are unknown. Paid tokens, credits, cash, external retries and discarded business outputs were zero; the isolated SQL check took about 13 seconds. Opportunity: use exact chunk/phase/final identities to avoid re-purchasing completed work after a process boundary.
