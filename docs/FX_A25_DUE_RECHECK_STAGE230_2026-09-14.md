# Stage 230 — scheduled FX retry under A25 progressive admission

At the stored due time, one credential-free official ECB reference fetch returned a well-formed but still stale 2026-09-11 date. The refresh recorded `staleOfficialReference`, retained the previous snapshot, and scheduled the next attempt for **2026-09-14 13:50:54 UTC**. The CNY reference remains `expired-or-invalid`; the Kimi and Beijing embedding price evidence itself is valid through Sep 20, but cannot establish a current USD reservation while FX is expired.

The immediately following read-only A25 preview shows the unchanged USD30 ceiling, USD12.324404 occupied and USD17.675596 remaining. Required `intent-light` and `knowledge-embedding` each return `expired-tariff`. Its `totalRunBoundUsd=null` is now a completion forecast, not a separate prerequisite to start; the **current actual first paid steps** still pause under A25's explicit expired-FX condition. No task was claimed and no paid provider request ran. The six earlier unknown bills remain unreconciled.

The sole current acceptance matrix's A02/A05/A11 rows were updated to distinguish this actual FX hold from the superseded whole-run-before-start rule. Historical stage reports and frozen Colombia evaluation are unchanged. This is an observation and documentation correction, not a new FX tariff, model route, paid authorization, business completion or acceptance pass.

Efficiency: one due free ECB HTTP input produced zero usable fresh rates and one stale-source discard; one read-only product preview produced one valid gate-status observation used for acceptance. No customer candidates, qualified/downstream-used leads or actual adoption were generated. Paid tokens, API credits, USD, provider calls, retries, mail and discarded business output zero; public refresh about three seconds and preview about eight seconds, real provider latency unknown. Opportunity: wait for the persisted next retry instead of repeated official fetches, then reassess the first actual paid step under A25.
