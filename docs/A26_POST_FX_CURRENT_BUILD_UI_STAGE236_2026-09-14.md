# 阶段 236：A26 汇率边界修复后的当前构建 UI 回归

在阶段234修正普通产品七天汇率期限、阶段235对齐当前验收表之后，以该生产构建在本机 `next start -p 3030` 运行 `scripts/verify-authenticated-ui.ts`。脚本建立随机隔离用户、工作区与合成公司，通过 Chrome 1366 和 390 宽度各 34 项，共 **68/68**。覆盖真实登录及停用拒绝、国家切换、任务数量与停止/暂停状态、预算/成本/动态汇率及公开费率、用量刷新幂等、个人角色/等级/路径和记忆、手动渠道图、策略草稿修订复用。此处验证当前页面读取和交互，不等于新公司真实生成或用户实际采用。

脚本输出 `paidCalls=0`、`realMailSent=0`、`syntheticReservations=1`；该预留仅在临时测试账号内，脚本报告清理临时用户/工作区/公司。3030监听进程已停止。独立只读预算复核为验收账号 USD23.520920/50、剩余 USD26.479080、`unknownBills=10`、`newProviderCalls=0`，与运行前一致。阶段234的 1,052 项测试、类型检查、lint、生产构建和阶段231依赖审计仍是相应证据；本阶段没有产品代码、费率、路由或汇率抓取变更。

效率口径：68 项UI输入和68项有效本地断言，68项用于本地验收；真实候选、合格线索、客户下游采用均未增加。外部模型 token、API credits、USD、真实重试及邮件为零；浏览器验证约一分钟，真实供应商延迟未知。优化机会是用这套隔离夹具在费用/FX展示逻辑改变后立即回归，再将真实供应商调用聚焦于待确认的 S02 最小请求。

阶段8仍未完成：S01现行请求已实际收到参数不兼容HTTP404，S02待A11单独确认；即使页面通过，搜索、补证、角色评分和国家结果入库的真实闭环仍无通过证据。
