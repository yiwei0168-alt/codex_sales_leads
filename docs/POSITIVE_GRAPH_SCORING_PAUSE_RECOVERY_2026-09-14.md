# 阶段139：正结果图任务评分前暂停与恢复

后续清理校正：阶段140核对并清除初版脚本遗留的6条无引用全局合成公司，更新后的暂停恢复再次通过且清理后残留0。[清理证据](POSITIVE_GRAPH_FIXTURE_CLEANUP_2026-09-14.md)。

在阶段138同一隔离合成正结果夹具中，新增 `--pause-resume` 模式：真实图完成知识、计划、发现、补证、主角色校正与路由后，在评分节点入口注入 `WorkflowPausedError`。PostgreSQL检查点的下一节点为 `score_candidates`，已校正公司和原证据仍在，评分调用0、产品公司保存0；先前7 micro-USD夹具预留仍占用7，不重置或认领。`checkpointInvocation`仅接受原用户、动作和计划，不匹配的用户或动作被拒绝。

解除合成暂停后从同一检查点继续。知识、计划、发现、补证、校正各保持1次，评分/复核各执行1次，实际产品SQL最终合格并保存1家、交接1、任务完成回执1、`target-met`。夹具预留7 micro-USD全数分配该公司，未分配0，没有重复预留。该模式在本地生产Chrome桌面1366×900及手机390×844均通过任务API、页面和刷新验收；清理了测试账号、公司、任务、预算和检查点。

本夹具把计划及各阶段结果预置，并在同一进程恢复；它验证可控评分前暂停的检查点接线，不证明跨进程完整正结果运行、在途付费请求未知费用的安全重放、真实模型质量、自然语言解析或供应商账单。此前独立的跨进程P06夹具仍是预置已校正候选，两项证据不能合并宣称A11真实闭环通过。

聚合记录：合成输入1家、评分有效1、下游产品评估/交接/保存各1、用户实际采用未知；前置阶段重跑0、技术暂停1、业务事实丢弃0、真实token/API credits/现金/付费重试及模型/搜索/SMTP外发均0。内部有效评分到保存利用率1/1，真实供应商延迟和节省金额未知，本地脚本约4–8秒。优化机会是把该检查点扩展到跨进程正结果和可信费用报告核验，未知付费请求须继续由现有防重放门禁阻止自动重购。

验证：`node scripts/run-tsx.cjs scripts/verify-positive-result-graph-sql.ts --pause-resume` 的SQL及本地生产双视口模式通过；原验收账号预算未改变。该阶段不重跑正式测评或启动真实市场搜索。
