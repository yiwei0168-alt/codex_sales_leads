# P06 备用路由精确检查点与恢复（2026-09-14）

最终回归：全量1007项/206文件、类型检查、生产构建、lint 0错误/11条既有警告、生产依赖审计0漏洞通过；SQL检查点及主路由/条件Pro/备用/原单次完整图均复验并清理夹具。

阶段161在既有已批准备用路由范围内，按**实际**提供方、映射模型、传输合同和付费HTTP请求哈希保存分阶段事实及最终评分的完成响应。路由枚举只描述配置，仍由原执行器选择主/备用，费用层逐次按严格费率与预算门禁决定是否允许传输。保存前验证实际提供方和模型；备用响应保留原请求模型，读取时同时核对两者。完成行先于后续付费阶段保存，评估缓存使用实际完成路由的合同。旧主路由行仍可读取；更换或移除已完成路由时，同一动作的同一来源/阶段不得作为新工作自动付费。

隔离 SQL 验证：一个 OpenAI 兼容备用阶段和一个备用最终响应分别按独立合同保存，另一进程读取两项时新模型调用0；改回仅主路由合同拒绝重付费，错误原请求模型拒绝保存，另一用户不可读。模拟已报告且完整的同哈希费用允许安全恢复，改为费用未知立即阻止。完整产品图的另一合成公司保留126条事实/126个本轮来源，跨进程执行3个事实阶段及1个最终评分假响应，其中一个阶段和最终使用备用合同；产品SQL评估1、评分指标1、模型用量4、合格和交付0、跨用户评估读取0。主路由分阶段、条件Pro及原单次无损路径分别回归4、5、1次假响应。全部外部传输被禁，真实token/API credits/现金和外部重试0。

本阶段没有批准新的备用模型或价格。默认 OpenRouter 备用路由的严格费率与输出合同仍未齐备，会在费用门禁停下；真实模型可能返回不同的模型别名，此时精确身份校验暂停，不能假定与请求模型等价。151事实的另一合成请求在OpenAI兼容备用完整请求下为66,543字节，超过61,440字节上限，正确保持技术待恢复；126事实夹具通过不能推广为任意长单公司处理能力。真实语义、可信账单、完整整次费用上界和A11付费闭环未验收。

回滚先将执行器恢复为主路由专用读取，保留已保存的备用响应及原费用流水待人工核对；本阶段不改数据库结构，不回写冻结测评或历史评分。优化机会是补齐仍缺的严格费率、长最终请求的有界拆解、真实提供方模型别名核对与完整业务采用遥测；真实节省和每条合格线索成本目前未知。
