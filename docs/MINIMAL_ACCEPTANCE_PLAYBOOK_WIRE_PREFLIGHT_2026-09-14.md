# 最小闭环市场计划 SDK 请求只读预检（阶段102）

最小真实闭环开始前的预检原先只按模型、端点及输出上限查静态费率，所有请求字节数填0，`actualRequestContractsChecked=false`。本阶段给预检增加一条**合成**市场计划的实际 LangChain SDK 序列化捕获：临时合成密钥和进程内 `fetch` 拦截器阻止一切外发，并在结束后恢复；拦截原始请求只在内存解析，报告不写入提示词、密钥或客户数据。随后按原始传输字节、模型、输出上限、方法、URL、标头和 JSON 字段运行生效合同检查。预算占用在捕获前后各只读一次并要求一致。

2026-09-14 本地预检捕获一条 `openai/gpt-5.6-sol` 市场计划合成请求：**4,171 字节、4,096 最大输出 token**，符合现行 `openrouter-sol-standard-json-v1` 文本/严格 JSON 合同，单次保守上界 **USD27.345252**。但最后核验的累计 USD30 占用仍为 **USD12.324404**，余额 **USD17.675596**，这一单次上界不能预留。三条合成 RAG 引用仅用于生成请求形态，未校验实际市场知识体量或实际自然语言输入。`actualRequestContractsChecked` 仍为 `false`，整次运行上界仍为未知；搜索、补证、校正、复核、备用路由以及真实服务商响应均未被这项单入口检查覆盖。S01路由候选和新费用合同仍待确认，未改变产品路由或门禁。

验证：脚本实际运行成功，TypeScript 与改动文件 lint 通过；预算前后占用相同，provider calls 0、job认领0、账号修改0。合成输入3条知识引用，有效输出为1份请求契约判定，下游仅供验收门禁审查，真实业务输出/采用0；模型token/API credits/现金0、重试0、丢弃输出0，运行时间不能当供应商延迟。优化机会是继续按实际序列化捕获逐入口费用合同，并在整次保守上界和实时余额可核对后才启动付费闭环。
