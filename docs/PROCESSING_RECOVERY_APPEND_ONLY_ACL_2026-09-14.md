# Processing-recovery lineage ACL — stage 184 (2026-09-14)

The current isolated recovery-budget SQL verifier initially **failed**: its application-role `UPDATE lead_processing_recovery SET metadata='{}'` succeeded where an immutable lineage link required rejection. Migration 055 granted only SELECT/INSERT when first created, but replaying migration 014 grants SELECT/INSERT/UPDATE/DELETE on every public table. The later billing ACL migration 071 did not include `lead_processing_recovery`, so a full migration replay left this recovery table writable. Row-level security restricts the owner; it does not prevent that owner from changing the budget lineage.

Migration [072](../db/migrations/072_processing_recovery_append_only_acl.sql) revokes all public/application grants on this table and grants the application role SELECT/INSERT only. The table, rows, RLS policy, insert validator and business API are unchanged. Existing recovery links remain readable and new approved links remain insertable; updates and deletes are denied. The migration is idempotent and appears after migration 014's broad grant. Its documented rollback restores migration 014's UPDATE/DELETE grant, which would reopen the issue and should be used only for an explicit compatibility rollback.

Verification on the local product database:

- Before migration 072, `verify-processing-recovery-budget.ts` failed at the expected UPDATE rejection. It cleaned its synthetic fixture.
- The complete migration sequence 001–072 ran twice. After each run, the isolated SQL verifier passed: effective application ACL SELECT/INSERT true, UPDATE/DELETE false; concurrent reservation 1/2, ancestor/child limit and descendant occupancy conservation, cross-user and scope isolation, unknown ancestor fee block, immutable link rejection. Each verifier removed its fixture.
- The broadened `verify-append-only-acl.ts` read-only audit passed 16/16 billing/recovery tables with no unexpected privileges. The current queue/claim/runner recovery guard separately passed three reserved/unknown attempts, kept its checkpoint and original 7-micro-USD synthetic reservation unchanged, and cleaned its fixture.
- Full 1,021-test / 207-file regression, typecheck and changed-script lint passed. The read-only A11 preview still reports USD12.324404 occupied of USD30, USD17.675596 remaining, `checkedTariffsAvailable=false`, total-run bound unknown, zero provider calls and zero claimed user jobs.

These synthetic checks prove the current local ACL and budget-lineage invariants. They do not reconcile a real provider invoice, exercise a genuinely interrupted external request, generate a qualified company, or complete A11. Workflow inputs were two full migration replays and isolated budget/recovery fixtures; valid outputs were two passing lineage checks, a 16-table ACL audit and the queue guard, all used for A06/A12 acceptance evidence. Model tokens, paid API credits, cash, external retries and mail sends were zero; local verifier latency was about 2–4 seconds per run and real provider latency/user adoption remain unknown. The initial overbroad grant was the rejected security state. Keeping the ACL audit in full replay validation avoids paying to rediscover this failure in a live recovery.
