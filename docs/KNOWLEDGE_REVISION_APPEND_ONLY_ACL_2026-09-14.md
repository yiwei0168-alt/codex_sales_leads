# Private knowledge revision ACL — stage 187 (2026-09-14)

`knowledge_document_revision` stores versioned private document content. Product writes insert a revision and history reads select it; there is no direct revision update/delete path. Migration 014's full-replay grant and migration 044's explicit DELETE left the application role with effective UPDATE/DELETE. A read-only audit confirmed both privileges true, allowing an owner to rewrite its own past content outside the versioned save path.

[Migration 075](../db/migrations/075_knowledge_revision_append_only_acl.sql) follows the old grants and limits direct application access to SELECT/INSERT. It preserves rows, RLS and public API shape. A document owner's existing DELETE of the parent document still cascades to its revisions, so privacy deletion remains available; the migration's explicit compatibility rollback would reopen direct history mutation. The migration is idempotent under full replay.

Local verification: two complete 001–075 migration replays passed. After each, the 23/23 effective-role audit and isolated SQL verifier passed: application revision insert/read succeeds, owner UPDATE/DELETE fails, and parent deletion cascades. The two-user private knowledge/RAG/mailbox isolation verifier passed with fixture cleanup. Full 1,021 tests / 207 files, typecheck and changed-script lint passed. No real embedding, model, paid search, SMTP, task claim or historical cost change occurred. This is local history integrity and isolation evidence, not live user adoption or A11 completion.

Efficiency record: two full replays and two isolated fixture types were inputs; two 23-table audits, two revision checks and one private-isolation check were valid outputs used for A13. One old ACL row was rejected for excess privileges. Customer content and business candidates were not used; real downstream use and adoption remain unknown. Model tokens, paid API credits, cash and external retries were zero. Local SQL checks took about one to four seconds; provider latency is unknown. Audit effective grants after replay to keep past private content and cost observations trustworthy.
