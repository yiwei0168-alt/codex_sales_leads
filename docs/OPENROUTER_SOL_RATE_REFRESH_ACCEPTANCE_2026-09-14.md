# OpenRouter Sol 公开费率定期复核（阶段104）

按已确认 A10/A11，本阶段只为**现行** `openrouter-sol-credits-standard-text-json` 静态合同接入七天公开元数据复核。worker 每五分钟检查是否到期；实际公共抓取受 PostgreSQL 事务咨询锁与 `next_attempt_at` 控制：成功后七天再查，网络或公共接口故障一小时后再试。复核只 GET [OpenRouter 模型目录](https://openrouter.ai/api/v1/models)及[Sol 端点接口](https://openrouter.ai/api/v1/models/openai/gpt-5.6-sol-20260709/endpoints)，逐端点与[已冻结公开基线](OPENROUTER_ROUTE_ENDPOINT_EVIDENCE_2026-09-14.json)比对。它不读取账号密钥、账单或客户内容，也不发送推理请求。

迁移 `db/migrations/056_openrouter_tariff_reference.sql` 新增三张公共表：不可由应用角色更新的追加式证据快照、刷新状态，以及仅聚合指标的刷新观测。检测到模型身份、端点、参数或价格合同变化、字段缺失或无法计算上界时，状态变为 `review-required`，`hold` 粘性保留，所有用户在 `reservePaidCall` 为此规则新预留前收到 `tariff-suspended`。网络失败保留旧快照和已有 hold；没有发现变化也**不会**延长 `request-bounds-v1.6.0` 的 `verifiedAt`、`expiresAt`、促销截止、模型路由或金额。现行静态规则到期仍由原门禁拒绝。预算 API/页面仅只读展示复核状态、上次检查和下次尝试；缺记录或读取故障保持未知，不把页面刷新当外部抓取或用户采用。

实际 PostgreSQL 验证脚本 `scripts/verify-openrouter-rate-refresh.ts` 应用迁移056后并发运行两次刷新：最新状态 `validated`、`hold=false`；最多两个公共 GET，随后重复检查0 GET。应用角色能读状态但不能改写历史快照；迁移前后付费预留笔数、费用观测笔数与预算占用一致。没有认领原队列或真实供应商调用。可回滚方式：先移除 worker 与预留查询对新表的依赖，再由迁移角色在审计留存完成后删除三张新表；本阶段未执行回滚、未更改历史费用。

验证：针对性30项通过；全量 **911项/198文件**、类型检查、Next.js生产构建、文档生成检查通过；全库 lint 0错误/11既有警告，生产依赖审计0漏洞。本地生产新构建在独立3018端口完成桌面1366px与手机390px共**60组**真实HTTP/Chrome核对，含 API 到费率状态页面、预算刷新只读、国家隔离及现有恢复/UI回归；临时合成用户/公司已清理。浏览器探针仅建立1笔可清理的合成预留，真实模型/搜索付费调用0、真实邮件0。端口已关闭。

本阶段业务效率：真实业务输入、有效输出、下游使用及用户实际采用均为0；模型 token/API-credit/现金费用0。公共源初次抓取2次、后续缓存读取0次 HTTP；无付费重试。刷新延迟由观测表记录，不把本地探针耗时当供应商延迟。优化机会是共享七天公开证据，先暂停漂移合同再做费率变更确认，避免每用户重复抓取或旧价继续预留。

**剩余验收**：本阶段只覆盖现行 Sol 合同。其他七条静态模型/搜索合同、人民币动态模型和五搜索入口的对应公开费率刷新及变更确认尚缺；真实涨价/合同变更的用户确认与新版本启用流程尚未实施；worker 长期定时触发未在本次短时验收中观察。S01 路由方案仍待确认，整次真实业务预留和 A11 闭环未通过。冻结哥伦比亚数据未改变。
