# P06 分阶段付费防重放身份核对（2026-09-14）

阶段152在原阶段完成行之外追加可空的 `paid_request_fingerprint`。DeepSeek 的主路由纯计算与费用层共用同一 `paid-request-replay-v1` 身份：HTTP 方法、源站、路径、查询和完整请求体；不含密钥。Resilient Provider 只暴露主路由身份，备用提供方的结果仍不能写入主路由检查点。读写阶段行必须同时匹配用户、工作区、动作、国家、公司、来源、阶段、请求合同与该付费请求身份。

图恢复门禁仅对状态 `reported`、输出非截断、且同任务有精确付费请求身份的已完成阶段行免除“检查点后出现评分成功响应”的阻止。无关联、未知费用或截断响应继续阻止；任务级未知费用门禁和费用预算门禁保持原样。此处不授权重放缺失/未知付费请求。

本地验证：迁移068重复执行、跨进程读取、租户/国家隔离、写后不可修改与幂等通过。用一条**模拟**评分费用行先验证不匹配阻止，再验证精确匹配通过；切为 `unknown` 或 `outputIncomplete=true` 均阻止。夹具及费用行已清理；真实供应商调用和新增占用均为0。类型检查、1002项/205文件全量测试、生产构建和改动文件 ESLint（0错误）通过。阶段 Agent 尚未使用这些检查点，分阶段最终综合与真实业务闭环未完成。

迁移为可兼容扩展：既有行的身份为空，不被恢复门禁当作已核对付费响应。回滚前须核对并保留非空身份与对应阶段完成行；旧应用可忽略新增列。未执行破坏性回滚。
