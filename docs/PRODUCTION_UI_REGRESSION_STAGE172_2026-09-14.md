# 阶段172：阶段171后本地生产页面回归

代码版本 `19ef936` 的 Next.js 16.3.4 生产构建在独立端口 `127.0.0.1:3029` 运行。`UI_VERIFY_BASE_URL=http://127.0.0.1:3029 node scripts/run-tsx.cjs scripts/verify-authenticated-ui.ts` 用真实 Chrome 在桌面 1366×900 与手机 390×844 各完成 33 项、合计 66 项，包括登录、国家切换与隔离、用户角色/账户等级/合作路径及个人记忆、手动渠道图、策略邮件人工正文和批准复用、九类任务状态、成本/采用API、暂停恢复、会话拒绝与刷新一致性。

隔离合成用户、工作区和公司均由脚本清理；真实模型/搜索付费调用 0，SMTP 发送 0，一条合成预留仅用于页面/SQL验收，不计真实支出。阶段171的发现请求量预检修正没有破坏这些页面行为。此项不证明真实模型判断、自然语言到合格公司或可信账单闭环；A11和整体验收仍未通过。
