# P06 最终评分响应防重复付费恢复（2026-09-14）

阶段157为超限公司分阶段事实处理后的**最终评分响应**新增追加式 SQL 检查点（迁移 069）。它按用户、工作区、动作、国家、公司、来源指纹、精确执行合同和付费 HTTP 请求哈希绑定。评分 Agent 先查完成响应；未命中才允许原有费用门禁下的提供方调用，返回后先保存结构化响应，再做业务 Schema 校验、确定性评分和评估缓存发布。保存或读取失败时不发下一次请求。已返回但业务输出缺项的响应也保留，恢复时读取同一结果，不将缺项自动重新付费。

恢复费用门禁仅在相关费用行已报告、未截断且付费请求哈希匹配完成的阶段或最终响应时放行。未知、截断或哈希不匹配继续阻止。与原阶段检查点一样，实际提供方/模型必须匹配主路由请求；备用路由响应在当前合同下不能被冒充为可复用主路由结果，需后续独立路由恢复方案。

隔离 SQL 验证：首进程在第2事实阶段预算故障后保留第1阶段；第二进程只完成余下3阶段和最终评分，最终响应行1；第三进程从阶段与最终行重建评分，模型调用0、用量重计0。模拟已报告最终费用行的精确哈希放行，改为未知、截断或不匹配均阻止；跨用户读不到最终行，应用角色不能更新。该夹具和所有合成费用行已清理，真实提供方调用/现金/token/API credits为0。完整图→产品 SQL 的151事实/151证据路径仍通过，原101/105共享短语单次评分路径不变。

本地 `db:migrate` 重放 001–069 通过；定向 Agent 的38项、全量1005项/206文件、类型检查、生产构建、lint 0错误/11既有警告、生产依赖审计0漏洞通过。回滚顺序为先将代码恢复至不读取新表的版本，保留069已保存行及原费用流水以便核对；只有确认不存在需恢复的行且完成备份后才考虑人工删除新表。旧评分、历史费用与冻结测评不回写。

仍缺真实模型摘要语义和质量、Flash→Pro 的分阶段最终请求、备用路由的独立恢复合同、任意长单事实，以及完整费用上界/真实业务闭环。不能因这项无付费验证宣称 P06 或整体验收完成。
