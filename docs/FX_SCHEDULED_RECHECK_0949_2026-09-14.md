# Stage 207: 09:49 UTC scheduled ECB FX recheck

The stored retry time from stage 195 was 2026-09-14 09:49:42.422 UTC. At 09:49:51 UTC, `node scripts/run-tsx.cjs scripts/refresh-billing-fx-reference-once.ts` made exactly **one** credential-free GET to the [ECB daily reference source](https://www.ecb.europa.eu/stats/eurofxref/eurofxref-daily.xml). The official response parsed, but its reference date remained 2026-09-11, past the confirmed 72-hour window. The refresh returned `status=unavailable`, `failureClass=staleOfficialReference`, `httpCalls=1`, and stored `nextAttemptAt=2026-09-14T10:49:51.727Z`. The read-only status at 09:49:52 UTC showed `fx.status=expired-or-invalid`, `asOf=2026-09-11T00:00:00Z`, and `effectiveExpiresAt=2026-09-14T00:00:00Z`. The three CNY model tariff rules themselves remain within their verification windows through 2026-09-20; the missing *usable FX* is a separate paid-admission hold.

The subsequent read-only budget check returned USD30 limit, USD12.324404 occupied and USD17.675596 remaining, with zero unsettled calls, six unknown historical bills and zero new paid provider calls. The refresh reported zero jobs claimed. No customer task, model, search, mail, invoice or real A11 run was touched. Do not call the refresh again before the stored next-attempt time. Even a later valid FX reference would not establish the still-unknown whole-run A11 cost bound.

Efficiency: one official reference input, zero valid new FX outputs, zero downstream-usable outputs and one `staleOfficialReference` discarded result; utilization 0. The single free HTTP attempt took about two seconds locally, with zero model tokens, paid API credits, cash, retries and discarded business outputs. Customer adoption and paid provider latency remain unknown. Opportunity: retain the shared backoff and conservative date hold until a newer official reference is available; do not make per-user or per-model retries.
