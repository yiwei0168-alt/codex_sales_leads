import {tenantQuery} from "@/lib/rag/db";import type {AssistantConversationTurn} from "@/lib/assistant/types";import type {KnowledgeBaseType} from "@/lib/rag/types";import {exactModelMentions,resolveAttributeCandidates} from "./query-normalizer";
export type KnowledgeAction="open-document"|"fact-query"|"compare-facts"|"explain";
export interface KnowledgeRequest{question:string;action:KnowledgeAction;entityKeys:string[];attributeKeys:string[];documentKind?:"datasheet"|"manual"|"original";collections?:KnowledgeBaseType[];entry:"assistant"|"knowledge-page";}
export async function parseKnowledgeRequest(userId:string,input:{question:string;history?:AssistantConversationTurn[];collections?:KnowledgeBaseType[];entry:KnowledgeRequest["entry"]}):Promise<KnowledgeRequest>{const models=await tenantQuery<{key:string}>(userId,"select canonical_key as key from knowledge_entity where entity_type='product' order by length(canonical_key) desc",[]);let entityKeys=exactModelMentions(input.question,models.map(row=>row.key));if(!entityKeys.length){for(const turn of [...(input.history??[])].reverse().slice(0,6)){entityKeys=exactModelMentions(turn.content,models.map(row=>row.key));if(entityKeys.length===1)break;if(entityKeys.length>1){entityKeys=[];break;}}}const attributes=resolveAttributeCandidates(input.question);const open=/\b(?:open|show|view|download)\b.*\b(?:datasheet|manual|pdf|document)\b|打开|查看|下载|原始资料|原文件|规格书/i.test(input.question);const compare=/\b(?:compare|versus|vs\.?)\b|比较|对比/i.test(input.question);return{question:input.question,action:open?"open-document":compare&&entityKeys.length>1?"compare-facts":attributes.length&&entityKeys.length?"fact-query":"explain",entityKeys,attributeKeys:attributes,documentKind:/manual|手册/i.test(input.question)?"manual":/datasheet|规格书/i.test(input.question)?"datasheet":"original",collections:input.collections,entry:input.entry};}
