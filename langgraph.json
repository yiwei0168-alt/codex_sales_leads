{
  "node_version": "24",
  "node_loader": "tsx/esm",
  "graphs": {
    "runtime_health": {
      "path": "./src/langgraph/server.ts:runtimeHealthGraph",
      "description": "Zero-cost local health graph for the standalone LangGraph runtime."
    },
    "assistant_workflow": {
      "path": "./src/langgraph/server.ts:assistantWorkflowGraph",
      "description": "Complete assistant orchestration with expandable intent, RAG, search, and synthesis nodes."
    },
    "knowledge_workflow": {
      "path": "./src/langgraph/server.ts:knowledgeWorkflowGraph",
      "description": "Shared document access, verified fact answers, and grounded explanation orchestration."
    },
    "lead_workflow": {
      "path": "./src/langgraph/server.ts:leadWorkflowGraph",
      "description": "Complete lead orchestration with expandable knowledge, discovery, evidence, scoring, review, and persistence nodes."
    }
  },
  "env": ".env.local",
  "http": {
    "cors": {
      "allow_origins": [
        "http://127.0.0.1:3000",
        "http://localhost:3000",
        "https://smith.langchain.com"
      ],
      "allow_methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"],
      "expose_headers": ["Content-Location", "X-Pagination-Total"]
    }
  }
}
