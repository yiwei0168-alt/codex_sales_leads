{
  "node_version": "24",
  "node_loader": "tsx/esm",
  "graphs": {
    "runtime_health": {
      "path": "./src/langgraph/server.ts:runtimeHealthGraph",
      "description": "Zero-cost local health graph for the standalone LangGraph runtime."
    },
    "assistant_workflow": {
      "path": "./src/langgraph/server.ts:assistantWorkflowGraph",
      "description": "Adapter for the existing budgeted assistant workflow."
    },
    "lead_workflow": {
      "path": "./src/langgraph/server.ts:leadWorkflowGraph",
      "description": "Adapter for the existing tenant-scoped lead workflow and PostgreSQL checkpoints."
    }
  },
  "env": ".env.local",
  "http": {
    "cors": {
      "allow_origins": [
        "http://127.0.0.1:3000",
        "http://localhost:3000"
      ],
      "allow_methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"],
      "allow_headers": ["Content-Type", "Authorization", "X-Api-Key"]
    }
  }
}
