# Network Channel Copilot PRD v1.1

## 2026-09-13 冻结请求计数验收范围

用户确认 B18，207 家冻结候选三个任务模式共 1,053 重建请求本地计数通过。详见规则表 v1.4.0 与脱敏聚合报告；现行批次最大输入 9,492/13,513/13,017 tokens。评分使用标准 playbook，不是原始上下文/线上费用重放。当前只新增诊断能力，不改变评分、证据、拆批、费率或付费门禁；固定提示复用是待研究机会，不是已实现优化。

## 2026-09-13 Docker 离线验证

用户批准 B17 隔离构建方案。新增独立诊断器 `scripts/offline-tokenizer/`，合成文本两端点格式的本地编码验证通过，不依赖 Windows 全局编译器或图像组件。准确数字及验证范围见规则表 v1.3.0；未接入产品、未核验线上 token 账单、未解锁付费请求。

## 2026-09-13 本地计数选型确认

用户确认规则 B16：固定官方 recipe＋匹配 V4 tokenizer，先隔离离线验证，不改变模型/thinking/付费放行。源码与文件完整性检查已完成；两种请求格式计数尚受本机构建环境限制未执行。准确版本、校验值与限制见 [规则表 v1.2.0](docs/CONFIRMED_PRODUCT_RULES.md)，不能把选型或文件校验当作生产验收。

## 2026-09-12 用户确认规则与实现边界

新增确认 B11–B15：优先匹配模型的官方本地 token 计数；无准确计数时仅用可核验上界；按输入上界＋输出上限＋其他费用逐次预留；未知历史占用保留；模型/端点/计费变化重新核验。详见规则表 v1.1.0。官方 recipe 的具体接入选型仍待确认，尚未实现或开放付费。

统一审阅入口：[用户确认规则登记表 v1.0.0](docs/CONFIRMED_PRODUCT_RULES.md)。费用账本 A01–A16、DeepSeek 请求边界 B01–B10 及历史明确业务规则均在表内保留编号、范围与状态。最新确认：完整请求体补证 36 KiB、仅评分 56 KiB、评分＋路径 60 KiB，输出默认 8,192；字节上限不是计费 token 上界，尚不能开放付费调用。完整账本新规则及这些请求边界仍待实现/验收，不因本次文档登记视为已完成。后续修改同步登记代码/配置与验证依据。

## 2026-09-12 自然语言预算提案（最新补充）

assistant-intent-plan-v1.3继续使用现有Kimi轻量模型和多轮历史，新增budget_change结构：user/task范围、绝对累计美元上限。币种、范围或金额不明确时澄清；数字校验由程序完成，预算提案不升级K3、不调用RAG/网页搜索。联合“新搜索+预算”先澄清两步建立计划/设置预算，不默默丢掉预算或自动执行。模型不能返回可信任务ID。

助理消息保存可审阅提案；页面允许修改金额，任务范围必须从当前对话拥有的搜索任务中手工选择，取消任务不列出。点击后再次明确确认，通过已有owner校验的/api/budget写入，旧预留不重置、额度不自动追加、任务不启动。初始没有预算或费率时，Kimi调用本身仍受阻；任务中心的直接预算表单可用于首次设置。

提案复用既有意图模型聚合遥测和预算变更审计，没有增加第二次模型识别、补证或搜索流程。预算提案生成不等于用户采用；成功确认只代表预算配置采用，不代表真实费用或搜索结果。后续优化：增加提案ID到确认审计的细粒度关联，以量化提案改写率；当前不虚构该指标。

567项单元测试/136文件、18项桌面/手机隔离浏览器检查、类型检查和生产构建通过；lint 0错误/11项既有警告。隔离检查现同时加载globals.css与ipados.css，避免漏掉实际主题覆盖。真实登录验收12组通过（新增预算提案读取/取消无写入），390px截图已复查，合成记录已清理。没有实际模型、搜索或SMTP调用；这不是Kimi真实账号连通性验收。剩余上线限制仍为审核后的供应商请求费用上界/发票核销合同、真实SMTP测试收件人，以及后续真实采用链路测量；未擅自更改实验策略。

## 2026-09-12 任务预算与聚合遥测收尾（最新状态）

迁移050已应用：task_spend_limit保存owner/action级可选累计上限，spend_budget_change以只追加记录保存用户确认的修改和零外部调用聚合指标。每次HTTP预留在同一用户行锁下同时检查总预算和任务预算，修改上限不得低于已有预留；不重置历史占用、不自动启动任务。任务详情折叠读取预算，支持明确确认修改，展示各阶段预留/已报告费用/未知账单/累计HTTP耗时；耗时不是并行墙钟时间，预留不是实际支出。无任务上限沿用用户总预算，续搜仍是需独立确认的新任务。

新增trackedOperation覆盖独立RAG回答、知识分块存储、手动私有记忆和跟进生成的成功/失败聚合记录：输入量、有效输出、存储采用或待审核、token/费用/重试未知值、耗时、弃用原因和优化机会。只保存计数，不写提示词、邮件或错误原文；指标落库失败不重放成功的业务操作。嵌套阶段保留父任务预算ID，不能因新建指标ID逃逸任务上限。RAG回答返回不等于用户采用；知识/记忆入库不等于未来检索或邮件采用。HTTP账本与操作指标有重叠，不相加。跟进token为最终响应报告，不冒充包含所有重试的总量。

验证：561项单元测试/135文件、16项隔离浏览器测试、类型检查通过，lint为0错误/11项既有警告；任务预算真实SQL探针验证预留拒绝不增加占用及跨用户RLS。登录验收扩展为10组桌面/手机真实本地API检查，包括零美元合成任务预算；付费调用和SMTP发送均为0。临时合成用户清理新增预算外键时曾回滚，已用严格身份/无付费记录检查清理并修正自动清理顺序；未删除客户数据。

仍不能声称已完成真实付费上线验收：已审核网关全包请求费用上界尚缺，config/billing/request-bounds-v1.0.0.json保持空规则；部分无输出上界请求继续阻止。发票核销/释放依赖账单接口合同，未知占用保留。真实SMTP需要专用测试收件人。自然语言预算变更尚未实现，当前只有显式表单确认；未实现的最终用户采用事件保持unknown，不虚构完整利用率或节省百分比。手动记忆持锁embedding的连接占用、SDK包装拒绝的无网络重试以及更细的语义采用链路作为后续优化记录，未擅自降模型或输出质量。

## 2026-09-12 登录后响应式验收

新增scripts/verify-authenticated-ui.ts：只允许localhost，用同库迁移连接建立临时合成用户/工作区，真实应用角色和密码登录访问API；检查1366px与390px的GB/MX独立候选、详情Escape、实际渠道图、任务/知识/邮箱导航及预算/用量读取。浏览器禁止外网及非登录写入，不替换API响应。8组检查通过，付费预留0、真实邮件0；临时用户/工作区/公司已清理，正常登录审计保留。另有14项隔离浏览器测试、552项单元测试及构建通过。

实际检查发现并修复手机顶栏溢出、筛选文字竖排、侧栏图标缺可访问名称和移动端退出入口被隐藏；渠道图表单整理为响应式布局，SVG横向滚动仅限图框。已截图复查。验收使用合成数据，不代表所有真实客户历史异常或SMTP发送均已验收；费率和真实收件人未确认的外部调用继续阻止。验收初次localhost探测受CLI代理影响已改为显式本地直连，不能据此断言原3000服务本身故障。

## 2026-09-12 剩余付费入口与拒绝审计

已接入联系人、存量关系分析、独立RAG查询/知识入库、开发上下文/反馈/手动记忆、邮箱授权学习的用户预算作用域；本地邮箱筛选不调用模型。Snov OAuth也不假定免费，表单仅按已审核端点/请求体积报价，无费率不发送。结果链接限定官方域名和结果路径、禁重定向；按顶层status判断完成，轮询未完成抛出unknown而非缓存为空；保留第一页最多50条的现有范围并明确提示，不自动翻页。依据：https://snov.io/api 。

DeepSeek/兼容模型/搜索/补证/主评分/复核捕获预算拒绝后向上抛出，不触发备用供应商、候选失败评分或付费重试。预算拒绝记录到product_operation_metric，仅本次被阻止HTTP的现金/token为0，不把此前调用视为免费。用量接口把操作表与30天HTTP预留/已报告费用/未知数/耗时分开，不重复加总；输入输出使用效率仍以各业务表语义为准。新增552项总回归通过、类型检查通过；不含真实付费调用。

剩余边界：供应商/网关的全包请求费用上界仍未审核，费率保持空，不把公开token单价或历史估算冒充合同上界；任务子预算、发票核销释放、独立RAG/记忆等完整语义利用率仍待补齐。手动记忆目前持锁进行embedding，需后续优化连接占用；仅新增费用检查未改变其并发写入语义。当前代码覆盖已识别的产品入口，不等于历史费用全量或真实模型/SMTP验收。

## 2026-09-12 助手与开发生成预算边界

助手主图、开发策略/修订及跟进生成建立用户费用作用域；Kimi、Claude、Gemini聊天搜索及综合回答HTTP传输接入预留检查。并发子作用域独立，禁止跨用户嵌套；开发生成沿用已持久化操作ID。Kimi/Claude捕获预算拒绝后直接退出，不重试、不生成模板冒充成功、不切换供应商。跟进入口返回明确402，不改变原邮件。既有输出token上限不降低，未设上限请求继续保守阻止。

验证：541项测试、类型检查通过，lint 0错误/11项已有警告；新增无费率零网络调用及并发归属测试。无付费调用、真实邮件发送或数据库迁移。联系人、独立知识处理/邮箱学习、完整阶段细分与拒绝遥测、费用上界配置及登录端到端验收仍未完成；未验证费率保持空，不能将预留占用当实际成本或声称全产品已封顶。

状态：用户已逐项确认；实施中。本文是 v1.0 的增量规范，冲突时以本文为准。

已完成但未填满：任务详情可建立缺口续搜提案，不执行、不重置原任务与费用。提案按剩余数量和原国家/角色创建新任务，排除前序已评估公司，按当前产品机制评分；需在子任务页单独确认计划及费用。重复点击复用同一子任务；最多三次续搜，确认停滞或连续零新增时先重新规划。不凭当前候选库倒推旧任务数量或历史评分。

异常缓存恢复：任务详情允许用户核实并结束超过十分钟的单公司联系人查询，以及失败/超过十分钟未结束的关系分析。操作不重试、不退款，不解除未知美元占用；需要新调用时仍须分别在公司页/关系图主动确认。旧任务迟到结果只记用量，不覆盖新结果；已完成、近期运行、其他用户及已核实任务不可重置。

国家业务状态已切换（覆盖下方基础阶段记录）：047/048已应用。候选公司按用户工作区与国家独立存储、展示、编辑和下游引用；共享身份与联系人不重复查询。重新评分保留开发状态与用户角色。新开发草稿和邮件固定国家；旧邮件不再借用公司当前国家，明确标注“国家未确认”，用户主动确认后才计入当地联系记录、开放跟进，不会重发。旧草稿只有可靠搜索任务来源才回填国家，否则留在未确认历史中。验证范围与限制见国家状态合同，未宣称登录端到端或真实发信已验收。

国家业务状态实施补充：新增047迁移及国家快照模块，已完成回滚式数据库隔离验证，尚未应用或切换生产读写。保留现有全局身份和外键；每国独立评分、人工覆盖与开发进度。旧评分国家或run归属冲突时标为待复核，不能冒充新评分。邮件/策略需明确国家来源后再统一切换，详见 `docs/COMPANY_MARKET_STATE_CONTRACT.md`。

2026-09-12 用户确认：缺费率/费用上界时禁止付费；同域名跨国共享身份与公共证据，但各国评分、角色路径、账户等级与开发进度独立。预算基础表和任务中心预算界面已接入后台线索工作流，未知费用保留占用、不自动释放；独立聊天/邮件/联系人等入口仍待接，不能宣称完整产品硬上限。费率上界配置未填入未经验证的数值。国家业务状态迁移尚在实施。

2026-09-12 安全修复：Next.js 16.3.4、mailparser 3.9.24 与测试依赖兼容补丁已更新，安装审计降至0项已知漏洞；后文“当前依赖告警”作为历史记录保留。该检查不替代完整安全审查，也不代表尚未闭环功能已完成。

关系建议采用追踪：载入不计采用；保存时关联原分析ID与建议索引，服务端校验用户、工作区、国家与有向公司对，同一建议重复保存只计一次。允许用户编辑；此记录证明建议参与了用户保存决策，不证明真实合作或用户原文照采。历史缺失记录不倒推。

2026-09-11 验收补充：公司、任务、证据弹层支持焦点进入/约束/恢复和仅关闭最上层的 Escape；评分错误在成功重试后清除。6项桌面/390px隔离浏览器测试通过，不等同登录后全流程验收或邮件实投。新增策略依赖版本、生成失败任务和入库增改计数详见工作流文档。生产依赖安全审计仍有告警，未将产品标记为可安全公开部署。

2026-09-11 实施记录：新增国家开发机会地址、真实全局统计、线索高级筛选和批量阶段管理、公司三页签、存量策略读取、联系人缓存与落库、关系图缩放和分组，以及任务阶段边界暂停与检查点恢复。联系人缓存按用户隔离，陈旧数据只提醒；付费重新核实需主动操作。恢复不重新初始化已完成阶段，最终落库已完成时不再暂停。完整完成状态与尚未闭环项见 `docs/UI_V1.1_COMPLETION_CHECKLIST.md`，验证及限制见 `docs/PRODUCT_UI_V1.1_WORKFLOW.md`；此记录不表示全部功能已完成。

## 产品范围与授权

2026-09-11 知识/关系增量：公共证据、共享知识、私有知识分别检索；私有知识可独立删除。同名不同内容上传先确认，保存内容版本并防并发覆盖；旧文本从片段重建时明确标注。邮箱首次导入缓存公司域名匹配，歧义/人工解除不被后续导入覆盖。关系分析只用存量证据及当前常规模型，最多两条待核实建议，精确引用校验、版本缓存和否定抑制；不添加搜索。关系任务加入统一详情。

2026-09-11 邮件/任务增量：发送历史分页；异常发送允许用户外部核实后修正，标注用户来源且不重发。跟进采用已有发送链与当前用户确认的风格记忆。邮箱可断开并保留数据；删除本地导入邮件默认不删已确认知识及产品发送记录。同步支持收件箱/已发送和指定日期，重复导入跳过已存 UID。四类任务共用详情抽屉与独立 URL；评分展示读取当次政策快照。尚未完成事项继续保持在完成清单中，不以按钮存在作为功能验收。

产品仅供用户独立使用，不建设团队、负责人分配或团队权限。所有人工修改和学习数据保存到数据库，个人长期记忆与通用知识隔离。此次授权包含下述产品功能；测评中的搜索、补满、角色矫正与评分机制修改仍须单独确认，不随 UI 更新进入生产。

## 国家导航与销售线索

- 国家独立地址：`/markets/CO/leads`、`/markets/CO/channel-map`。刷新、收藏和前进后退保持国家。两板块共享国家，列表提供全部国家，关系图要求具体国家。
- 国家选择包含已有公司和任务国家；空国家显示空状态。切换国家只读数据库，不调用模型或搜索。助理结果入口携带任务国家并获取最新保存数据。
- 产品流程确认后的结果自动写入用户/工作区国家候选库；新建、更新、待核实分别统计，去重并保留来源和人工覆盖；入库不等于关系成立。
- 主表：公司、主角色、综合评分、账户等级、合作路径、开发阶段。城市及官网附于名称，评分证据放详情。
- 常用筛选：关键词、角色、阶段；高级筛选：等级、分数、路径、新鲜度。默认综合分降序，支持发现和更新时间。
- 合格、待核实分开；排除记录留在任务中。用户可修改主角色、等级、路径并记录历史和个人记忆。未生成路径显示未分析，不自动调用 Agent。
- 信息超过一年未有效核实时提醒；编辑不刷新核实日期，不自动失效或搜索。

## 公司详情

- 概览、评分与证据、开发记录三页签；联系人及路径位于概览，展开细节。
- 主角色与辅助角色区分；改变主角色后旧评分标记待更新。人工覆盖不得被后台静默覆盖。
- 一级分销不能选 KA；其他账户等级遵循现行配置。
- 最多两条推荐路径，展示类型、理由和适配分，允许选择及修改五种已确认类型；当前路径输入后续策略/邮件。
- 展示总分、政策版本、评分时间和 50/15/15/10/10 子项及证据；其他项名称读取实际配置。
- 联系人状态区分可用、待复核、无效；支持主动查询。
- 保存反馈包括进行中、成功、失败及重试；实际记录、历史版本与个人偏好分别保存。

## 渠道关系图

- 国家内按角色聚合、展开、搜索、筛选、缩放与定位；明确显示当前节点数及总量，不固定限制 5+8 家。
- 连线仅来自证据、Agent 分析或人工添加，移除数组配对假设。路径未指定具体公司时不生成关系。
- 关系状态：证据支持、待核实、用户确认、用户否定；记录类型、方向、来源和日期。确认/否定必须持久化。
- 用户添加、编辑关系，否定记录阻止无新依据的重复推荐。超过一年提醒。
- 可选择库内公司或新建公司（名称、国家必填，官网与角色可选）。检查域名和名称重复；新建标用户添加/未评估，进入待核实库，不赋分不付费调用。
- 无关系时展示节点及空状态，关系分析由用户主动触发。点击连线开侧栏，待核实列表默认折叠。

## 机会与邮件

- 机会仅收纳用户加入名单的公司；按国家/全部国家查看。阶段：待开发、准备中、已联系、沟通中、合作中、暂缓、已关闭。
- 看板及列表；支持下一步和到期日期，无负责人。批量加入、改阶段、移出名单不删除公司或历史。
- 手工设置已联系或产品成功发送可推进阶段；不倒退沟通中/合作中。生成、批准不是发送。
- Agent 响应生成策略、邮件、反馈修改等行动；已有版本优先展示，页面打开不自动生成。
- 同公司不同联系人可复用邮件，独立草稿/收件人/称呼及历史。版本、批准和人工编辑留存，学习不跨用户污染。
- 单封发送：已连接邮箱分别具备收信和发信状态；确认收件人/主题/正文后发送，防重复，结果不明先核实，不盲目重发。保存发送快照、时间、结果；发送成功不等于已读或送达。
- 已联系列表展示首次/最近发送时间和跟进次数；手动联系但无发信记录单独标注。
- 用户用自然语言描述跟进内容，Agent 参考首封、往来、联系人和个人风格生成称呼/正文/落款，缺失信息不编造；可展开历史作参考，编辑确认后发送；同联系人优先延续会话。
- 不含群发、自动回复、定时跟进。策略上下文变化标记需检查，不自动重新生成。

## 任务中心与详情

- 搜索、补证评分、关系、联系人、策略邮件、发信统一任务，工具调用为子步骤。
- 列表：运行中、待处理、最近完成；筛选国家/类型/状态。行展示名称、范围、状态、结果、时间。
- 助理、线索摘要和任务列表打开同一抽屉，并支持独立详情地址。
- 详情包含目标与交付数、时间、阶段（搜索/核实/角色/评分/入库）、去重及流失原因、新增/更新/转移；数字可查看公司明细，不重复计数。
- 部分完成区分停滞、轮数、预算、故障、用户停止。继续入口展示缺口及费用；恢复复用结果，停止在安全节点保存。
- 成本默认累计/预算/耗时，展开阶段用量、重试、缓存、实际与估算。异常显示影响和恢复状态，技术日志折叠。
- 已成功保存结果即时出现在国家库，保存失败不计交付。页面内通知，不加外部推送。

## 知识与邮箱

- 知识分产品公司、行业市场、个人长期记忆；公司证据从详情与统一入口查询。
- 记忆包括分类/等级/路径/关系/邮件风格/确认业务信息，保留适用范围、来源和时间；单公司事实不自动推广。
- 支持新增、编辑、停用、删除记忆，后续检索排除停用/删除项，历史邮件快照不改变。
- 知识上传处理状态可见；冲突保留双方，用户确认信息不冒充公开验证。技术检索测试与成本折叠。
- 邮箱按文件夹与时间范围同步，复用已同步数据；按地址/域名/会话关联公司，歧义由用户选择。
- 历史邮件学习候选允许看原文、编辑、批准、拒绝；未经授权审核不进入长期偏好。
- 断开连接停止收发但保留数据；删除邮件和知识独立操作并说明范围。此处不建设完整邮件客户端。

## AI 首页与全球概览

- 助理默认首页，多轮会话、知识问答、库内检索与明确行动；分清查看存量和新增搜索。
- 搜索计划可用自然语言修改国家/角色/数量/目标/预算，确认启动。Agent/OEM 销售机会须明确要求。
- 对话执行策略与跟进等行动，发送仍单独确认；结果卡只列交付与缺口及链接。
- 全球概览按国家展示候选、开发中、已联系、待跟进、运行任务。使用真实记录，无占位指标。

## 实施与验收

2026-09-12 SMTP补充：经用户确认，产品连接改用系统DNS解析，保留SMTP域名进行TLS证书校验；最多4个地址、每地址5秒，仅认证前连接故障允许切换，不自动重发邮件。修复后已保存凭据的真实认证通过（504毫秒），未发送邮件或修改数据库。实际测试邮件发送/回执/投递仍需下一项确认，不能将认证通过等同于发送验收通过。

### 2026-09-12 本地生产验收（优先于下方历史记录）

本次仅验收本地生产构建，不部署云端。用户确认真实模型验收累计预算30美元，官方实际费率优先，无法获取时用OpenAI参考费率并标明；超过上限须重新确认。独立验收预算不得覆盖用户日常预算。已完成586项测试、18项隔离浏览器测试、14组真实登录后桌面/移动端检查、类型检查和生产构建。停用账户旧会话与畸形登录请求防护已修复；Kimi K3输出限制字段和计费门禁同步更新。

真实模型接口4/5通过，DeepSeek返回402；SMTP发送前连接校验失败，未产生测试发送记录。当前验收预留10/30美元，服务商返回费用与参考估算分开记录。全产品付费费率规则仍未放行，完整业务链路与最终采用遥测尚未全部验收，不能将接口探针通过称为全面上线通过。完整费率来源、费用及恢复节点见 `docs/LOCAL_PRODUCTION_ACCEPTANCE_2026-09-12.md`。

继续开发补充：策略现在检查知识/长期记忆/关系依赖版本；生成/修改失败尝试进入任务中心；任务详情优先显示结构化摘要。新搜索交付统计拆分新增、更新及其中主角色变化，写入与结果同一事务并防重复统计；人工覆盖保留但评分关联可更新。预算门禁与多国家同域名成员关系仍未验收，不视为全部开发完成。

恢复后进展：全球概览已增加五类任务全量运行计数，不按任务列表第一页推算；手动公司支持可选主角色；公司开发页展示已关联往来（20条分页、原文按需读取、不触发同步或学习）。493项自动测试通过，真实浏览器与发信验收仍未完成。

2026-09-11恢复补充：意图识别和库内查询新增用户隔离的持久用量记录，含调用前未结算状态、token/重试/耗时及下游边界。`/api/tasks/usage`仅汇总这两类新接入环节，不代表完整产品账单；预算输入和美元硬门禁仍未完成。

兼容迁移保留数据；中文主界面、真实空/错误/保存状态；桌面和平板及窄屏核心操作可用。前后端同步，不交付无效按钮。分阶段检查并提交到 origin/main；相关工作流和效率台账同阶段更新。每个工作流记录输入、有效/下游使用输出、费用、耗时、重试、弃用原因和优化机会。发信测试不向真实客户投递，实投只用用户指定测试邮箱。

| 阶段 | 状态 |
|---|---|
| 国家导航与线索 | 国家路由、资格分区、高级筛选/排序、批量操作、保存后自动刷新已实现；刷新为全量读取，非服务端增量。新入库公司不自动进开发名单，历史不批量改动 |
| 公司详情、人工覆盖与关系图 | 三页签、历史评分与人工覆盖、关系保存/确认/否定、手动公司、聚合/缩放/聚焦、主动存量证据分析及缓存已实现；关系建议采用链路、多国家成员关系审计待补 |
| 机会、单封发送与跟进 | 七阶段、下一步/日期、SMTP确认/加密历史/分页/不明发送核实、同公司复用、私有风格与有界往来跟进、加密草稿载入及发送预留采用记录已实现；策略上下文变化提醒已接，关系/知识版本变化提醒待补；真实邮箱联调待验收 |
| 任务、知识与邮箱管理 | 五类任务共同抽屉/独立地址、搜索/联系人明细分页、安全checkpoint暂停恢复已实现；知识分区/冲突/内容版本、记忆管理、邮箱范围同步/断开/本地删除/公司关联已实现；完整预算、失败尝试及持久成本遥测仍待补 |
| AI首页与概览 | Kimi多轮库内查询/策略/跟进入口已接，不自动生成或发送；真实公司统计已接，运行任务数及预算修改未完成 |
| 全流程联调与验收 | 479项自动测试与数据库读/隔离检查通过；登录后响应式/键盘/视觉验收、指定邮箱实投尚未完成。2026-09-11按用户要求暂停，详见 docs/RESUME_PRODUCT_UI_2026-09-11.md |
