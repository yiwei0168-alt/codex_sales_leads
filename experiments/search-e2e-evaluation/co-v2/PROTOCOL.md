# Frozen protocol: Colombia search E2E v2.0.12

v2.0.1 supersedes the execution guard in v2.0.0 after the first Retail attempt exposed a Kimi constraint-copy failure before Product discovery. Kimi remains mandatory for lightweight intent/template-fit recognition, while the already user-confirmed preregistered country, count and category are authoritative for execution. Divergence is retained as a warning and efficiency loss. The valid unchanged Gemini control and all sunk cost remain in the same ledger.

v2.0.2 supersedes only intent recovery after the next Retail attempt returned truncated JSON at the exact former 2,000-token ceiling. Invalid structured output now receives one bounded Kimi retry with aggregate attempt usage; the light-response ceiling is 4,000 tokens. If Kimi remains invalid/unavailable, disclosed DeepSeek Flash supplies an equivalent structured intent plan. Deterministic fallback remains an observable degraded state and is not accepted in the formal run. Search, evidence, scoring, samples and win gates remain unchanged.

v2.0.3 supersedes only provider recovery after CO-retail discovered 31 unique candidates and enriched/corrected 27 but returned zero final candidates. The DeepSeek direct endpoint reported `Insufficient Balance`, no explicitly configured fallback route existed, and deterministic correction correctly left primary roles unresolved. Public-evidence DeepSeek work may now use the same DeepSeek tier through the configured OpenRouter gateway after the bounded primary failure. Qualification remains private when user cooperation-path memory is present; the automatic OpenRouter route is public-only. Brave uses its supported `ALL` market parameter when a country code such as `CO` is unavailable, while keeping Colombia in the query. The interrupted cell resumes from its same-run intent, RAG, playbook, discovery, evidence and supplemental-evidence artifacts; it may repeat semantic correction/scoring but must not repeat search or Tavily acquisition for those candidates. SearchAPI's exhausted monthly quota is retained as an observed provider failure, not a no-value batch. Samples, metrics, scoring and win gates remain unchanged.

v2.0.4 supersedes only structured-model runtime control after the v2.0.3 recovery still returned zero Product candidates. OpenRouter had applied reasoning by default to DeepSeek Flash and Pro, consuming 21,795 reasoning tokens across the observable late-round model events and causing routine 75-second timeouts. The correction and qualification catch paths then incorrectly promoted transport/provider failures to Pro without a valid semantic prediction of an eight-point score change or critical-state resolution. All automatic public DeepSeek fallback requests now set reasoning effort to none; one provider instance shares the direct-provider circuit across the complete Product cell; a malformed schema receives at most one same-tier, single-candidate repair; and transport, balance or timeout failures never qualify for Pro escalation. Pro remains available only after a valid routine result explicitly meets the frozen material-change rule. Recovery reuses all same-run acquisition and correction evidence. Samples, metrics, scoring, acquisition routes and win gates remain unchanged.

v2.0.5 supersedes runtime batching and the recovery boundary after v2.0.4 reclassified 51 cached candidates but timed out its oversized correction batches, then incorrectly entered a fifth discovery round. Correction evidence packets now retain at most ten relevance-prioritized current sources with excerpts capped at 1,200 characters and form batches under 28,000 serialized input characters. Formal cache recovery uses one candidate per call and two-way concurrency; score-only qualification uses the same recovery limits. Request-scoped timeouts no longer open a provider-wide fallback circuit. Most importantly, `--resume-product` is acquisition-closed: it may re-run cached semantic correction and scoring, but cannot start another discovery, homepage, Tavily or supplementation round. The USD 0.368 acquisition spend caused by the v2.0.4 defect remains in the ledger. Samples, metrics, scoring, ordinary non-recovery search routes and win gates remain unchanged.

v2.0.6 supersedes search localization and primary-role normalization after v2.0.5 successfully reclassified the cached pool but retained only 6/50 Retailer/E-tailer candidates. The runtime trace proved that every Spanish market was using Mexico's city sequence: Colombia queries combined `Ciudad de México`, `Guadalajara`, `Monterrey`, `Puebla/Querétaro` and `Tijuana/León/Mérida` with the word Colombia. Exa company-profile URLs on `exa.ai` also became identity domains and merged unrelated companies, while missing Latin American public-suffix rules generated identities such as `com.pe`. Finally, 19 candidates were marked Hybrid, including single-family Retailer+E-tailer companies such as Alkosto; Hybrid is now valid only across two or more role families. Search focus is keyed by country code, Colombia uses Bogotá/Medellín/Cali/Barranquilla/Bucaramanga/Cartagena, Exa-owned profiles cannot establish a company domain, Latin American registrable domains are normalized, and same-family Hybrid is converted to a concrete model-ordered supported role. A documented `--repair-search` extension retains all prior eligible outputs and evidence, retries only incomplete cached assessments, excludes all cached domains, and runs up to five corrected acquisition rounds. All sunk cost remains in the ledger; sample, control output, scoring, metrics and win gates remain unchanged.

v2.0.7 supersedes evidence affiliation and target-country gating after the v2.0.6 extension returned 11/50 Retailer/E-tailer records but exposed invalid evidence lineage. Earlier public-suffix collapse had sent Tavily requests with targets such as `com.pe` and `co.cr`, causing unrelated sites across those suffixes to be labeled official evidence. Supplemental correction results could also be retained without naming the candidate, allowing another Bogotá retailer to prove a Mexico company was in Colombia. The final scorer was then allowed to override correction evidence and mark target-country presence supported. The v2.0.6 Product output is therefore invalid for outcome measurement, is archived, and is restarted cold; every prior dollar remains an anomaly/debug cost and the frozen Gemini output is not rerun. v2.0.7 requires a valid registrable company domain before paid official-site evidence, filters independent evidence by candidate affiliation, defines country-presence findings strictly against the target market, and deterministically prevents scoring from overriding those findings. It also bounds malformed light-gate fields, excludes provider profiles and domainless IDs from valid discovery yield, ignores model-failed rounds for exhaustion, and permits up to ten rounds for an explicit 50-company target. Arms, scoring weights, blind sample, win gates and budget thresholds remain unchanged.

v2.0.8 supersedes only model-route recovery after the clean v2.0.7 Retail restart left 21/47 corrected candidates as deterministic `Unresolved` fallbacks and three assessments incomplete. Direct DeepSeek remains primary. For public packets, at most two equivalent fallbacks are attempted in order: the same DeepSeek tier through OpenRouter with a 45-second route deadline, then OpenAI `gpt-4o-mini` for routine work or `gpt-4o` for a separately policy-qualified escalation with a 25-second route deadline. A stalled route therefore cannot consume the complete request budget. Private workspace packets never use these automatic public routes, and embeddings never fail over. The documented `--repair-incomplete` run retries only deterministic correction fallbacks and missing, retry-required or materially affected scores, rebuilds the ranking from all cached records, and cannot issue discovery, homepage, Tavily or other evidence-acquisition calls. Every prior dollar remains in the ledger; arms, evidence, scoring rules, samples, win gates and budget thresholds remain unchanged.

v2.0.9 supersedes deterministic semantic consistency after v2.0.8 raised Retail output from 6 to 13 at USD 0.033937 incremental semantic cost. Mercado Libre was explicitly identified by its own supported findings as a third-party seller marketplace but was still labeled E-tailer, contradicting the frozen business-model definition. Techniservice received 15/15 scale despite an `Unknown` scale class and 15/15 buying influence despite a deterministic 9/15 evidence cap. The correction normalizer now removes Retailer/E-tailer roles when supported findings explicitly establish a third-party marketplace. Qualification caps unknown scale at neutral 8/15—not zero—and caps buying influence at the existing deterministic evidence level before totals and account tiers are computed. The `--repair-consistency` path reuses all model semantic output and acquisition, applies those deterministic rules to cached records, and makes no external call. All costs and superseded outputs remain visible; arms, evidence, samples, win gates and budget thresholds remain unchanged.

v2.0.10 supersedes only the provider-gap fallback policy after the corrected Retail artifact remained 12/50. Across eight cached rounds, SearchAPI produced zero results: five requests failed with the confirmed monthly-quota HTTP 429 and 35 calls were skipped by the provider circuit/cooldown. Both national Retail and E-tail core tracks depended on that index, while complementary Brave, Exa and Places routes could not replace its broad merchant recall. Hybrid-search v1.6.0 therefore inserts Gemini Full after those SearchAPI steps—and the equivalent Reseller tracks—but executes it only when the corresponding SearchAPI track has failed, a provider/invocation circuit is open, or recovery is in cooldown. When SearchAPI is healthy the backup is recorded as a zero-cost skip. This is not Gemini Product, does not call Tavily discovery and does not run concurrently as a duplicate search mechanism. The documented `--repair-search` path retains all valid candidates, evidence, role corrections and scores, excludes cached domains, and pays only for newly acquired work. Arms, scoring, evidence rules, samples, win gates and budget thresholds remain unchanged.

v2.0.11 supersedes the search-extension cache boundary after the first v2.0.10 extension exposed a mismatch between its documented contract and implementation. The session did not preload prior discovered/enriched/corrected domains, so 25 of 81 light-gate/evidence inputs were already processed entities. They did not overwrite prior corrections or scores, so the 14 final candidates remain valid, but the repeated gate, Tavily and correction work violates the mandatory within-task cache rule and makes the extension cost-inefficient. The extension now preloads domains from prior discovered candidates, rejected candidates, enriched candidates and corrected identities before any route executes. Cached entities are filtered before the semantic gate and cannot re-enter evidence, correction or scoring. All USD 9.448230 incurred cost remains in the budget ledger. No further paid work may execute while the forecast pause is active; resumption requires the protocol's explicit budget decision. Arms, scoring, search routes, evidence rules, samples and win gates remain unchanged.

v2.0.12 records the user's explicit budget decision. CO Retail is frozen at 14/50 and receives no additional search. The USD 50 hard cap and USD 10/20/30 reviews remain unchanged. A versioned `--phase=resume-budget` command records the authorization, baseline USD 45.7926 expected/USD 59.6814 upper forecast and permits exactly one next frozen cell start (`CO-distribution`). During that cell, cost events and any crossed checkpoint are still persisted; a forecast warning is evaluated again after the cell, while the absolute hard cap remains enforceable during execution. This prevents a forecast-only pause from discarding partial in-memory cell work. The authorization cannot bypass unpriced cost, a next-call hard-cap risk or the USD 50 hard stop. Arms, Product mechanism, samples and win gates remain unchanged.

## Hypothesis and arms

The experiment tests whether the current product search, evidence, primary-role correction and scoring pipeline produces higher sales-lead value than an un-tuned Gemini Full baseline in Colombia.

- Control: one Gemini `gemini-3.6-flash` Interactions call with Google Search per category, no follow-up or evidence repair before output freeze, provider order preserved.
- Product: the current versioned hybrid search, fresh-evidence, primary-role correction and score-only qualification workflow. No cooperation path, strategy, email or contact output.
- Both arms are cold start: no prior candidates, evidence, page cache, scores, private company memory or cross-arm visibility. Frozen product knowledge and policies are allowed. Within-run call, identity, evidence and correction caches remain mandatory to prevent repeated work.

## Sample and execution

Colombia is the only market. The four cells are Distributor/VAD, Reseller/VAR, Retailer/E-tailer and SI/MSP. Each arm receives 50 slots per cell, for 200 slots per arm and 400 total. Missing, duplicate, invalid and wrong-primary-role slots receive zero utility.

Cell order and arm-start order are frozen in `config/experiment.v2.0.0.json`. Arms start together inside a cell. Runtime is recorded separately and is not a quality gate.

## Shared evaluation and metrics

Product candidates keep their current-run evidence and score. After both result sets are frozen, only Gemini-unique companies receive the identical fresh-evidence, role-correction and score-only mechanism. Overlap reuses one record, so no candidate is re-enriched or rescored merely because both arms found it.

Primary metric: macro mean Slot Utility@50. Diagnostics include NDCG@50, valid/65+/75+ counts, unique high-value companies, per-category deltas, provider fractional contribution and a 10,000-iteration cell-stratified bootstrap.

Frozen win gates: macro gain at least 5 points; Product wins at least 3/4 cells; market delta at least -3; worst-cell delta at least -5; more unique 75+ companies; bootstrap lower bound above zero; blind calibration passes.

## Blind audit v2.1

Each cell contributes six deterministic representative cases and two diagnostic stress cases, for 24 representative and 8 stress packets. Representative cases alone control gates. Evidence selection is independent of scoring citations and cannot trigger new search.

Claude Opus 5 and OpenAI GPT-5.6-sol independently judge every packet without Web search. A DeepSeek Pro arbitrator is called only when scores differ by at least eight points or a critical identity, market, role-family, threshold or eligibility state conflicts. Cached valid decisions are reused before retry. If a required provider is unavailable, the run records the error and uses the previously authorized in-conversation Codex fallback only through a separately frozen decision checkpoint; no silent same-family substitution is allowed.

## Budget and telemetry

USD 50 is a hard experiment-wide cap across Control, Product and shared evaluation. Mandatory reviews occur at USD 10, 20 and 30. Every review records incurred and forecast cost, ledger/stage splits, input/raw/valid/downstream-used volume, discard reasons, latency and retries. If the expected or conservative completion forecast may exceed USD 50, the runner pauses before further paid work for user confirmation; the experiment design is not silently changed.

Every workflow stage records the permanent efficiency fields required by `AGENTS.md`. Unexpected underfill, provider failure, unpriced calls, schema failures, abnormal duplicate/discard rates or cost spikes are reported immediately and preserved rather than erased. Provider preflight must include a minimal structured inference, rather than treating the presence of an API key as proof of runtime availability.
